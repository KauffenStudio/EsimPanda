---
phase: 05-user-accounts
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/auth/forgot-password-form.tsx
  - src/components/auth/reset-password-form.tsx
  - src/components/auth/account-conversion-cta.tsx
  - src/app/[locale]/forgot-password/page.tsx
  - src/app/[locale]/reset-password/page.tsx
  - src/lib/email/send-reset.ts
  - src/lib/email/templates/reset-email.tsx
  - src/lib/email/__tests__/send-reset.test.ts
  - src/components/delivery/delivery-page.tsx
autonomous: true
requirements: [ACC-01, ACC-03]

must_haves:
  truths:
    - "User can request password reset at /forgot-password and sees 'Check your email' confirmation"
    - "User can set new password at /reset-password after clicking email link"
    - "Password reset email is branded with eSIM Panda header, Bambu, and blue accent"
    - "Guest user sees 'Save your eSIM' CTA on delivery page with password-only field"
    - "Guest conversion creates account using email from checkout (hidden field)"
    - "Successful conversion shows 'Account created!' with Bambu bounce"
    - "User can dismiss conversion CTA with 'Skip for now'"
  artifacts:
    - path: "src/app/[locale]/forgot-password/page.tsx"
      provides: "Forgot password page route"
      contains: "ForgotPasswordForm"
    - path: "src/app/[locale]/reset-password/page.tsx"
      provides: "Reset password page route"
      contains: "ResetPasswordForm"
    - path: "src/components/auth/forgot-password-form.tsx"
      provides: "Email-only form to request reset"
      exports: ["ForgotPasswordForm"]
    - path: "src/components/auth/reset-password-form.tsx"
      provides: "New password + confirm form"
      exports: ["ResetPasswordForm"]
    - path: "src/components/auth/account-conversion-cta.tsx"
      provides: "Delivery page guest-to-account CTA"
      exports: ["AccountConversionCTA"]
    - path: "src/lib/email/send-reset.ts"
      provides: "Password reset email sender via Resend"
      exports: ["sendResetEmail"]
    - path: "src/lib/email/templates/reset-email.tsx"
      provides: "React Email template for password reset"
      exports: ["ResetEmail"]
  key_links:
    - from: "src/components/auth/forgot-password-form.tsx"
      to: "src/lib/auth/actions.ts"
      via: "resetPassword server action"
      pattern: "import.*resetPassword.*from.*auth/actions"
    - from: "src/components/auth/reset-password-form.tsx"
      to: "src/lib/auth/actions.ts"
      via: "updatePassword server action"
      pattern: "import.*updatePassword.*from.*auth/actions"
    - from: "src/components/auth/account-conversion-cta.tsx"
      to: "src/lib/auth/actions.ts"
      via: "convertGuestToAccount server action"
      pattern: "import.*convertGuestToAccount.*from.*auth/actions"
    - from: "src/components/delivery/delivery-page.tsx"
      to: "src/components/auth/account-conversion-cta.tsx"
      via: "AccountConversionCTA rendered after setup guide"
      pattern: "AccountConversionCTA"
---

<objective>
Build password reset flow (forgot + reset pages + branded email), guest-to-account conversion CTA on delivery page, and wire conversion into the delivery experience.

Purpose: Implements ACC-01 (guest-to-account conversion) and ACC-03 (password reset). Completes the full auth feature set.
Output: Working forgot/reset password pages, branded reset email template, conversion CTA on delivery page.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-accounts/05-CONTEXT.md
@.planning/phases/05-user-accounts/05-UI-SPEC.md
@.planning/phases/05-user-accounts/05-01-SUMMARY.md

<interfaces>
<!-- Auth actions from Plan 01 -->
From src/lib/auth/actions.ts:
```typescript
export async function resetPassword(formData: FormData): Promise<AuthResult>;
export async function updatePassword(formData: FormData): Promise<AuthResult | void>;
export async function convertGuestToAccount(formData: FormData): Promise<AuthResult>;
```

From src/lib/auth/types.ts:
```typescript
export interface AuthResult { success?: boolean; error?: string; }
```

From src/lib/auth/mock.ts:
```typescript
export function isMockMode(): boolean;
export const MOCK_USER: { id: string; email: string; created_at: string; };
```

<!-- Email sending pattern from Phase 4 -->
From src/lib/email/send-delivery.ts:
```typescript
// Lazy-init Resend pattern:
let _resend: Resend | null = null;
function getResend(): Resend { if (!_resend) { _resend = new Resend(process.env.RESEND_API_KEY); } return _resend; }
// Mock mode check:
if (process.env.NEXT_PUBLIC_STRIPE_MOCK === 'true') { console.log('[MOCK]...'); return { id: 'mock_email_id' }; }
```

From src/lib/email/templates/delivery-email.tsx:
```typescript
// React Email template with branded header:
// Blue (#2979FF) header bar with white "eSIM Panda" text
// Plus Jakarta Sans font family
// 600px max-width container
```

<!-- Delivery page structure -->
From src/components/delivery/delivery-page.tsx:
```typescript
export function DeliveryPage({ paymentIntentId, email }: DeliveryPageProps) {
  // Renders: ProvisioningState -> EsimCredentials -> SetupGuide
  // email prop available from checkout
  // Conversion CTA goes AFTER SetupGuide component
}
```

From src/stores/delivery.ts:
```typescript
// Has status, data, email fields
// email is set during checkout flow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forgot/reset password forms, pages, and reset email template</name>
  <files>
    src/components/auth/forgot-password-form.tsx
    src/components/auth/reset-password-form.tsx
    src/app/[locale]/forgot-password/page.tsx
    src/app/[locale]/reset-password/page.tsx
    src/lib/email/send-reset.ts
    src/lib/email/templates/reset-email.tsx
    src/lib/email/__tests__/send-reset.test.ts
  </files>
  <read_first>
    src/components/auth/login-form.tsx
    src/lib/auth/actions.ts
    src/lib/email/send-delivery.ts
    src/lib/email/templates/delivery-email.tsx
    src/components/bambu/bambu-welcome.tsx
    src/components/ui/input.tsx
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    messages/en.json
    .planning/phases/05-user-accounts/05-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/auth/forgot-password-form.tsx`:
- 'use client' component
- Uses `useTranslations('auth')` for all copy
- Uses `useActionState` to call `resetPassword` server action
- Two states: "form" (default) and "sent" (after successful submit)

**Form state:**
- Heading: t('forgot.heading') = "Reset Password" (24px bold)
- Body text: t('forgot.body') = "Enter your email and we'll send you a link to reset your password." (16px, #666666)
- Email input: type="email", required, uses existing Input component
- Submit button: primary Button, text t('forgot.submit') = "Send Reset Link", full width
- Loading state: button disabled, BambuLoading inline
- Footer: "Back to Log In" link to `/${locale}/login`, centered, 14px

**Sent state (after action returns { success: true }):**
- Heading: t('forgot.successHeading') = "Check Your Email" (24px bold)
- Body: t('forgot.successBody') = "If an account exists with that email, we've sent a reset link. Check your inbox." (16px)
- Bambu encouraging pose (use BambuWelcome variant="wave" as placeholder)
- "Back to Log In" link

Same Card layout as login-form (max-width 400px, 32px/24px padding).
Export `ForgotPasswordForm`.

Create `src/components/auth/reset-password-form.tsx`:
- 'use client' component
- Uses `useActionState` to call `updatePassword` server action
- Two fields:
  1. "New password" (t('field.newPassword')): password input with show/hide toggle, min 8 chars
  2. "Confirm password" (t('field.confirmPassword')): password input with show/hide toggle
- Client-side validation: check passwords match before submit, show t('error.passwordMismatch') = "Passwords don't match." if mismatch
- Client-side validation: check min 8 chars, show t('error.passwordShort') if < 8
- Submit button: primary Button, text t('reset.submit') = "Set New Password"
- Error display: role="alert", #E53935, 14px
- Heading: t('reset.heading') = "Set New Password"
- On success: server action redirects to login page (handled by action)
- Bambu encouraging pose above form
- Export `ResetPasswordForm`

Create `src/app/[locale]/forgot-password/page.tsx`:
- Server component
- Renders BambuWelcome + ForgotPasswordForm, centered, same layout as login page
- Page title: "Reset Password"

Create `src/app/[locale]/reset-password/page.tsx`:
- Server component
- Renders BambuWelcome + ResetPasswordForm, centered, same layout as login page
- Page title: "Set New Password"

Create `src/lib/email/templates/reset-email.tsx`:
- React Email component following exact pattern from delivery-email.tsx
- Props: `{ resetUrl: string }`
- Structure (matching UI-SPEC email template):
  - Blue header bar: backgroundColor '#2979FF', height 64px, white "eSIM Panda" text 22px bold, centered
  - Body section: padding '32px 40px', textAlign 'center'
  - Heading: "Reset Your Password", 24px bold, #1A1A1A
  - Body text: "Click below to set a new password for your account.", 16px, #666666
  - CTA button: href={resetUrl}, backgroundColor '#2979FF', white text, 16px bold, height 48px, borderRadius 8px, padding '0 32px'
  - Expiry note: "This link expires in 1 hour. If you didn't request this, ignore this email.", 14px, #999999
  - Font family: 'Plus Jakarta Sans, Arial, sans-serif' throughout
  - Max-width: 600px container
- Export `ResetEmail`

Create `src/lib/email/send-reset.ts`:
- Follow exact pattern from send-delivery.ts (lazy Resend init, mock mode check)
- Export `sendResetEmail({ to, resetUrl }: { to: string; resetUrl: string })`
- Mock mode: `console.log('[MOCK] Would send reset email to:', to)` and return `{ id: 'mock_reset_email_id' }`
- Production: send via Resend with:
  - from: 'eSIM Panda <noreply@esimpanda.com>'
  - to: params.to
  - subject: 'Reset your eSIM Panda password'
  - react: ResetEmail({ resetUrl: params.resetUrl })

Create `src/lib/email/__tests__/send-reset.test.ts`:
- Mock resend module
- Test: sendResetEmail in mock mode returns { id: 'mock_reset_email_id' } without calling Resend
- Test: sendResetEmail in production mode calls Resend.emails.send with correct from/to/subject
- Test: resetUrl is passed through to the React Email template
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx vitest run src/lib/email/__tests__/send-reset.test.ts --reporter=verbose && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/forgot-password-form.tsx contains `export function ForgotPasswordForm`
    - src/components/auth/forgot-password-form.tsx contains `import { resetPassword } from`
    - src/components/auth/forgot-password-form.tsx contains `successHeading` or `Check Your Email`
    - src/components/auth/reset-password-form.tsx contains `export function ResetPasswordForm`
    - src/components/auth/reset-password-form.tsx contains `import { updatePassword } from`
    - src/components/auth/reset-password-form.tsx contains `passwordMismatch` or `Passwords don't match`
    - src/app/[locale]/forgot-password/page.tsx contains `ForgotPasswordForm`
    - src/app/[locale]/reset-password/page.tsx contains `ResetPasswordForm`
    - src/lib/email/templates/reset-email.tsx contains `export function ResetEmail`
    - src/lib/email/templates/reset-email.tsx contains `#2979FF`
    - src/lib/email/templates/reset-email.tsx contains `Reset Your Password`
    - src/lib/email/templates/reset-email.tsx contains `Plus Jakarta Sans`
    - src/lib/email/send-reset.ts contains `export async function sendResetEmail`
    - src/lib/email/send-reset.ts contains `[MOCK]`
    - src/lib/email/send-reset.ts contains `noreply@esimpanda.com`
    - src/lib/email/__tests__/send-reset.test.ts exits 0
  </acceptance_criteria>
  <done>Forgot password page shows email form then "Check your email" confirmation. Reset password page has new password + confirm fields with match validation. Branded reset email matches delivery email style. Email sending follows existing Resend pattern with mock mode. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Guest-to-account conversion CTA on delivery page</name>
  <files>
    src/components/auth/account-conversion-cta.tsx
    src/components/delivery/delivery-page.tsx
  </files>
  <read_first>
    src/components/delivery/delivery-page.tsx
    src/components/delivery/setup-guide.tsx
    src/lib/auth/actions.ts
    src/stores/auth.ts
    src/stores/delivery.ts
    src/components/ui/input.tsx
    src/components/ui/button.tsx
    src/components/bambu/bambu-welcome.tsx
    src/components/bambu/bambu-success.tsx
    messages/en.json
    .planning/phases/05-user-accounts/05-UI-SPEC.md
    .planning/phases/05-user-accounts/05-CONTEXT.md
  </read_first>
  <action>
Create `src/components/auth/account-conversion-cta.tsx`:
- 'use client' component
- Props: `{ email: string }` (the guest's email from checkout)
- Uses `useTranslations('auth')` for all copy
- Uses `useAuthStore` to check if user is already logged in (hide CTA if so)
- Uses `useActionState` to call `convertGuestToAccount` server action

**Three states managed by local useState:**

1. **"form" state (default):**
   - Card with background #E3F0FF (accent-soft), border-radius 10px, padding 24px
   - Small Bambu icon/emoji at top
   - Heading: t('convert.heading') = "Save your eSIM" (24px bold)
   - Body: t('convert.body') = "Create an account to access your eSIM anytime, manage your plans, and get top-up reminders." (16px, #666666)
   - Read-only email display: show the email as text (not editable), 14px, gray-600
   - Hidden input: name="email" value={email} type="hidden"
   - Password input: label t('field.choosePassword') = "Choose a password", min 8 chars, show/hide toggle (Eye/EyeOff), uses Input component
   - Client-side validation: min 8 chars, show t('error.passwordShort') if < 8
   - Submit button: primary Button, text t('convert.submit') = "Create Account", full width
   - Loading state: button disabled, BambuLoading inline
   - Dismiss link: t('convert.skip') = "Skip for now", 14px, text-gray-600, centered below button. On click: set state to "dismissed"
   - Error display: role="alert", #E53935, 14px. On 'already_registered' error, show t('error.conversionExists') = "This email already has an account. Log in to link your eSIM." with link to /login

2. **"success" state (after action returns { success: true }):**
   - Card collapses current content, shows:
   - BambuWelcome variant="bounce" at 80px
   - t('convert.success') = "Account created!" in success green (#43A047), 24px bold
   - Checkmark icon (Lucide Check)
   - Motion animation: spring collapse of form content + fade in of success content (stiffness: 300, damping: 20)

3. **"dismissed" state:**
   - Render nothing (component returns null)

- If useAuthStore shows user is already logged in, render nothing
- Export `AccountConversionCTA`

Update `src/components/delivery/delivery-page.tsx`:
- Import AccountConversionCTA from '@/components/auth/account-conversion-cta'
- Import useAuthStore from '@/stores/auth'
- After the SetupGuide component renders (when status === 'ready'), add AccountConversionCTA
- Pass the email prop: use the `email` prop from DeliveryPageProps (already available)
- Only render AccountConversionCTA when:
  1. status === 'ready' (eSIM has been provisioned)
  2. email is available (truthy)
  3. User is NOT already logged in (check useAuthStore user === null)
- Position: after SetupGuide, before any existing footer content
- Wrap in a Motion div with initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 1 }} to appear 1 second after the eSIM content loads (let user see their eSIM first)
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit --pretty 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/account-conversion-cta.tsx contains `export function AccountConversionCTA`
    - src/components/auth/account-conversion-cta.tsx contains `import { convertGuestToAccount } from`
    - src/components/auth/account-conversion-cta.tsx contains `useAuthStore`
    - src/components/auth/account-conversion-cta.tsx contains `E3F0FF`
    - src/components/auth/account-conversion-cta.tsx contains `type="hidden"` (hidden email field)
    - src/components/auth/account-conversion-cta.tsx contains `Skip for now`  or `convert.skip`
    - src/components/auth/account-conversion-cta.tsx contains `Account created` or `convert.success`
    - src/components/auth/account-conversion-cta.tsx contains `already_registered` or `conversionExists`
    - src/components/auth/account-conversion-cta.tsx contains `#43A047` (success green)
    - src/components/delivery/delivery-page.tsx contains `AccountConversionCTA`
    - src/components/delivery/delivery-page.tsx contains `import.*AccountConversionCTA`
    - TypeScript compilation produces no errors related to auth or delivery components
    - All vitest tests pass
  </acceptance_criteria>
  <done>Delivery page shows "Save your eSIM" CTA after QR/setup content. Password-only field with email from checkout as hidden field. Success state shows Bambu bounce + "Account created!". Dismiss with "Skip for now". CTA hidden when already logged in. Existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx vitest run --reporter=verbose` -- all tests pass including reset email tests
- /en/forgot-password renders email form, shows "Check your email" after submit
- /en/reset-password renders new password + confirm password form
- Delivery page shows conversion CTA after eSIM is ready
- Conversion CTA has password-only field with email pre-filled from checkout
</verification>

<success_criteria>
- /en/forgot-password renders with anti-enumeration success message
- /en/reset-password renders with password match validation
- Reset email template matches delivery email branding (blue header, Plus Jakarta Sans)
- Delivery page shows "Save your eSIM" CTA after successful provisioning
- Conversion uses email from checkout (hidden field), only asks for password
- Success shows Bambu bounce + "Account created!"
- "Skip for now" dismisses the CTA
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-accounts/05-03-SUMMARY.md`
</output>
