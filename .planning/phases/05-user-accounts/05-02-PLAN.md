---
phase: 05-user-accounts
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/bambu/bambu-welcome.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/components/auth/user-menu.tsx
  - src/components/auth/auth-provider.tsx
  - src/app/[locale]/login/page.tsx
  - src/app/[locale]/signup/page.tsx
  - src/components/layout/header.tsx
  - src/app/[locale]/layout.tsx
autonomous: true
requirements: [ACC-02, ACC-04]

must_haves:
  truths:
    - "User can navigate to /login and see a login form with email and password fields"
    - "User can navigate to /signup and see a signup form with email and password fields"
    - "Login form submits to login server action and shows errors inline"
    - "Signup form submits to signup server action and shows errors inline"
    - "Header shows 'Log in' button when not authenticated"
    - "Header shows avatar circle with email initial and dropdown menu when authenticated"
    - "Bambu welcoming wave appears on login and signup pages"
    - "Auth store initializes on app mount via AuthProvider"
  artifacts:
    - path: "src/app/[locale]/login/page.tsx"
      provides: "Login page route"
      contains: "LoginForm"
    - path: "src/app/[locale]/signup/page.tsx"
      provides: "Signup page route"
      contains: "SignupForm"
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form with email + password"
      exports: ["LoginForm"]
    - path: "src/components/auth/signup-form.tsx"
      provides: "Signup form with email + password"
      exports: ["SignupForm"]
    - path: "src/components/auth/user-menu.tsx"
      provides: "Header user avatar + dropdown"
      exports: ["UserMenu"]
    - path: "src/components/auth/auth-provider.tsx"
      provides: "Client wrapper that initializes auth store"
      exports: ["AuthProvider"]
    - path: "src/components/bambu/bambu-welcome.tsx"
      provides: "Bambu welcoming wave pose"
      exports: ["BambuWelcome"]
    - path: "src/components/layout/header.tsx"
      provides: "Updated header with UserMenu"
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/lib/auth/actions.ts"
      via: "form action import"
      pattern: "import.*login.*from.*auth/actions"
    - from: "src/components/auth/signup-form.tsx"
      to: "src/lib/auth/actions.ts"
      via: "form action import"
      pattern: "import.*signup.*from.*auth/actions"
    - from: "src/components/auth/user-menu.tsx"
      to: "src/stores/auth.ts"
      via: "useAuthStore hook"
      pattern: "import.*useAuthStore.*from.*stores/auth"
    - from: "src/components/layout/header.tsx"
      to: "src/components/auth/user-menu.tsx"
      via: "UserMenu import"
      pattern: "import.*UserMenu.*from.*auth/user-menu"
---

<objective>
Build the login page, signup page, header user menu, Bambu welcome pose, and AuthProvider. Users can log in, create accounts, and see their auth state reflected in the header.

Purpose: Implements ACC-02 (login) and ACC-04 (session visibility in UI). Forms connect to the server actions created in Plan 01.
Output: Working /login and /signup pages, header with auth state, AuthProvider wrapping the app.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-accounts/05-CONTEXT.md
@.planning/phases/05-user-accounts/05-UI-SPEC.md
@.planning/phases/05-user-accounts/05-01-SUMMARY.md

<interfaces>
<!-- Auth actions created in Plan 01 -->
From src/lib/auth/actions.ts:
```typescript
export async function login(formData: FormData): Promise<AuthResult | void>;
export async function signup(formData: FormData): Promise<AuthResult | void>;
export async function signOut(): Promise<void>;
```

From src/lib/auth/types.ts:
```typescript
export interface AuthResult { success?: boolean; error?: string; }
export interface AuthUser { id: string; email: string; created_at: string; user_metadata?: Record<string, unknown>; }
```

From src/stores/auth.ts:
```typescript
export const useAuthStore = create<AuthState>((set, get) => ({
  user: User | null,
  loading: boolean,
  initialized: boolean,
  initialize: () => Promise<void>,
  setUser: (user: User | null) => void,
  clear: () => void,
}));
```

From src/components/bambu/bambu-base.tsx (existing pattern):
```typescript
// Bambu poses are SVG-based components with Motion spring animations
// Each pose exports a single component with optional size prop
```

From src/components/layout/header.tsx (current):
```typescript
export function Header() {
  // Currently has: Logo, desktop nav links, ThemeToggle
  // Needs: UserMenu added between nav and ThemeToggle
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bambu welcome pose, auth forms, and AuthProvider</name>
  <files>
    src/components/bambu/bambu-welcome.tsx
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
    src/components/auth/auth-provider.tsx
  </files>
  <read_first>
    src/components/bambu/bambu-success.tsx
    src/components/bambu/bambu-base.tsx
    src/components/ui/input.tsx
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/layout/page-transition.tsx
    src/lib/auth/actions.ts
    src/lib/auth/types.ts
    src/stores/auth.ts
    messages/en.json
    .planning/phases/05-user-accounts/05-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/bambu/bambu-welcome.tsx`:
- Follow the same pattern as bambu-success.tsx and bambu-base.tsx
- SVG-based panda with a waving arm pose
- Two variants via a `variant` prop: "wave" (default, for login/signup pages) and "bounce" (for successful signup)
- Wave variant: arm animates with Motion spring (stiffness: 300, damping: 15) in a gentle wave
- Bounce variant: entire panda does a y-axis bounce with Motion spring (stiffness: 400, damping: 10)
- Accept `size` prop defaulting to 120 (px), mobile uses 100
- Export `BambuWelcome`

Create `src/components/auth/login-form.tsx`:
- 'use client' component
- Uses `useTranslations('auth')` for all copy
- Uses `useActionState` from React (or `useFormState` if React 18) to call `login` server action
- Form fields:
  - Email input: type="email", required, placeholder from t('field.email'), uses existing Input component
  - Password input: type="password" / "text" toggle, min 8 chars, uses existing Input component with Eye/EyeOff toggle icon (Lucide)
  - Password toggle: button with Lucide Eye/EyeOff, 20px, gray-400, aria-label="Show password"/"Hide password"
- "Forgot password?" link: right-aligned below password, links to `/${locale}/forgot-password`, ghost Button style, 14px
- Submit button: existing primary Button component, text t('login.submit') = "Log In", full width, whileTap scale 0.97
- Loading state: button disabled during pending, text replaced with BambuLoading at 24px inline size
- Error display: error message below form in #E53935 text, role="alert", 14px
- Footer: "Don't have an account? Create one" with link to `/${locale}/signup`, centered, 14px
- Form container: existing Card component, max-width 400px, padding 32px horizontal 24px vertical
- Export `LoginForm`

Create `src/components/auth/signup-form.tsx`:
- Same structure as login-form.tsx with these differences:
- Heading: t('signup.heading') = "Create Account"
- Submit: t('signup.submit') = "Create Account"
- Calls `signup` server action
- No "Forgot password?" link
- Footer: t('signup.hasAccount') = "Already have an account? Log in" with link to `/${locale}/login`
- Password validation: client-side check for min 8 characters, show t('error.passwordShort') if < 8
- On signup error with 'already_registered', show t('error.emailExists') = "This email already has an account. Log in instead?" with "Log in" as a link to /login
- Export `SignupForm`

Create `src/components/auth/auth-provider.tsx`:
```typescript
'use client';
import { useEffect } from 'react';
import { useAuthStore } from '@/stores/auth';

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const initialize = useAuthStore((s) => s.initialize);

  useEffect(() => {
    initialize();
  }, [initialize]);

  return <>{children}</>;
}
```
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit --pretty 2>&1 | head -50 && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bambu/bambu-welcome.tsx contains `export function BambuWelcome`
    - src/components/bambu/bambu-welcome.tsx contains `stiffness: 300`
    - src/components/bambu/bambu-welcome.tsx contains `variant`
    - src/components/auth/login-form.tsx contains `'use client'`
    - src/components/auth/login-form.tsx contains `export function LoginForm`
    - src/components/auth/login-form.tsx contains `import { login } from`
    - src/components/auth/login-form.tsx contains `useTranslations('auth')`
    - src/components/auth/login-form.tsx contains `type="password"` or `type={showPassword`
    - src/components/auth/login-form.tsx contains `forgot-password`
    - src/components/auth/login-form.tsx contains `role="alert"`
    - src/components/auth/signup-form.tsx contains `export function SignupForm`
    - src/components/auth/signup-form.tsx contains `import { signup } from`
    - src/components/auth/signup-form.tsx contains `already_registered`
    - src/components/auth/auth-provider.tsx contains `export function AuthProvider`
    - src/components/auth/auth-provider.tsx contains `useAuthStore`
    - src/components/auth/auth-provider.tsx contains `initialize`
    - TypeScript compiles without errors
    - All existing vitest tests still pass
  </acceptance_criteria>
  <done>Login form, signup form, Bambu welcome pose, and AuthProvider all created. Forms connect to Plan 01 server actions. Password show/hide toggle works. Error states display with role="alert". i18n keys used for all copy. TypeScript and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Login/signup pages, user menu, header update, and layout AuthProvider</name>
  <files>
    src/app/[locale]/login/page.tsx
    src/app/[locale]/signup/page.tsx
    src/components/auth/user-menu.tsx
    src/components/layout/header.tsx
    src/app/[locale]/layout.tsx
  </files>
  <read_first>
    src/app/[locale]/layout.tsx
    src/components/layout/header.tsx
    src/components/layout/page-transition.tsx
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
    src/components/auth/auth-provider.tsx
    src/components/bambu/bambu-welcome.tsx
    src/stores/auth.ts
    src/lib/auth/actions.ts
    messages/en.json
    .planning/phases/05-user-accounts/05-UI-SPEC.md
  </read_first>
  <action>
Create `src/app/[locale]/login/page.tsx`:
- Server component page wrapper
- Renders BambuWelcome (variant="wave", size responsive: 100 on mobile, 120 on desktop) centered
- Below Bambu (48px gap), renders LoginForm inside a centered container
- Uses PageTransition wrapper for route animation (follow pattern from other pages)
- Page title via metadata or next-intl: "Log In"

Create `src/app/[locale]/signup/page.tsx`:
- Same layout as login page
- Uses BambuWelcome (variant="wave") and SignupForm
- Page title: "Create Account"

Create `src/components/auth/user-menu.tsx`:
- 'use client' component
- Uses `useAuthStore` to get user and loading state
- Uses `useTranslations('auth')` for copy
- Uses `useLocale` from next-intl

**Logged-out state (user === null and !loading):**
- Render a ghost Button linking to `/${locale}/login`, text t('header.login') = "Log in", 14px weight 400

**Loading state:**
- Render nothing (or a 32px placeholder to prevent layout shift)

**Logged-in state (user !== null):**
- Avatar circle: 32px diameter, bg-[#2979FF], white text, first letter of user.email uppercase, rounded-full
- On click: toggle dropdown open/closed
- Dropdown menu: positioned absolutely below avatar, right-aligned
  - Width: 200px
  - Background: white, border: #E5E5E5, border-radius: 8px
  - Shadow: `shadow-[0_4px_12px_rgba(0,0,0,0.1)]`
  - Animation: Motion AnimatePresence, initial opacity:0 y:-8, animate opacity:1 y:0, exit opacity:0 y:-8, transition 200ms ease-out
  - Items:
    1. User email (14px, text-gray-600, truncated with overflow-hidden text-ellipsis, 16px horizontal padding)
    2. Separator (1px border-t border-[#E5E5E5])
    3. "My eSIMs" (14px, text-gray-400, 40px row height, 16px padding, cursor-not-allowed, title="Coming soon")
    4. "Settings" (14px, text-gray-400, 40px row height, 16px padding, cursor-not-allowed, title="Coming soon")
    5. Separator
    6. "Log out" (14px, text-[#E53935], 40px row height, 16px padding, calls signOut action on click)
  - Dismiss: click outside (useEffect with document click listener) or Escape key
  - Keyboard: onKeyDown Escape closes dropdown

Export `UserMenu`

Update `src/components/layout/header.tsx`:
- Import UserMenu from '@/components/auth/user-menu'
- Add UserMenu between the desktop nav and ThemeToggle
- Desktop: UserMenu rendered inline in the flex row
- Mobile: UserMenu rendered in the same flex row (it handles its own responsive styling)
- Keep all existing nav links and ThemeToggle unchanged

Update `src/app/[locale]/layout.tsx`:
- Import AuthProvider from '@/components/auth/auth-provider'
- Wrap the children (or the entire body content) with `<AuthProvider>...</AuthProvider>`
- Place AuthProvider inside the existing providers/layout but wrapping the content that needs auth state
- Do NOT replace or remove any existing providers (ThemeProvider, etc.)
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit --pretty 2>&1 | head -50 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/login/page.tsx contains `LoginForm`
    - src/app/[locale]/login/page.tsx contains `BambuWelcome`
    - src/app/[locale]/signup/page.tsx contains `SignupForm`
    - src/app/[locale]/signup/page.tsx contains `BambuWelcome`
    - src/components/auth/user-menu.tsx contains `export function UserMenu`
    - src/components/auth/user-menu.tsx contains `useAuthStore`
    - src/components/auth/user-menu.tsx contains `signOut`
    - src/components/auth/user-menu.tsx contains `#2979FF`
    - src/components/auth/user-menu.tsx contains `Coming soon`
    - src/components/auth/user-menu.tsx contains `AnimatePresence`
    - src/components/layout/header.tsx contains `UserMenu`
    - src/app/[locale]/layout.tsx contains `AuthProvider`
    - TypeScript compilation produces no errors related to auth components
  </acceptance_criteria>
  <done>Login and signup pages render with Bambu and forms. Header shows "Log in" when unauthenticated, avatar+dropdown when authenticated. My eSIMs and Settings disabled with "Coming soon". AuthProvider wraps app layout for auth store initialization.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx vitest run --reporter=verbose` -- all existing + new tests pass
- Login page renders at /en/login with email, password fields, Bambu wave, and "Forgot password?" link
- Signup page renders at /en/signup with email, password fields, and "Already have an account?" link
- Header shows "Log in" button linking to /en/login
- User menu dropdown appears with email, disabled menu items, and Log out
</verification>

<success_criteria>
- /en/login renders login form with Bambu wave and all i18n copy
- /en/signup renders signup form with Bambu wave and all i18n copy
- Header displays UserMenu with logged-out / logged-in states
- AuthProvider initializes auth store on app mount
- All TypeScript compiles without errors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-accounts/05-02-SUMMARY.md`
</output>
