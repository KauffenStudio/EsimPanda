---
phase: 06-esim-management
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/dashboard/circular-gauge.tsx
  - src/components/dashboard/esim-card.tsx
  - src/components/dashboard/esim-grid.tsx
  - src/components/dashboard/dashboard-tabs.tsx
  - src/components/dashboard/low-data-banner.tsx
  - src/components/dashboard/usage-timestamp.tsx
  - src/components/dashboard/dashboard-skeleton.tsx
  - src/app/[locale]/dashboard/page.tsx
  - src/components/dashboard/__tests__/circular-gauge.test.tsx
  - src/components/dashboard/__tests__/esim-card.test.tsx
  - src/components/dashboard/__tests__/dashboard-tabs.test.tsx
autonomous: true
requirements: [MGT-01, MGT-03]

must_haves:
  truths:
    - "Dashboard page renders eSIM cards with status badges, circular gauges, and expiry dates"
    - "Cards are sorted: active first (by expiry ascending), then pending, then expired"
    - "Circular gauge shows data remaining with color-coded thresholds (green > 20%, amber 10-20%, red < 10%)"
    - "Low-data banner appears at top when any eSIM has <= 20% data remaining"
    - "Dashboard shows loading skeleton while fetching, empty state with Bambu when no eSIMs"
    - "Tabs switch between My eSIMs and Purchase History views"
    - "Usage timestamp shows last refresh time with manual refresh button"
  artifacts:
    - path: "src/components/dashboard/circular-gauge.tsx"
      provides: "SVG circular progress ring component"
      exports: ["CircularGauge"]
    - path: "src/components/dashboard/esim-card.tsx"
      provides: "Individual eSIM card with flag, status, gauge, expiry, Top Up CTA"
      exports: ["EsimCard"]
    - path: "src/components/dashboard/dashboard-tabs.tsx"
      provides: "Two-tab switcher with animated indicator"
      exports: ["DashboardTabs"]
    - path: "src/app/[locale]/dashboard/page.tsx"
      provides: "Full dashboard page with state machine (loading, empty, populated, error)"
      contains: "useDashboardStore"
  key_links:
    - from: "src/app/[locale]/dashboard/page.tsx"
      to: "src/stores/dashboard.ts"
      via: "useDashboardStore hook"
      pattern: "useDashboardStore"
    - from: "src/components/dashboard/esim-card.tsx"
      to: "src/components/dashboard/circular-gauge.tsx"
      via: "CircularGauge component import"
      pattern: "import.*CircularGauge"
    - from: "src/components/dashboard/low-data-banner.tsx"
      to: "src/stores/dashboard.ts"
      via: "reads esims from store, filters for low data"
      pattern: "data_remaining_pct"
---

<objective>
Build the dashboard UI components: eSIM cards with circular data gauges, tab navigation, low-data warning banners, loading/empty/error states, and the full dashboard page.

Purpose: Users can view their active eSIMs with status, data usage visualization, and expiry information from a single dashboard page.
Output: 8 UI components + dashboard page replacement.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-esim-management/06-CONTEXT.md
@.planning/phases/06-esim-management/06-RESEARCH.md
@.planning/phases/06-esim-management/06-UI-SPEC.md
@.planning/phases/06-esim-management/06-01-SUMMARY.md

<interfaces>
<!-- Types and store from Plan 01 -->

From src/lib/dashboard/types.ts (created in Plan 01):
```typescript
export interface DashboardEsim {
  id: string;
  iccid: string;
  destination: string;
  destination_iso: string;
  status: 'pending' | 'active' | 'expired';
  data_total_gb: number;
  data_used_gb: number;
  data_remaining_gb: number;
  data_remaining_pct: number;
  expires_at: string | null;
  activated_at: string | null;
  last_usage_check: string | null;
  plan_name: string;
  order_id: string;
  wholesale_plan_id: string;
}
```

From src/stores/dashboard.ts (created in Plan 01):
```typescript
export const useDashboardStore = create<DashboardState>((set, get) => ({
  esims: DashboardEsim[];
  purchases: PurchaseRecord[];
  loading: boolean;
  error: string | null;
  active_tab: 'esims' | 'history';
  last_usage_refresh: string | null;
  usage_refreshing: boolean;
  top_up_esim: DashboardEsim | null;
  top_up_status: 'idle' | 'plan-select' | 'payment' | 'processing' | 'success' | 'error';
  // ... actions
}));
```

From src/components/ui/card.tsx:
```typescript
// Card component with className prop, Tailwind styling
```

From src/components/ui/button.tsx:
```typescript
// Button with variant prop ('primary' | 'secondary' | 'ghost'), whileTap animation
```

From src/components/bambu/bambu-empty.tsx:
```typescript
export function BambuEmpty({ size, className }: { size?: number; className?: string });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CircularGauge, EsimCard, and core component tests</name>
  <files>
    src/components/dashboard/circular-gauge.tsx,
    src/components/dashboard/esim-card.tsx,
    src/components/dashboard/esim-grid.tsx,
    src/components/dashboard/__tests__/circular-gauge.test.tsx,
    src/components/dashboard/__tests__/esim-card.test.tsx
  </files>
  <read_first>
    src/lib/dashboard/types.ts,
    src/stores/dashboard.ts,
    src/components/ui/card.tsx,
    src/components/ui/button.tsx,
    src/components/bambu/bambu-empty.tsx,
    src/lib/mock-data/dashboard.ts,
    .planning/phases/06-esim-management/06-UI-SPEC.md
  </read_first>
  <behavior>
    - CircularGauge Test 1: renders SVG with two circle elements (track + progress)
    - CircularGauge Test 2: renders "2.5" and "GB left" text for total_gb=5, used_gb=2.5
    - CircularGauge Test 3: uses accent color (#2979FF) stroke when remaining > 20%
    - CircularGauge Test 4: uses warning color (#FB8C00) stroke when remaining is 10-20%
    - CircularGauge Test 5: uses destructive color (#E53935) stroke when remaining < 10%
    - CircularGauge Test 6: renders "Expired" text when status is expired
    - EsimCard Test 1: renders destination name, status badge, and "Top Up" button
    - EsimCard Test 2: renders CircularGauge component
    - EsimCard Test 3: active status badge has green color classes
    - EsimCard Test 4: expired status badge has gray color classes
    - EsimCard Test 5: calls onTopUp callback when "Top Up" button is clicked
  </behavior>
  <action>
    1. Create `src/components/dashboard/circular-gauge.tsx`:
    - Pure SVG with two `<circle>` elements (track + progress ring)
    - Props: `{ total_gb: number; used_gb: number; size?: number; stroke_width?: number; status?: 'pending' | 'active' | 'expired' }`
    - Default size=96, stroke_width=8
    - Calculate: radius = (size - stroke_width) / 2, circumference = 2 * PI * radius
    - remaining_gb = total_gb - used_gb, remaining_pct = (remaining_gb / total_gb) * 100
    - strokeDashoffset = circumference * (1 - remaining_pct / 100)
    - Color logic: remaining_pct > 20 = '#2979FF' (accent), 10-20 = '#FB8C00' (warning), < 10 = '#E53935' (destructive)
    - Track circle: fill="none", stroke="#E5E5E5", strokeWidth={stroke_width}
    - Progress circle: motion.circle from "motion/react", animated strokeDashoffset, strokeLinecap="round", transform rotate(-90deg)
    - Spring animation: stiffness 300, damping 20
    - Center text: remaining_gb.toFixed(1) at 24px/700 weight, "GB left" at 14px/400 gray-600
    - Expired state: full gray track, no progress ring, center text "Expired" at 14px/400 gray-400
    - aria-valuenow={remaining_gb}, aria-valuemin={0}, aria-valuemax={total_gb}, role="progressbar"
    - aria-label from i18n: "Data usage for {destination}" — accept destination as optional prop

    2. Create `src/components/dashboard/esim-card.tsx`:
    - Props: `{ esim: DashboardEsim; onTopUp: (esim: DashboardEsim) => void }`
    - Card wrapper with surface background (#F5F5F5), 16px padding, rounded-[10px]
    - Header row: country flag emoji (convert destination_iso to emoji flag), destination name (16px/700), status badge (top-right pill)
    - Status badge colors per UI-SPEC:
      - active: bg-[#43A047]/15 text-[#43A047]
      - expired: bg-[#9E9E9E]/15 text-[#616161]
      - pending: bg-[#FB8C00]/15 text-[#FB8C00]
    - Badge text from i18n: dashboard.status_active, dashboard.status_expired, dashboard.status_pending
    - CircularGauge component centered, size 96 on mobile (md:size 120 via prop)
    - Expiry text: "Expires: {date}" formatted with Intl.DateTimeFormat, 14px/400 gray-600
    - Usage text: "{used} GB of {total} GB used", 14px/400 gray-600
    - "Top Up" button: primary accent, full width at bottom, 44px min height, calls onTopUp(esim) on click
    - For expired eSIMs: show "Topping up will reactivate this eSIM" note (14px, gray-600, italic) above button
    - Motion stagger on mount: initial opacity 0, y 10, animate to opacity 1, y 0

    3. Create `src/components/dashboard/esim-grid.tsx`:
    - Props: `{ esims: DashboardEsim[]; onTopUp: (esim: DashboardEsim) => void }`
    - Sort esims: active first sorted by expires_at ascending (soonest first), then pending, then expired sorted by expires_at descending (most recent first)
    - Responsive grid: 1 column default, 2 columns at md (768px), 3 columns at lg (1024px)
    - Gap: 24px (lg spacing token)
    - Renders EsimCard for each eSIM
    - Stagger children: 50ms delay per card using motion staggerChildren

    4. Write CircularGauge tests in `src/components/dashboard/__tests__/circular-gauge.test.tsx`
    5. Write EsimCard tests in `src/components/dashboard/__tests__/esim-card.test.tsx`
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx vitest run src/components/dashboard/__tests__/circular-gauge.test.tsx src/components/dashboard/__tests__/esim-card.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "CircularGauge" src/components/dashboard/circular-gauge.tsx returns a match
    - grep "motion.circle" src/components/dashboard/circular-gauge.tsx returns a match
    - grep "strokeDashoffset" src/components/dashboard/circular-gauge.tsx returns a match
    - grep "#2979FF" src/components/dashboard/circular-gauge.tsx returns a match (accent color)
    - grep "#FB8C00" src/components/dashboard/circular-gauge.tsx returns a match (warning color)
    - grep "#E53935" src/components/dashboard/circular-gauge.tsx returns a match (destructive color)
    - grep "aria-valuenow" src/components/dashboard/circular-gauge.tsx returns a match
    - grep "EsimCard" src/components/dashboard/esim-card.tsx returns a match
    - grep "CircularGauge" src/components/dashboard/esim-card.tsx returns a match (imports gauge)
    - grep "onTopUp" src/components/dashboard/esim-card.tsx returns a match
    - grep "#43A047" src/components/dashboard/esim-card.tsx returns a match (active badge)
    - grep "esim-grid" src/components/dashboard/esim-grid.tsx returns a match or file exists
    - All CircularGauge and EsimCard tests pass
  </acceptance_criteria>
  <done>CircularGauge renders SVG with animated progress ring and color thresholds. EsimCard displays eSIM info with flag, status badge, gauge, expiry, and Top Up CTA. EsimGrid sorts and lays out cards in responsive grid. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard tabs, banners, skeleton, and page assembly</name>
  <files>
    src/components/dashboard/dashboard-tabs.tsx,
    src/components/dashboard/low-data-banner.tsx,
    src/components/dashboard/usage-timestamp.tsx,
    src/components/dashboard/dashboard-skeleton.tsx,
    src/app/[locale]/dashboard/page.tsx,
    src/components/dashboard/__tests__/dashboard-tabs.test.tsx
  </files>
  <read_first>
    src/app/[locale]/dashboard/page.tsx,
    src/stores/dashboard.ts,
    src/lib/dashboard/types.ts,
    src/lib/mock-data/dashboard.ts,
    src/components/dashboard/esim-grid.tsx,
    src/components/dashboard/esim-card.tsx,
    src/components/bambu/bambu-empty.tsx,
    src/components/ui/button.tsx,
    .planning/phases/06-esim-management/06-UI-SPEC.md
  </read_first>
  <action>
    1. Create `src/components/dashboard/dashboard-tabs.tsx`:
    - Props: `{ active_tab: 'esims' | 'history'; onTabChange: (tab: 'esims' | 'history') => void }`
    - Two tabs: "My eSIMs" and "Purchase History" (text from i18n: dashboard.tab_esims, dashboard.tab_history)
    - Container: full width, h-12, border-bottom 1px border-gray-200
    - Active tab: 16px/700 text, accent-soft background (#E3F0FF) with rounded top corners (8px), 2px bottom border accent (#2979FF)
    - Inactive tab: 16px/400 text, gray-600 (#616161)
    - Animated indicator: motion.div with layoutId for horizontal spring slide (stiffness 400, damping 25)
    - Content switch handled by parent — tabs only switch the active_tab value
    - role="tablist" on container, role="tab" on each tab button, aria-selected on active tab

    2. Create `src/components/dashboard/low-data-banner.tsx`:
    - Props: `{ esims: DashboardEsim[]; onTopUp: (esim: DashboardEsim) => void }`
    - Filter esims: only active eSIMs with data_remaining_pct <= 20
    - For each low-data eSIM, render a banner:
      - Warning (10-20%): bg-[#FB8C00]/10, border-l-4 border-[#FB8C00], text from i18n dashboard.low_data_warning with {destination} and {percent}
      - Critical (< 10%): bg-[#E53935]/10, border-l-4 border-[#E53935], text from i18n dashboard.low_data_critical with {destination} and {percent}
    - Each banner includes inline "Top Up" link-button (accent color) calling onTopUp for that eSIM
    - Stack vertically with 8px gap
    - role="alert" on each banner for screen reader
    - Entry animation: motion.div with initial opacity 0, y -10, animate to opacity 1, y 0, 200ms ease-out
    - If no low-data eSIMs, render nothing (return null)

    3. Create `src/components/dashboard/usage-timestamp.tsx`:
    - Props: `{ last_refresh: string | null; refreshing: boolean; onRefresh: () => void }`
    - Calculate minutes since last_refresh using Date math
    - Display: "Last updated: just now" if < 1 min, "Last updated: {N} min ago" otherwise (i18n keys: dashboard.usage_fresh, dashboard.usage_stale)
    - Text color: gray-600 normally, warning color (#FB8C00) if > 5 minutes
    - Refresh button: Lucide RefreshCw icon (16px), aria-label from i18n (dashboard.refresh_label)
    - When refreshing: icon rotates 360deg continuously (linear, 600ms)
    - Button disabled while refreshing

    4. Create `src/components/dashboard/dashboard-skeleton.tsx`:
    - Renders pulsing skeleton for dashboard loading state
    - Tab bar skeleton: two 80px x 20px rectangles, 16px apart
    - Card skeletons: responsive grid matching esim-grid layout
      - Mobile: 2 full-width cards, 200px height
      - Desktop: 3 cards in grid
    - Each card has circular skeleton (96/120px) for gauge
    - Pulse animation: bg-gray-200 animate-pulse (Tailwind built-in)

    5. Replace `src/app/[locale]/dashboard/page.tsx` entirely:
    - 'use client' directive
    - Import and use useDashboardStore, useAuthStore, useTranslations
    - On mount (useEffect): call useDashboardStore.getState().initialize()
    - State machine rendering:
      - loading=true: render DashboardSkeleton
      - error exists: render BambuError + error message + "Try Again" button that calls initialize()
      - esims.length === 0 AND loading=false: render empty state with BambuEmpty, "No eSIMs yet?" heading, "Browse Plans" CTA linking to /browse
      - esims.length > 0: render full dashboard:
        - Page title: "My eSIMs" (h1, 24px/700)
        - LowDataBanner (above tabs, receives esims and onTopUp)
        - DashboardTabs (active_tab from store, onTabChange calls setActiveTab)
        - AnimatePresence for tab content switching:
          - active_tab === 'esims': UsageTimestamp + EsimGrid
          - active_tab === 'history': placeholder div with text "Purchase History" (Plan 03 will replace)
        - Tab content switch animation: fade + slide-x (opacity 0->1, x 10->0, 200ms)
    - onTopUp handler: calls useDashboardStore.getState().openTopUp(esim) — modal rendering deferred to Plan 03
    - Page padding: px-4 py-8, max-w-5xl mx-auto

    6. Write DashboardTabs tests in `src/components/dashboard/__tests__/dashboard-tabs.test.tsx`:
    - Test 1: renders both tab labels "My eSIMs" and "Purchase History"
    - Test 2: active tab has aria-selected="true"
    - Test 3: clicking inactive tab calls onTabChange with correct tab value
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx vitest run src/components/dashboard/__tests__/dashboard-tabs.test.tsx --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "DashboardTabs" src/components/dashboard/dashboard-tabs.tsx returns a match
    - grep "role=\"tablist\"" src/components/dashboard/dashboard-tabs.tsx returns a match
    - grep "aria-selected" src/components/dashboard/dashboard-tabs.tsx returns a match
    - grep "layoutId" src/components/dashboard/dashboard-tabs.tsx returns a match (animated indicator)
    - grep "LowDataBanner" src/components/dashboard/low-data-banner.tsx returns a match
    - grep "role=\"alert\"" src/components/dashboard/low-data-banner.tsx returns a match
    - grep "data_remaining_pct" src/components/dashboard/low-data-banner.tsx returns a match
    - grep "UsageTimestamp" src/components/dashboard/usage-timestamp.tsx returns a match
    - grep "RefreshCw" src/components/dashboard/usage-timestamp.tsx returns a match
    - grep "DashboardSkeleton" src/components/dashboard/dashboard-skeleton.tsx returns a match
    - grep "animate-pulse" src/components/dashboard/dashboard-skeleton.tsx returns a match
    - grep "useDashboardStore" src/app/[locale]/dashboard/page.tsx returns a match
    - grep "DashboardSkeleton" src/app/[locale]/dashboard/page.tsx returns a match
    - grep "BambuEmpty" src/app/[locale]/dashboard/page.tsx returns a match
    - grep "LowDataBanner" src/app/[locale]/dashboard/page.tsx returns a match
    - grep "DashboardTabs" src/app/[locale]/dashboard/page.tsx returns a match
    - grep "EsimGrid" src/app/[locale]/dashboard/page.tsx returns a match
    - All tests pass across full test suite
  </acceptance_criteria>
  <done>Dashboard page renders with state machine: skeleton while loading, BambuEmpty when no eSIMs, full grid with gauges/badges/tabs when populated. Low-data banners appear for eSIMs at 20% and 10%. Tab switching animates between eSIMs and history views. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass
- Dashboard page imports and uses useDashboardStore
- CircularGauge has all 3 color thresholds
- LowDataBanner filters for active eSIMs with <= 20% data
- DashboardTabs has aria-selected and role="tablist"
- Dashboard page handles loading, empty, error, and populated states
</verification>

<success_criteria>
- CircularGauge renders SVG with animated progress ring and 3 color thresholds
- EsimCard displays flag, destination, status badge, gauge, expiry, and Top Up button
- EsimGrid sorts cards (active by expiry asc, pending, expired by expiry desc) in responsive grid
- DashboardTabs switches between My eSIMs and Purchase History with animated indicator
- LowDataBanner warns for eSIMs at 20% (amber) and 10% (red) with Top Up CTA
- Dashboard page handles loading/empty/error/populated states
- All accessibility attributes present (aria-valuenow, role=tablist, role=alert)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-esim-management/06-02-SUMMARY.md`
</output>
