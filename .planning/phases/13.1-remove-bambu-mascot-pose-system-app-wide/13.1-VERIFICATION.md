---
phase: 13.1-remove-bambu-mascot-pose-system-app-wide
verified: 2026-05-17T22:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
human_verification:
  - test: "Open /en/login in a browser; click Sign in. While the form action is pending, confirm the submit button shows a spinning circle (no Bambu pose), stays disabled, and screen-reader tools report the accessible name via aria-label."
    expected: "Inline CSS spinner visible inside the button; button stays disabled; accessible name survives via aria-label on the Button element."
    why_human: "Visual rendering and screen-reader experience cannot be verified by grep or tsc."
  - test: "Open any page that renders BambuVideo (e.g. /en/login page hero, or the device-compatibility checker) and confirm the panda video still plays."
    expected: "BambuVideo renders and plays as before; no broken video or console errors."
    why_human: "Video playback requires a browser."
---

# Phase 13.1: Remove Bambu Mascot Pose System — Verification Report

**Phase Goal:** The Bambu mascot pose system is removed — the 8 pose components are deleted and their usages replaced with the existing inline spinner primitive; `bambu-video.tsx` (the panda hello video) is kept untouched.
**Requirement:** UXD-09
**Verified:** 2026-05-17T22:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No Bambu pose component file remains in `src/components/bambu/` — only `bambu-video.tsx` | VERIFIED | `ls src/components/bambu/` returns exactly `bambu-video.tsx` |
| 2 | No source file imports or references any of the 8 pose components | VERIFIED | Grep for all 8 kebab paths + all 8 `Bambu*` PascalCase names returns 0 hits across `src/` |
| 3 | The 6 auth buttons render the existing inline CSS spinner while pending | VERIFIED | All 6 files confirmed via `grep -rln 'animate-spin' src/components/auth/`; ternary wiring confirmed in `login-form.tsx` and `oauth-buttons.tsx` by inspection |
| 4 | Each loading/OAuth button keeps a stable accessible name via `aria-label` | VERIFIED | `aria-label` confirmed on all 6 submit/OAuth buttons: `login.submit`, `signup.submit`, `forgot.submit`, `reset.submit`, `convert.submit`, `continueWithGoogle`, `continueWithApple` |
| 5 | `bambu-video.tsx` and its importers are unchanged | VERIFIED | File present; `BambuVideo` export intact; `BambuVideo` imports in `forgot-password-form.tsx`, `reset-password-form.tsx`, `account-conversion-cta.tsx` are all present and untouched |
| 6 | `npx tsc --noEmit` is clean; `npm run build` succeeds | VERIFIED | `npx tsc --noEmit` produced no output (clean); `npm run build` completed successfully |
| 7 | `npm test` passes with no regression below 273-test baseline | VERIFIED | Full suite: 273 passed, 43 todo (316 total) — meets the 273 baseline exactly (a transient pool timeout in the first run caused an undercount of 247; the checkout.test.ts runs fine in isolation, and the second run confirmed 273) |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `src/components/bambu/bambu-video.tsx` | Kept panda hello video component | VERIFIED | Present; exports `BambuVideo`; no pose dependency |
| `src/components/auth/login-form.tsx` | Login submit with inline CSS spinner | VERIFIED | `animate-spin` span in `pending` ternary at L103–104; `aria-label` at L101 |
| `src/components/auth/oauth-buttons.tsx` | Google + Apple OAuth buttons with inline CSS spinner | VERIFIED | `animate-spin` spans in `pending === 'google'` and `pending === 'apple'` ternaries at L74–75 and L93–94; `aria-label` at L72 and L91 |
| `src/components/auth/signup-form.tsx` | Signup submit with inline CSS spinner | VERIFIED | `animate-spin` confirmed; `aria-label={t('signup.submit')}` at L145 |
| `src/components/auth/forgot-password-form.tsx` | Forgot-password submit with inline CSS spinner | VERIFIED | `animate-spin` confirmed; `aria-label={t('forgot.submit')}` at L75; `BambuVideo` import kept at L11 |
| `src/components/auth/reset-password-form.tsx` | Reset-password submit with inline CSS spinner | VERIFIED | `animate-spin` confirmed; `aria-label={t('reset.submit')}` at L97; `BambuVideo` import kept at L10 |
| `src/components/auth/account-conversion-cta.tsx` | Account-conversion CTA submit with inline CSS spinner | VERIFIED | `animate-spin` confirmed; `aria-label={t('convert.submit')}` at L122; `BambuVideo` import kept at L13 |
| 8 deleted pose files (bambu-base, bambu-empty, bambu-error, bambu-loading, bambu-preparing, bambu-success, bambu-travel, bambu-welcome) | Removed dead-weight pose system | VERIFIED — DELETED | `ls src/components/bambu/` returns only `bambu-video.tsx`; grep-zero confirmed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `login-form.tsx` Button | Inline CSS spinner span | `{pending ? <span ... animate-spin /> : t(...)}` | WIRED | Confirmed at L103–104 with `disabled={pending}` at L99 |
| `oauth-buttons.tsx` Google Button | Inline CSS spinner span | `{pending === 'google' ? <span ... animate-spin /> : (...)}` | WIRED | Confirmed at L74–75 with `disabled={pending !== null}` at L69 |
| `oauth-buttons.tsx` Apple Button | Inline CSS spinner span | `{pending === 'apple' ? <span ... animate-spin /> : (...)}` | WIRED | Confirmed at L93–94 with `disabled={pending !== null}` at L88 |
| All 6 auth Button elements | Stable accessible name | `aria-label` attribute on `<Button>` | WIRED | All 7 loading/OAuth buttons carry an `aria-label` pointing to the i18n key for the action |
| `forgot-password-form.tsx` | `BambuVideo` (kept) | `import { BambuVideo }` retained | WIRED | Import at L11; usage at L34 — untouched |
| `reset-password-form.tsx` | `BambuVideo` (kept) | `import { BambuVideo }` retained | WIRED | Import at L10; usage at L43 — untouched |
| `account-conversion-cta.tsx` | `BambuVideo` (kept) | `import { BambuVideo }` retained | WIRED | Import at L13; usage at L62 — untouched |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| UXD-09 | 13.1-01-PLAN.md | The Bambu mascot pose system is removed — 8 pose components and all their usages gone; only `bambu-video.tsx` remains | SATISFIED | All 8 pose files deleted; grep-zero for all pose names; 6 auth files use inline CSS spinner; tsc clean; build green; 273 tests pass |

UXD-09 was listed as `Pending` in REQUIREMENTS.md traceability table mapped to Phase 13.1. All verification evidence satisfies the requirement.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | No TODOs, no placeholder returns, no stub implementations, no console-log-only handlers detected in the modified files |

---

### Unit Tests Confirmed

| Test File | Result | Tests |
|-----------|--------|-------|
| `src/components/auth/__tests__/oauth-buttons.test.tsx` | PASSED | 4/4 — accessible-name-while-pending preserved |
| `src/components/dashboard/__tests__/top-up-modal.test.tsx` | PASSED | 7/7 — `bambu-video` mock still resolves |
| `src/stores/__tests__/checkout.test.ts` | PASSED | 9/9 — runs clean in isolation (had transient pool timeout in the full suite run) |
| Full suite | PASSED | 273 passed, 43 todo — at baseline |

---

### Human Verification Required

#### 1. Auth Button Spinner Visual

**Test:** Run `npm run dev`. On `/en/login`, trigger a login form submit (any credentials). While the action is pending, observe the submit button.
**Expected:** The button shows an inline spinning circle (the `animate-spin` span), remains disabled, and the button accessible name is readable by a screen reader via `aria-label`. No Bambu pose image or animation should appear.
**Why human:** CSS visual rendering and screen-reader output cannot be verified programmatically.

#### 2. BambuVideo Still Plays

**Test:** In dev, navigate to a page that uses `BambuVideo` (e.g., the login page hero, the device-compatibility checker, or any checkout success/error screen).
**Expected:** The panda video renders and plays normally. No console errors about missing modules.
**Why human:** Video playback requires a browser; `bambu-video.tsx` is present and its importers are verified by grep, but actual rendering is manual.

---

### Gaps Summary

None. All automated must-haves pass.

---

## Notes

- The first `npm test` run showed 247 tests due to a transient Vitest pool worker timeout on `checkout.test.ts` — this is a flaky infrastructure issue (worker startup timeout under load), not a code regression. Running `checkout.test.ts` in isolation gives 9/9 passed. The second full-suite run confirmed 273 passed, meeting the baseline exactly.
- The SUMMARY's claim that Task 2's commit was authored by the orchestrator after an API socket error is consistent with the codebase state — the end state is correct regardless of execution path.

---

_Verified: 2026-05-17T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
