---
phase: 13.1-remove-bambu-mascot-pose-system-app-wide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/components/auth/oauth-buttons.tsx
  - src/components/auth/forgot-password-form.tsx
  - src/components/auth/reset-password-form.tsx
  - src/components/auth/account-conversion-cta.tsx
  - src/components/bambu/bambu-base.tsx
  - src/components/bambu/bambu-empty.tsx
  - src/components/bambu/bambu-error.tsx
  - src/components/bambu/bambu-loading.tsx
  - src/components/bambu/bambu-preparing.tsx
  - src/components/bambu/bambu-success.tsx
  - src/components/bambu/bambu-travel.tsx
  - src/components/bambu/bambu-welcome.tsx
autonomous: true
requirements: [UXD-09]

must_haves:
  truths:
    - "No Bambu pose component file remains in src/components/bambu/ — only bambu-video.tsx"
    - "No source file imports or references any of the 8 pose components (BambuLoading, BambuEmpty, BambuError, BambuPreparing, BambuSuccess, BambuTravel, BambuWelcome, Bambu base)"
    - "The 6 auth buttons render the existing inline CSS spinner while pending instead of a Bambu pose"
    - "Each auth loading/OAuth button keeps a stable accessible name while pending via aria-label"
    - "The panda hello video (bambu-video.tsx) and all its 16 importers are unchanged and still work"
    - "tsc --noEmit is clean and npm run build succeeds with no dangling-import errors"
    - "npm test passes with no regression below the 273-test baseline"
  artifacts:
    - path: "src/components/bambu/bambu-video.tsx"
      provides: "The kept panda hello video component — must remain untouched"
      contains: "BambuVideo"
    - path: "src/components/auth/login-form.tsx"
      provides: "Login submit button with inline CSS spinner replacing BambuLoading"
      contains: "animate-spin"
    - path: "src/components/auth/oauth-buttons.tsx"
      provides: "Google + Apple OAuth buttons with inline CSS spinner replacing BambuLoading"
      contains: "animate-spin"
  key_links:
    - from: "src/components/auth/*-form.tsx + oauth-buttons.tsx + account-conversion-cta.tsx"
      to: "inline CSS spinner span"
      via: "pending-ternary children of <Button>"
      pattern: "border-current border-t-transparent rounded-full animate-spin"
    - from: "src/components/auth/* Button elements"
      to: "stable accessible name"
      via: "aria-label on the Button while pending"
      pattern: "aria-label="
---

<objective>
Remove the Bambu mascot pose system from the codebase: delete the 8 pose component files under `src/components/bambu/` and repoint the only live pose consumer (`BambuLoading`, used in 6 auth files) to the codebase's existing inline CSS spinner primitive. Keep `bambu-video.tsx` (the panda hello video) and all its importers completely untouched.

Purpose: Closes requirement UXD-09. Phase 14 (E2E + deploy) must test the final poseless UI, so the pose system must be gone before then. The pose components are dead weight — 7 of 8 have zero importers and the 8th (`bambu-loading`) is trivially replaceable by an existing spinner pattern.

Output: 8 deleted pose files; 6 edited auth files; `src/components/bambu/` containing exactly `bambu-video.tsx`; grep-zero for all pose names/paths; green `tsc`, `build`, and full test suite.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-RESEARCH.md
@.planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-VALIDATION.md

<scope_facts>
The ROADMAP's "22 files" figure is STALE — do NOT plan or hunt for 22 files. The real, grep-verified scope (13.1-RESEARCH.md) is:
- 8 file deletions: the 8 pose components in `src/components/bambu/`.
- 6 file edits: all in `src/components/auth/`, all swapping `BambuLoading` for the existing inline spinner.
- 0 new test files: `oauth-buttons.test.tsx` and `top-up-modal.test.tsx` already exist and cover this.

KEEP UNTOUCHED — explicitly out of scope (do NOT edit any of these):
- `src/components/bambu/bambu-video.tsx` (the panda hello video) and its `videoMap` / `public/bambu/*.mp4` assets.
- All 14 other `bambu`-mentioning files (they use `BambuVideo` or the `panda-face.png` logo, not poses): `src/app/[locale]/login/page.tsx`, `src/app/[locale]/signup/page.tsx`, `src/app/[locale]/page.tsx`, `src/components/browse/device-compatibility/device-checker.tsx`, `src/components/checkout/payment-error.tsx`, `payment-processing.tsx`, `payment-success.tsx`, `src/components/dashboard/top-up-modal.tsx`, `src/components/delivery/provisioning-error.tsx`, `provisioning-state.tsx`, `src/components/pwa/install-banner.tsx`, `push-permission-modal.tsx`, `splash-screen.tsx`, `src/components/layout/header.tsx`.
- `public/bambu/` — no asset is orphaned by this phase.
- `src/components/dashboard/__tests__/top-up-modal.test.tsx` — it mocks `bambu-video` (kept), no change needed.
</scope_facts>

<interfaces>
<!-- The replacement primitive — copy this verbatim. Verified in-repo at two call sites. -->
The codebase's canonical inline CSS spinner (do NOT invent a new component):

From src/components/checkout/coupon-input.tsx:147 and src/components/cart/cart-drawer.tsx:178 (identical):
```tsx
<span className="inline-block w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin" />
```
- Tailwind-only, no JS, no import line.
- Inherits `currentColor` so it works inside any Button variant and in dark mode.
- Use `w-4 h-4` uniformly for all 6 sites (replaces both size-24 and size-20 BambuLoading usages).

The Button component (`src/components/ui/button.tsx`) already forwards `aria-label`, `disabled`, and `className`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap BambuLoading for the existing inline CSS spinner across the 6 auth files</name>
  <files>src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx, src/components/auth/oauth-buttons.tsx, src/components/auth/forgot-password-form.tsx, src/components/auth/reset-password-form.tsx, src/components/auth/account-conversion-cta.tsx</files>

  <read_first>
    - src/components/auth/login-form.tsx
    - src/components/auth/signup-form.tsx
    - src/components/auth/oauth-buttons.tsx
    - src/components/auth/forgot-password-form.tsx
    - src/components/auth/reset-password-form.tsx
    - src/components/auth/account-conversion-cta.tsx
    - src/components/checkout/coupon-input.tsx (line ~147 — the canonical inline spinner to copy)
    - src/components/ui/button.tsx (confirms aria-label is forwarded)
    - .planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-RESEARCH.md (Importer × Pose × Replacement table, Pitfall 1, Pitfall 3)
    - .planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-VALIDATION.md (Per-Task Verification Map)
  </read_first>

  <action>
    Edit all 6 files in `src/components/auth/`. In each: (a) delete the `BambuLoading` import line, (b) replace each `<BambuLoading size={N} />` usage with the existing inline CSS spinner span, (c) add an `aria-label` to the enclosing `<Button>` so the accessible name survives the pending state (Pitfall 1 — the spinner span has no accessible text).

    The replacement spinner span (verbatim, use `w-4 h-4` for ALL sites — replaces size 24 and size 20 alike):
    ```tsx
    <span className="inline-block w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin" />
    ```

    Exact per-file inventory (line numbers are from 13.1-RESEARCH.md — confirm against the live file when editing):

    1. `src/components/auth/login-form.tsx`
       - Delete import at L12: `import { BambuLoading } from '@/components/bambu/bambu-loading';`
       - At ~L99, the submit `<Button type="submit" disabled={pending} className="w-full">`: replace `{pending ? <BambuLoading size={24} /> : t('login.submit')}` with `{pending ? <span className="inline-block w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin" /> : t('login.submit')}`.
       - Add `aria-label={t('login.submit')}` to that `<Button>`.

    2. `src/components/auth/signup-form.tsx`
       - Delete the `BambuLoading` import (~L12).
       - At ~L143 submit button: replace `{pending ? <BambuLoading size={24} /> : t('signup.submit')}` with the spinner-span ternary.
       - Add `aria-label={t('signup.submit')}` to that `<Button>`.

    3. `src/components/auth/oauth-buttons.tsx` — TWO usages:
       - Delete the `BambuLoading` import (~L7).
       - At ~L75 (Google): replace `{pending === 'google' ? <BambuLoading size={20} /> : (...)}` — keep the existing `(...)` icon+label branch exactly, only swap the `<BambuLoading size={20} />` for the spinner span.
       - At ~L93 (Apple): same — swap `<BambuLoading size={20} />` for the spinner span, keep the `(...)` branch.
       - Add `aria-label={t('continueWithGoogle')}` to the Google `<Button>` and `aria-label={t('continueWithApple')}` to the Apple `<Button>`. Use whatever existing key the file already uses for those labels (the `auth`/`oauth` namespace already has `continueWithGoogle` / `continueWithApple` — match the file's existing `t` namespace usage; do NOT add new keys).

    4. `src/components/auth/forgot-password-form.tsx`
       - Delete the `BambuLoading` import at ~L11. CAUTION (Pitfall): a `BambuVideo` import sits on the line directly below — it STAYS. Delete ONLY the `bambu-loading` import line.
       - At ~L73 submit button: replace `{pending ? <BambuLoading size={24} /> : t('forgot.submit')}` with the spinner-span ternary.
       - Add `aria-label={t('forgot.submit')}` to that `<Button>`.

    5. `src/components/auth/reset-password-form.tsx`
       - Delete the `BambuLoading` import at ~L10. CAUTION: a `BambuVideo` import sits directly below — it STAYS.
       - At ~L95 submit button: replace `{pending ? <BambuLoading size={24} /> : t('reset.submit')}` with the spinner-span ternary.
       - Add `aria-label={t('reset.submit')}` to that `<Button>`.

    6. `src/components/auth/account-conversion-cta.tsx`
       - Delete the `BambuLoading` import at ~L13. CAUTION: a `BambuVideo` import sits directly below — it STAYS.
       - At ~L120 submit button: replace `{pending ? <BambuLoading size={24} /> : t('convert.submit')}` with the spinner-span ternary.
       - Add `aria-label={t('convert.submit')}` to that `<Button>`.

    Do NOT delete the 8 pose files in this task — Task 2 owns deletions (build must stay green between tasks; `bambu-loading.tsx` must survive until all 6 importers are repointed).
    Do NOT touch any `BambuVideo` import or `<BambuVideo .../>` usage anywhere. Do NOT add a new `<Spinner>` component, a new dependency, or a barrel file.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/auth/__tests__/oauth-buttons.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'BambuLoading\|bambu-loading' src/components/auth/` returns 0 lines (no import, no usage left in auth).
    - `grep -rln 'animate-spin' src/components/auth/` lists all 6 edited files.
    - `grep -rn 'BambuVideo' src/components/auth/forgot-password-form.tsx src/components/auth/reset-password-form.tsx src/components/auth/account-conversion-cta.tsx` still returns hits — the adjacent `BambuVideo` imports were NOT removed.
    - `grep -rn 'aria-label' src/components/auth/login-form.tsx src/components/auth/signup-form.tsx src/components/auth/oauth-buttons.tsx src/components/auth/forgot-password-form.tsx src/components/auth/reset-password-form.tsx src/components/auth/account-conversion-cta.tsx` shows an `aria-label` on each loading/OAuth button.
    - `npx tsc --noEmit` is clean.
    - `npx vitest run src/components/auth/__tests__/oauth-buttons.test.tsx` passes (accessible name preserved while pending).
  </acceptance_criteria>

  <done>
    All 6 auth files import zero pose components, render the existing inline CSS spinner while pending, and carry a stable `aria-label`; adjacent `BambuVideo` imports untouched; `tsc` clean; `oauth-buttons.test.tsx` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the 8 Bambu pose component files and verify grep-zero + build green</name>
  <files>src/components/bambu/bambu-base.tsx, src/components/bambu/bambu-empty.tsx, src/components/bambu/bambu-error.tsx, src/components/bambu/bambu-loading.tsx, src/components/bambu/bambu-preparing.tsx, src/components/bambu/bambu-success.tsx, src/components/bambu/bambu-travel.tsx, src/components/bambu/bambu-welcome.tsx</files>

  <read_first>
    - .planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-RESEARCH.md (Pose Inventory table — confirms 7 of 8 files have zero importers; bambu-base/bambu-video independence analysis)
    - .planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-VALIDATION.md (Per-Task Verification Map — exact grep/ls/build/test commands)
    - The 8 files being deleted (listed in <action>) — a quick read confirms none is `bambu-video.tsx`.
  </read_first>

  <action>
    Depends on Task 1 — all 6 `BambuLoading` importers must already be repointed before `bambu-loading.tsx` is deleted, otherwise `tsc` / `next build` red-walls (Pitfall 3).

    Delete exactly these 8 files (use `git rm` or filesystem delete):
    - src/components/bambu/bambu-base.tsx
    - src/components/bambu/bambu-empty.tsx
    - src/components/bambu/bambu-error.tsx
    - src/components/bambu/bambu-loading.tsx
    - src/components/bambu/bambu-preparing.tsx
    - src/components/bambu/bambu-success.tsx
    - src/components/bambu/bambu-travel.tsx
    - src/components/bambu/bambu-welcome.tsx

    Do NOT delete `src/components/bambu/bambu-video.tsx` — it is the kept panda hello video, fully self-contained (imports only React hooks; no dependency on `bambu-base` or any pose). After the deletions, `src/components/bambu/` must contain exactly one file: `bambu-video.tsx`.

    Do NOT touch `public/bambu/` — every `.mp4`/`.png` asset there belongs to `bambu-video.tsx` or the `header.tsx` logo and stays. There is no `index.ts` barrel in `src/components/bambu/` (confirmed by research) — nothing to prune.

    After deleting, run the full verification chain below.
  </action>

  <verify>
    <automated>test "$(ls src/components/bambu/)" = "bambu-video.tsx" && npx tsc --noEmit && npm run build && npx vitest run src/components/dashboard/__tests__/top-up-modal.test.tsx && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/components/bambu/` returns exactly `bambu-video.tsx` (the 8 pose files are gone, the video remains).
    - `grep -rn 'bambu-base\|bambu-empty\|bambu-error\|bambu-loading\|bambu-preparing\|bambu-success\|bambu-travel\|bambu-welcome\|BambuEmpty\|BambuError\|BambuLoading\|BambuPreparing\|BambuSuccess\|BambuTravel\|BambuWelcome' src/ --include='*.tsx' --include='*.ts'` returns 0 lines.
    - `grep -rn 'import { Bambu }' src/` returns 0 lines (the `bambu-base` `Bambu` export has no importers).
    - `npx tsc --noEmit` is clean (no dangling-import / missing-module errors).
    - `npm run build` succeeds.
    - `npx vitest run src/components/dashboard/__tests__/top-up-modal.test.tsx` passes (`bambu-video` still resolves in the mock).
    - `npm test` is green with test count ≥ the pre-phase baseline of 273.
  </acceptance_criteria>

  <done>
    The 8 pose files are deleted; `src/components/bambu/` holds only `bambu-video.tsx`; grep-zero for all pose names/paths; `tsc` clean; `npm run build` succeeds; `top-up-modal.test.tsx` and the full suite green.
  </done>
</task>

</tasks>

<verification>
Phase-gate checks (run before `/gsd:verify-work`), from 13.1-VALIDATION.md:

1. `ls src/components/bambu/` → exactly `bambu-video.tsx`.
2. `grep -rn 'bambu-base\|bambu-empty\|bambu-error\|bambu-loading\|bambu-preparing\|bambu-success\|bambu-travel\|bambu-welcome\|BambuEmpty\|BambuError\|BambuLoading\|BambuPreparing\|BambuSuccess\|BambuTravel\|BambuWelcome' src/ --include='*.tsx' --include='*.ts'` → 0 lines. (Pattern intentionally excludes `BambuVideo` and bare `Bambu` — the video stays.)
3. `npx tsc --noEmit` → clean.
4. `npm run build` → succeeds.
5. `npx vitest run src/components/auth/__tests__/oauth-buttons.test.tsx` → passes (accessible-name-while-pending preserved).
6. `npx vitest run src/components/dashboard/__tests__/top-up-modal.test.tsx` → passes (`bambu-video` mock still resolves).
7. `npm test` → green, count ≥ 273.

Inspection-only (note in VERIFICATION.md, not a blocker): the 6 auth surfaces render a sensible inline spinner in the submit/OAuth button while pending — one visual spot-check covers the class since all 6 use the identical pattern.
</verification>

<success_criteria>
- The 8 Bambu pose component files under `src/components/bambu/` are deleted; `bambu-video.tsx` remains and is unchanged.
- The grep-zero gate (verification check 2) returns 0 hits.
- The 6 auth files render the existing inline CSS spinner while `pending`, with a stable `aria-label` on each button — no broken imports, no empty render holes.
- `bambu-video.tsx` and all 16 of its importers are unchanged — the panda hello video still plays where it did before.
- `npx tsc --noEmit` is clean; `npm run build` succeeds; `npm test` is green with no regression below 273 tests.
- UXD-09 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-remove-bambu-mascot-pose-system-app-wide/13.1-01-SUMMARY.md`
</output>
