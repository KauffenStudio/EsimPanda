---
phase: 13.1
slug: remove-bambu-mascot-pose-system-app-wide
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-17
---

# Phase 13.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.4 (already installed; `vitest.config.ts` at repo root) |
| **Quick run command** | `npx vitest run src/components/auth` |
| **Full suite command** | `npm test` |
| **Type/build gate** | `npx tsc --noEmit` + `npm run build` |
| **Estimated runtime** | ~8s suite; ~30s build |

No framework install, no new test files needed — existing infrastructure covers every requirement. This is a deletion + mechanical-spinner-swap phase.

---

## Sampling Rate

- **After every task commit:** `npx tsc --noEmit && npx vitest run src/components/auth`
- **After the wave:** `npm test` + the grep-zero gate + `ls src/components/bambu/`
- **Phase gate:** `npm run build` succeeds + grep-zero for pose imports + full suite green, before `/gsd:verify-work`
- **Max feedback latency:** ~30s (the build)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 13.1-* | 01 | 1 | UXD-09 | unit | `npx vitest run src/components/auth/__tests__/oauth-buttons.test.tsx` — OAuth buttons keep an accessible name while pending after the spinner swap | ✅ | ⬜ pending |
| 13.1-* | 01 | 1 | UXD-09 | grep gate | `grep -rn 'bambu-base\|bambu-empty\|bambu-error\|bambu-loading\|bambu-preparing\|bambu-success\|bambu-travel\|bambu-welcome\|BambuEmpty\|BambuError\|BambuLoading\|BambuPreparing\|BambuSuccess\|BambuTravel\|BambuWelcome' src/ --include='*.tsx' --include='*.ts'` returns 0 lines | ✅ | ⬜ pending |
| 13.1-* | 01 | 1 | UXD-09 | file-existence | `ls src/components/bambu/` returns exactly `bambu-video.tsx` (8 pose files deleted, video kept) | ✅ | ⬜ pending |
| 13.1-* | 01 | 1 | UXD-09 | type/build | `npx tsc --noEmit` clean; `npm run build` succeeds (no dangling-import / missing-module errors) | ✅ | ⬜ pending |
| 13.1-* | 01 | 1 | UXD-09 | unit | `npx vitest run src/components/dashboard/__tests__/top-up-modal.test.tsx` — `bambu-video` still resolves (kept component unaffected) | ✅ | ⬜ pending |
| 13.1-* | 01 | 1 | UXD-09 | full suite | `npm test` green, count ≥ the pre-phase baseline (273) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## What is automated vs needs browser verification

**Automated (MUST pass):**
- grep-zero for all 8 pose component names/paths in `src/`
- `ls src/components/bambu/` = exactly `bambu-video.tsx`
- `npx tsc --noEmit` clean; `npm run build` succeeds
- `oauth-buttons.test.tsx` passes (accessible-name-while-pending preserved after the spinner swap)
- `top-up-modal.test.tsx` passes (`bambu-video` import still resolves)
- `npm test` green, no regression below 273

**Inspection-only (note in VERIFICATION.md):**
- The 6 auth surfaces (login, signup, OAuth Google/Apple, forgot-password, reset-password, account-conversion CTA) render a sensible inline spinner in the submit/OAuth button while pending. All 6 use the identical pattern — one visual spot-check covers the class.

---

## Wave 0 Requirements

- None. Existing test infrastructure (`oauth-buttons.test.tsx`, `top-up-modal.test.tsx`) covers all phase requirements. No new test files or fixtures.

---

## Tests that BREAK from the deletions (Pitfall 6)

None. The research's full `__tests__` scan found **no test imports any of the 8 pose components**. The only Bambu reference in tests is a mock of the KEPT `bambu-video.tsx` (in `top-up-modal.test.tsx`) — unaffected. Zero test cascade.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Auth submit/OAuth buttons show a sensible spinner while pending | UXD-09 | Visual rendering of the swapped inline spinner | `npm run dev`; on `/en/login`, `/en/signup`, forgot/reset-password, and the OAuth buttons — trigger a submit and confirm the button shows the existing inline CSS spinner (no Bambu), button stays disabled, accessible name intact |
| The panda hello video still plays where it did before | UXD-09 | `bambu-video.tsx` is kept — confirm the removal didn't disturb it | In dev, visit the surface that renders `BambuVideo` (e.g. device-compatibility checker) and confirm the video still plays |

---

## Validation Sign-Off

- [ ] All tasks have an `<automated>` verify command
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0: none required
- [ ] No watch-mode flags
- [ ] `npm run build` + grep-zero gate are part of the phase gate
- [ ] `nyquist_compliant: true` set in frontmatter after the executor passes the map

**Approval:** pending
