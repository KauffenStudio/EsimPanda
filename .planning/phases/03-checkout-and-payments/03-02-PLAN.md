---
phase: 03-checkout-and-payments
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[locale]/checkout/page.tsx
  - src/app/[locale]/checkout/success/page.tsx
  - src/components/checkout/checkout-page.tsx
  - src/components/checkout/order-summary.tsx
  - src/components/checkout/email-field.tsx
  - src/components/checkout/device-check.tsx
  - src/components/checkout/coupon-input.tsx
  - src/components/checkout/express-checkout.tsx
  - src/components/checkout/payment-divider.tsx
  - src/components/checkout/card-payment.tsx
  - src/components/checkout/pay-button.tsx
  - src/components/checkout/payment-processing.tsx
  - src/components/checkout/payment-success.tsx
  - src/components/checkout/payment-error.tsx
  - src/components/checkout/checkout-skeleton.tsx
  - src/components/checkout/confetti-effect.tsx
  - messages/en.json
autonomous: false
requirements:
  - CHK-01
  - CHK-02
  - CHK-03
  - CHK-04
  - CHK-05
  - INF-05

user_setup:
  - service: stripe
    why: "Stripe packages needed for Elements UI rendering (even in mock mode, loadStripe is called)"
    env_vars:
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Publishable key (use test mode key pk_test_...)"
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (use test mode key sk_test_...)"
    dashboard_config: []

must_haves:
  truths:
    - "User sees order summary with plan name, data, duration, and price breakdown"
    - "User can enter email address for guest checkout"
    - "User sees device compatibility auto-populated from localStorage if previously checked"
    - "User can expand 'Have a code?' and apply STUDENT30 for 30% off with visual strikethrough"
    - "User sees Express Checkout buttons (Apple Pay/Google Pay/PayPal) at top when available"
    - "User sees inline card payment form below express checkout"
    - "User sees Pay EUR X.XX button with correct total amount"
    - "Bambu eating animation shows during payment processing"
    - "Bambu celebration with confetti shows on payment success"
    - "Bambu error with retry shows on payment failure"
    - "Success page shows order confirmation and auto-redirects after 5 seconds"
    - "Success page only shows PaymentSuccess when payment_intent searchParam exists"
  artifacts:
    - path: "src/components/checkout/checkout-page.tsx"
      provides: "Main checkout orchestrator with Stripe Elements"
      contains: "'use client'"
    - path: "src/components/checkout/order-summary.tsx"
      provides: "Price breakdown card"
      contains: "subtotal"
    - path: "src/components/checkout/coupon-input.tsx"
      provides: "Collapsible coupon field"
      contains: "Have a code"
    - path: "src/components/checkout/express-checkout.tsx"
      provides: "Apple Pay / Google Pay / PayPal buttons"
      contains: "ExpressCheckoutElement"
    - path: "src/components/checkout/card-payment.tsx"
      provides: "Stripe Payment Element wrapper"
      contains: "PaymentElement"
    - path: "src/components/checkout/payment-success.tsx"
      provides: "Bambu celebration screen"
      contains: "confetti"
    - path: "src/app/[locale]/checkout/page.tsx"
      provides: "Checkout route"
      contains: "CheckoutPage"
    - path: "messages/en.json"
      provides: "Checkout i18n keys"
      contains: "checkout.title"
  key_links:
    - from: "src/components/checkout/checkout-page.tsx"
      to: "src/stores/checkout.ts"
      via: "useCheckoutStore import"
      pattern: "import.*useCheckoutStore.*from.*stores/checkout"
    - from: "src/components/checkout/checkout-page.tsx"
      to: "/api/checkout/create-intent"
      via: "fetch on mount"
      pattern: "fetch.*api/checkout/create-intent"
    - from: "src/components/checkout/coupon-input.tsx"
      to: "/api/checkout/validate-coupon"
      via: "fetch on apply"
      pattern: "fetch.*api/checkout/validate-coupon"
    - from: "src/components/checkout/order-summary.tsx"
      to: "src/stores/checkout.ts"
      via: "store selectors for pricing"
      pattern: "useCheckoutStore"
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "src/components/checkout/checkout-page.tsx"
      via: "component import"
      pattern: "import.*CheckoutPage"
    - from: "src/components/checkout/checkout-page.tsx"
      to: "src/components/checkout/payment-processing.tsx"
      via: "conditional render on payment_status"
      pattern: "PaymentProcessing"
    - from: "src/components/checkout/checkout-page.tsx"
      to: "src/components/checkout/payment-error.tsx"
      via: "conditional render on payment_status"
      pattern: "PaymentError"
---

<objective>
Complete checkout UI: single-page checkout flow with Stripe Elements, all payment components, coupon UX, Bambu status screens, confetti celebration, success page, and i18n keys.

Purpose: Wire all data layer contracts from Plan 01 into a visual checkout experience that feels fast ("under 2 minutes" target). Express payment at top for one-tap checkout, inline card form, collapsible coupon, and rewarding Bambu celebration on success.

Output: Full checkout page at /[locale]/checkout with all 13 checkout-specific components, success page at /[locale]/checkout/success, and complete i18n translation keys.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-checkout-and-payments/03-CONTEXT.md
@.planning/phases/03-checkout-and-payments/03-RESEARCH.md
@.planning/phases/03-checkout-and-payments/03-UI-SPEC.md
@.planning/phases/03-checkout-and-payments/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/checkout/types.ts (created in Plan 01):
```typescript
export type PaymentStatus = 'idle' | 'creating' | 'processing' | 'succeeded' | 'failed';

export interface CheckoutPricing {
  retail_price_cents: number;
  discount_cents: number;
  subtotal_cents: number;
  tax_amount_cents: number;
  tax_rate: number;
  total_cents: number;
  currency: 'EUR';
}

export interface CreateIntentResponse {
  client_secret: string;
  amount: number;
  tax_amount: number;
  subtotal: number;
  discount: number;
}

export interface CouponResult {
  valid: boolean;
  code: string;
  discount_percent: number;
  error?: string;
}
```

From src/stores/checkout.ts (created in Plan 01):
```typescript
export const useCheckoutStore = create<CheckoutState>((set) => ({
  plan_id, email, coupon_code, discount_cents, subtotal_cents, tax_cents, total_cents,
  payment_status, error_message, client_secret,
  setPlan, setEmail, applyCoupon, removeCoupon, setPricing, setPaymentStatus, setClientSecret, reset
}));
```

From src/lib/stripe/config.ts (created in Plan 01):
```typescript
export const stripeAppearance: Appearance = { theme: 'flat', variables: {...}, rules: {...} };
```

From src/lib/stripe/client.ts (created in Plan 01):
```typescript
export function getStripe(): Promise<Stripe | null>;
```

From src/lib/mock-data/plans.ts (Phase 2):
```typescript
export interface MockPlan { id, name, data_gb, duration_days, retail_price_cents, ... }
```

From src/hooks/use-device-compat.ts (Phase 2):
```typescript
// Zustand store with localStorage persistence (key: "esim-panda-device-compat")
// Provides: selectedBrand, selectedModel, isCompatible
```

From src/components/ui/button.tsx:
```typescript
export function Button({ variant, size, children, className, disabled, onClick, type }: ButtonProps)
```

From src/components/ui/input.tsx:
```typescript
export const Input = forwardRef<HTMLInputElement, InputProps>(...)
// InputProps: { label?, error?, ...InputHTMLAttributes }
```

From src/components/ui/card.tsx:
```typescript
// Card component with elevated/flat variants
```

From src/components/bambu/:
```typescript
// BambuLoading -- eating bamboo loop animation
// BambuSuccess -- bounce with sparkles
// BambuError -- wobble with sweat drop
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Stripe packages, checkout components, and checkout page route</name>
  <files>
    package.json,
    src/app/[locale]/checkout/page.tsx,
    src/components/checkout/checkout-page.tsx,
    src/components/checkout/order-summary.tsx,
    src/components/checkout/email-field.tsx,
    src/components/checkout/device-check.tsx,
    src/components/checkout/coupon-input.tsx,
    src/components/checkout/express-checkout.tsx,
    src/components/checkout/payment-divider.tsx,
    src/components/checkout/card-payment.tsx,
    src/components/checkout/pay-button.tsx,
    src/components/checkout/checkout-skeleton.tsx,
    messages/en.json
  </files>
  <read_first>
    src/app/[locale]/browse/page.tsx,
    src/components/browse/destination-grid.tsx,
    src/components/ui/button.tsx,
    src/components/ui/input.tsx,
    src/components/ui/card.tsx,
    src/components/ui/badge.tsx,
    src/hooks/use-device-compat.ts,
    src/stores/checkout.ts,
    src/lib/stripe/config.ts,
    src/lib/stripe/client.ts,
    src/lib/checkout/types.ts,
    src/lib/mock-data/plans.ts,
    messages/en.json,
    .planning/phases/03-checkout-and-payments/03-UI-SPEC.md,
    .planning/phases/03-checkout-and-payments/03-CONTEXT.md,
    .planning/phases/03-checkout-and-payments/03-RESEARCH.md
  </read_first>
  <action>
    **Step 0: Install Stripe packages**
    ```bash
    npm install @stripe/stripe-js @stripe/react-stripe-js stripe
    ```

    **Step 1: Create checkout route page** -- src/app/[locale]/checkout/page.tsx
    - Server component that reads `searchParams.plan` for plan_id and `searchParams.coupon` for auto-apply coupon
    - Looks up plan from mock data (import from src/lib/mock-data/plans.ts)
    - If plan not found, redirect to /browse
    - Renders `<CheckoutPage plan={plan} couponFromUrl={couponParam} />`
    - Use next-intl `getTranslations` for metadata
    - Follow pattern from src/app/[locale]/browse/page.tsx

    **Step 2: Create CheckoutPage client component** -- src/components/checkout/checkout-page.tsx
    - 'use client' directive
    - Wraps everything in Stripe `<Elements>` provider using getStripe() and stripeAppearance
    - On mount: call POST /api/checkout/create-intent with plan.id and empty email
    - Store client_secret in checkout store via setClientSecret
    - Store pricing in checkout store via setPricing
    - If couponFromUrl prop provided, auto-apply it (call validate-coupon then update-intent)
    - Layout: max-width 480px centered, 16px horizontal padding
    - Renders in order: OrderSummary, EmailField, DeviceCheck, CouponInput, ExpressCheckout, PaymentDivider, CardPayment, PayButton
    - Shows CheckoutSkeleton while client_secret is loading
    - When payment_status changes to 'processing': show PaymentProcessing overlay
    - When payment_status is 'succeeded': redirect to /checkout/success
    - When payment_status is 'failed': show PaymentError
    - Map Stripe error types to errorType: card_declined -> 'declined', network errors -> 'network', everything else -> 'generic'

    **Step 3: Create OrderSummary** -- src/components/checkout/order-summary.tsx
    - 'use client', reads from useCheckoutStore (subtotal_cents, discount_cents, tax_cents, total_cents, coupon_code)
    - Also accepts plan prop for name, data_gb, duration_days display
    - Uses Card component (flat variant) with bg-[#F5F5F5] and shadow-[0_2px_8px_rgba(0,0,0,0.06)]
    - Shows: plan name + data/duration badge, subtotal line, discount line (only if coupon applied with strikethrough on original + green savings text), VAT line (with tax_rate %), total line (24px bold)
    - Format cents to EUR: `(cents / 100).toFixed(2)` with EUR prefix
    - If tax_cents is 0 and payment_status is 'creating': show "Calculating..." with pulse animation for VAT line
    - Use motion layout animation (spring stiffness:300 damping:25) for price number changes
    - All text via useTranslations('checkout.summary')

    **Step 4: Create EmailField** -- src/components/checkout/email-field.tsx
    - 'use client', uses Input component from ui/input.tsx
    - inputMode="email", autoComplete="email", type="email"
    - fontSize 16px (mandatory per UI-SPEC to prevent iOS zoom)
    - On blur: validate email with simple regex, show error if invalid
    - On change: call useCheckoutStore.setEmail(value)
    - Label: t('checkout.email.label'), placeholder: t('checkout.email.placeholder')

    **Step 5: Create DeviceCheck** -- src/components/checkout/device-check.tsx
    - 'use client', imports useDeviceCompatStore from src/hooks/use-device-compat.ts (or the store it uses)
    - If device already checked (selectedBrand + selectedModel in localStorage): show "{Brand} {Model} -- eSIM compatible" badge with fade-in animation
    - If not compatible: show warning text "This device may not support eSIM. You can still purchase for another device."
    - If not checked: show "Check your device compatibility" link that opens a compact inline picker (reuse DeviceChecker component from Phase 2 or link to /browse with device checker)
    - Use bg-[#F5F5F5] section background

    **Step 6: Create CouponInput** -- src/components/checkout/coupon-input.tsx
    - 'use client', collapsible section
    - Default state: small text link "Have a code?" (14px, text-gray-600, underline)
    - On click: expand with height animation (0 -> auto, 200ms ease-in-out via motion) to reveal input + "Apply Code" button
    - Input: placeholder "Enter discount code", focus ring accent blue
    - Apply button: secondary variant, calls POST /api/checkout/validate-coupon
    - On validating: spinner in apply button
    - On valid: call POST /api/checkout/update-intent with coupon code, then useCheckoutStore.applyCoupon with returned pricing
    - Show strikethrough on original price (handled by OrderSummary reading store) + green "You save EUR X.XX" text (300ms fade-in from y:-4)
    - On invalid: red border on input + "Invalid code -- check spelling and try again" error text below (14px text-destructive)
    - After successful apply: collapse input, show applied badge with "STUDENT30" + "Remove code" link
    - Remove code: calls useCheckoutStore.removeCoupon, calls update-intent without coupon, re-expand the link
    - All text via useTranslations('checkout.coupon')

    **Step 7: Create ExpressCheckout** -- src/components/checkout/express-checkout.tsx
    - 'use client', wraps Stripe ExpressCheckoutElement
    - Only render after client_secret is available (Elements provider handles this)
    - onReady: check availablePaymentMethods, hide entire section if null/empty
    - onConfirm: set payment_status to 'processing', handle confirmation
    - Options: paymentMethods: { applePay: 'auto', googlePay: 'auto', paypal: 'auto' }
    - Fade-in animation (300ms ease-out) when ready
    - Label above: "Express checkout" (14px text-gray-400 uppercase tracking-wide)

    **Step 8: Create PaymentDivider** -- src/components/checkout/payment-divider.tsx
    - Horizontal rule with centered "or pay with card" text
    - 14px text-gray-400, uppercase, letter-spacing 0.05em
    - Pattern: flex items-center gap with two flex-1 border-t lines and text between
    - Text via useTranslations('checkout')

    **Step 9: Create CardPayment** -- src/components/checkout/card-payment.tsx
    - 'use client', wraps Stripe PaymentElement
    - Shows CheckoutSkeleton (pulse animation) while Stripe loads
    - PaymentElement with layout: { type: 'tabs' } or default
    - Fade-in from skeleton to element

    **Step 10: Create PayButton** -- src/components/checkout/pay-button.tsx
    - 'use client', uses Button component (primary variant)
    - Full width, 48px height (taller than standard for primary action emphasis)
    - Text: "Pay EUR {total}" where total is formatted from useCheckoutStore.total_cents
    - Disabled if: email is empty/invalid, payment_status is 'processing' or 'creating'
    - On click: validate email, then use stripe.confirmPayment with elements and return_url to /checkout/success
    - During processing: spinner + "Processing..." text, scale 0.97 tap animation disabled
    - Sticky bottom on mobile: fixed bottom with 16px padding + safe-area-inset-bottom
    - Not sticky on desktop (md: and above): static position
    - 24px gap above the button
    - Haptic: navigator.vibrate?.(10) on tap

    **Step 11: Create CheckoutSkeleton** -- src/components/checkout/checkout-skeleton.tsx
    - Pulse animation (opacity 0.5 -> 1, 1.5s infinite ease-in-out)
    - Renders placeholder blocks matching the shape of: order summary card, email field, express checkout area, card form area, pay button
    - Uses rounded rectangles with bg-gray-200 dark:bg-gray-700

    **Step 12: Add checkout i18n keys to messages/en.json**
    Add under "checkout" key -- ALL keys from the UI-SPEC Copywriting Contract:
    ```json
    {
      "checkout": {
        "title": "Checkout",
        "email": {
          "label": "Email address",
          "placeholder": "your@email.com",
          "error": "Please enter a valid email address"
        },
        "device": {
          "label": "Your device",
          "compatible": "{brand} {model} -- eSIM compatible",
          "incompatible": "This device may not support eSIM. You can still purchase for another device.",
          "check": "Check your device compatibility"
        },
        "coupon": {
          "trigger": "Have a code?",
          "placeholder": "Enter discount code",
          "apply": "Apply Code",
          "savings": "You save EUR {amount}",
          "invalid": "Invalid code -- check spelling and try again",
          "remove": "Remove code"
        },
        "express": {
          "title": "Express checkout"
        },
        "divider": "or pay with card",
        "summary": {
          "title": "Order summary",
          "subtotal": "Subtotal",
          "discount": "Student discount (30%)",
          "vat": "VAT ({rate}%)",
          "vatCalculating": "Calculating...",
          "total": "Total"
        },
        "pay": "Pay EUR {amount}",
        "success": {
          "heading": "Your eSIM is ready!",
          "order": "Order {id}",
          "email": "Check your email for confirmation and setup instructions.",
          "redirect": "Redirecting in {seconds}s...",
          "dashboard": "Go to dashboard"
        },
        "error": {
          "heading": "Payment failed",
          "declined": "Your card was declined. Please try a different payment method.",
          "generic": "Something went wrong. Please try again.",
          "network": "Could not connect. Check your internet and try again.",
          "retry": "Try again",
          "contact": "Need help? Contact us on WhatsApp"
        }
      }
    }
    ```
    IMPORTANT: Merge into existing en.json content -- do NOT overwrite existing keys (browse, common, etc.).
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@stripe/stripe-js"
    - package.json contains "@stripe/react-stripe-js"
    - package.json contains "stripe"
    - src/app/[locale]/checkout/page.tsx contains `searchParams`
    - src/app/[locale]/checkout/page.tsx contains `CheckoutPage`
    - src/components/checkout/checkout-page.tsx contains `'use client'`
    - src/components/checkout/checkout-page.tsx contains `Elements`
    - src/components/checkout/checkout-page.tsx contains `getStripe`
    - src/components/checkout/checkout-page.tsx contains `stripeAppearance`
    - src/components/checkout/checkout-page.tsx contains `useCheckoutStore`
    - src/components/checkout/checkout-page.tsx contains `api/checkout/create-intent`
    - src/components/checkout/order-summary.tsx contains `useCheckoutStore`
    - src/components/checkout/order-summary.tsx contains `subtotal`
    - src/components/checkout/order-summary.tsx contains `discount`
    - src/components/checkout/order-summary.tsx contains `line-through`
    - src/components/checkout/email-field.tsx contains `inputMode="email"`
    - src/components/checkout/email-field.tsx contains `setEmail`
    - src/components/checkout/coupon-input.tsx contains `Have a code`
    - src/components/checkout/coupon-input.tsx contains `api/checkout/validate-coupon`
    - src/components/checkout/coupon-input.tsx contains `STUDENT30`
    - src/components/checkout/express-checkout.tsx contains `ExpressCheckoutElement`
    - src/components/checkout/express-checkout.tsx contains `paypal`
    - src/components/checkout/card-payment.tsx contains `PaymentElement`
    - src/components/checkout/pay-button.tsx contains `Pay EUR`
    - src/components/checkout/pay-button.tsx contains `confirmPayment`
    - src/components/checkout/pay-button.tsx contains `48px` or `h-12`
    - src/components/checkout/checkout-skeleton.tsx contains `animate`
    - messages/en.json contains `checkout.title` or `"checkout"` with nested `"title"`
    - messages/en.json contains `"Your eSIM is ready!"`
    - messages/en.json still contains existing `"browse"` key (not overwritten)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>All 12 checkout components created, checkout route renders full single-page flow, Stripe Elements mounted, coupon UX works with collapsible expand, i18n keys complete. TypeScript compiles clean, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Success page, payment status screens, and confetti effect</name>
  <files>
    src/app/[locale]/checkout/success/page.tsx,
    src/components/checkout/checkout-page.tsx,
    src/components/checkout/payment-processing.tsx,
    src/components/checkout/payment-success.tsx,
    src/components/checkout/payment-error.tsx,
    src/components/checkout/confetti-effect.tsx
  </files>
  <read_first>
    src/components/checkout/checkout-page.tsx,
    src/components/bambu/bambu-loading.tsx,
    src/components/bambu/bambu-success.tsx,
    src/components/bambu/bambu-error.tsx,
    src/components/ui/button.tsx,
    src/stores/checkout.ts,
    src/lib/checkout/types.ts,
    messages/en.json,
    .planning/phases/03-checkout-and-payments/03-UI-SPEC.md,
    .planning/phases/03-checkout-and-payments/03-CONTEXT.md
  </read_first>
  <action>
    **Step 1: Create ConfettiEffect** -- src/components/checkout/confetti-effect.tsx
    - 'use client', CSS keyframe-based confetti (no external library)
    - 40 particles using small div elements with random trajectories
    - Colors: mix of accent (#2979FF) and success (#43A047) with some gold (#FFD700)
    - Each particle: random x position, random rotation, gravity fall animation
    - Duration: 2000ms total, particles fade out in last 500ms
    - Use CSS @keyframes with randomized animation-delay and animation-duration per particle
    - Component accepts `active: boolean` prop, renders nothing when inactive
    - Position: fixed, pointer-events-none, z-50, inset-0
    - Particles generated with Array.from({ length: 40 }).map with random style properties

    **Step 2: Create PaymentProcessing** -- src/components/checkout/payment-processing.tsx
    - 'use client', full-viewport overlay
    - Backdrop: fixed inset-0, bg-white/80, backdrop-blur-sm, z-40
    - Fade-in: 300ms ease-out opacity animation
    - Center: flex items-center justify-center
    - Content: BambuLoading component (eating bamboo animation) at 120px size
    - No text (per UI-SPEC: "No text -- Bambu eating bamboo animation only")
    - Prevent body scroll while visible (useEffect with overflow:hidden on body)

    **Step 3: Create PaymentSuccess** -- src/components/checkout/payment-success.tsx
    - 'use client', accepts orderId prop
    - Full-screen centered layout (max-width 480px)
    - BambuSuccess component (bounce with sparkles) at 160px size
    - ConfettiEffect active={true} triggered simultaneously
    - Below Bambu: "Your eSIM is ready!" heading (24px bold) with success color
    - Order number: "Order {orderId}" (14px text-gray-600)
    - Email reminder: "Check your email for confirmation and setup instructions." (16px)
    - Auto-redirect countdown: "Redirecting in {n}s..." with useState counting down from 5
    - useEffect with setInterval decrementing counter, router.push('/dashboard') when counter reaches 0
    - "Go to dashboard" link below countdown for immediate navigation
    - Haptic: navigator.vibrate?.([50, 30, 50]) success pattern on mount
    - All text via useTranslations('checkout.success')

    **Step 4: Create PaymentError** -- src/components/checkout/payment-error.tsx
    - 'use client', accepts errorType prop ('declined' | 'generic' | 'network') and onRetry callback
    - Centered layout with BambuError component (wobble with sweat drop) at 120px size
    - "Payment failed" heading (24px bold, text-destructive)
    - Error body text varies by errorType:
      - 'declined': "Your card was declined. Please try a different payment method."
      - 'network': "Could not connect. Check your internet and try again."
      - 'generic': "Something went wrong. Please try again."
    - "Try again" button (primary variant, calls onRetry which resets payment_status to 'idle')
    - "Need help? Contact us on WhatsApp" link below (14px, text-gray-600)
    - Haptic: navigator.vibrate?.(100) warning pattern on mount
    - All text via useTranslations('checkout.error')

    **Step 5: Create success route page** -- src/app/[locale]/checkout/success/page.tsx
    - Server component
    - Reads searchParams for `payment_intent` or `payment_intent_client_secret` (Stripe return_url params)
    - IMPORTANT (WARNING fix): If `payment_intent` searchParam is NOT present, redirect to /checkout (prevents showing success unconditionally on direct URL access or refresh)
    - When payment_intent IS present: generate a stable mock order ID using the payment_intent value as seed (e.g., "ORD-" + payment_intent.slice(-8).toUpperCase()) so it does NOT regenerate on refresh
    - Renders PaymentSuccess with orderId
    - Add comment: `// TODO: Phase 4 production -- verify Payment Intent status via stripe.paymentIntents.retrieve(payment_intent) before showing success. See RESEARCH.md Pitfall 6.`
    - In mock/dev mode without real Stripe: also accept a `mock=true` searchParam to allow testing the success page directly (e.g., /checkout/success?payment_intent=pi_mock_test&mock=true)

    **Step 6: Wire payment status screens into CheckoutPage**
    - Update src/components/checkout/checkout-page.tsx to conditionally render:
      - PaymentProcessing overlay when payment_status === 'processing'
      - PaymentError when payment_status === 'failed' (with onRetry resetting to 'idle')
      - These render ON TOP of the checkout form (overlay for processing, replace for error)
    - Map Stripe error types to errorType: card_declined -> 'declined', network errors -> 'network', everything else -> 'generic'
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/checkout/confetti-effect.tsx contains `'use client'`
    - src/components/checkout/confetti-effect.tsx contains `40` (particle count)
    - src/components/checkout/confetti-effect.tsx contains `2000` or `2s` (duration)
    - src/components/checkout/confetti-effect.tsx contains `pointer-events-none`
    - src/components/checkout/payment-processing.tsx contains `BambuLoading`
    - src/components/checkout/payment-processing.tsx contains `backdrop-blur`
    - src/components/checkout/payment-processing.tsx contains `overflow`
    - src/components/checkout/payment-success.tsx contains `BambuSuccess`
    - src/components/checkout/payment-success.tsx contains `ConfettiEffect`
    - src/components/checkout/payment-success.tsx contains `Redirecting`
    - src/components/checkout/payment-success.tsx contains `setInterval` or `setTimeout`
    - src/components/checkout/payment-success.tsx contains `router.push`
    - src/components/checkout/payment-error.tsx contains `BambuError`
    - src/components/checkout/payment-error.tsx contains `onRetry`
    - src/components/checkout/payment-error.tsx contains `declined`
    - src/app/[locale]/checkout/success/page.tsx contains `PaymentSuccess`
    - src/app/[locale]/checkout/success/page.tsx contains `payment_intent`
    - src/app/[locale]/checkout/success/page.tsx contains `redirect`
    - src/app/[locale]/checkout/success/page.tsx contains `ORD-`
    - src/app/[locale]/checkout/success/page.tsx contains `TODO`
    - src/components/checkout/checkout-page.tsx contains `PaymentProcessing`
    - src/components/checkout/checkout-page.tsx contains `PaymentError`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Payment processing overlay shows Bambu eating during confirmation. Success page shows Bambu celebration + confetti + auto-redirect countdown only when payment_intent param exists. Random refreshes redirect to /checkout. Error screen shows Bambu apologetic + retry button. checkout-page.tsx wires PaymentProcessing and PaymentError. Build passes clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of checkout flow</name>
  <what-built>Complete single-page checkout flow with Stripe Elements, coupon UX, payment status screens, and Bambu celebration</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/en/browse
    2. Expand a destination and tap a plan card -- should navigate to /en/checkout?plan={planId}
       NOTE: If plan cards still show "Coming soon" toast, manually visit http://localhost:3000/en/checkout?plan={first-plan-id-from-mock-data}
    3. Verify checkout page layout:
       - Order summary card at top with plan name, data, duration, price
       - Email field below
       - Device check section (auto-populated if you tested device earlier)
       - "Have a code?" collapsible link
       - Express checkout section (may show "no payment methods available" in dev)
       - "or pay with card" divider
       - Card form area (Stripe Elements -- may show loading state without real keys)
       - "Pay EUR X.XX" button at bottom
    4. Test coupon flow:
       - Tap "Have a code?" -- should expand smoothly
       - Enter "STUDENT30" and tap "Apply Code"
       - Should show strikethrough on original price + green "You save EUR X.XX"
       - Try "INVALID" -- should show red error text
    5. Test URL coupon: visit /en/checkout?plan={planId}&coupon=STUDENT30 -- discount should auto-apply
    6. Visit /en/checkout/success?payment_intent=pi_mock_test -- should show Bambu celebration + confetti + countdown
    7. Visit /en/checkout/success (no params) -- should redirect to /checkout
    8. Mobile responsive: check at 375px width -- single column, pay button sticky at bottom
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx vitest run` -- all tests pass (existing + new)
- `npx next build` -- build succeeds (run separately, not in task verify due to 60-120s runtime)
- Checkout route exists at /[locale]/checkout
- Success route exists at /[locale]/checkout/success
- All 13 checkout components exist in src/components/checkout/
- messages/en.json contains all checkout translation keys
- Success page redirects to /checkout when payment_intent param missing
- Visual verification of complete checkout flow
</verification>

<success_criteria>
- Single-page checkout renders all sections in correct order per UI-SPEC layout contract
- Order summary shows plan details and price breakdown with EUR formatting
- Coupon "Have a code?" expands/collapses smoothly, STUDENT30 applies 30% discount with visual feedback
- Express checkout section renders (hidden gracefully if no wallets in dev)
- Card payment area renders Stripe Payment Element
- Pay button shows dynamic EUR total amount
- Payment processing shows Bambu eating bamboo overlay
- Payment success shows Bambu celebration + 40-particle confetti + 5s countdown redirect
- Payment error shows Bambu apologetic + error-specific message + retry button
- Success page only shows PaymentSuccess when payment_intent searchParam exists; redirects otherwise
- checkout-page.tsx conditionally renders PaymentProcessing and PaymentError based on payment_status
- All text uses i18n translation keys
- Mobile layout is single-column with sticky pay button
- Desktop layout is narrow centered (480px max) with sticky order summary
- TypeScript compiles, build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-checkout-and-payments/03-02-SUMMARY.md`
</output>
