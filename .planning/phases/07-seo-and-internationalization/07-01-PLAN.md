---
phase: 07-seo-and-internationalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/middleware.ts
  - src/app/[locale]/layout.tsx
  - messages/pt.json
  - messages/es.json
  - messages/fr.json
autonomous: true
requirements: [GRW-03]

must_haves:
  truths:
    - "Routing config includes en, pt, es, fr locales"
    - "Middleware matcher handles all 4 locale prefixes"
    - "Locale layout calls setRequestLocale for static rendering"
    - "Translation files exist for pt, es, fr with all keys from en.json"
    - "Navigation module exports locale-aware Link, useRouter, usePathname"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "Multi-locale routing config"
      contains: "locales: ['en', 'pt', 'es', 'fr']"
    - path: "src/i18n/navigation.ts"
      provides: "Locale-aware navigation exports"
      exports: ["Link", "redirect", "usePathname", "useRouter", "getPathname"]
    - path: "messages/pt.json"
      provides: "Portuguese translations"
      min_lines: 200
    - path: "messages/es.json"
      provides: "Spanish translations"
      min_lines: 200
    - path: "messages/fr.json"
      provides: "French translations"
      min_lines: 200
  key_links:
    - from: "src/i18n/routing.ts"
      to: "src/middleware.ts"
      via: "routing import"
      pattern: "import.*routing.*from.*i18n/routing"
    - from: "src/i18n/routing.ts"
      to: "src/i18n/navigation.ts"
      via: "createNavigation(routing)"
      pattern: "createNavigation.*routing"
    - from: "src/i18n/routing.ts"
      to: "src/app/[locale]/layout.tsx"
      via: "generateStaticParams uses routing.locales"
      pattern: "routing\\.locales"
---

<objective>
Expand the i18n infrastructure from English-only to 4 locales (EN, PT, ES, FR), create the locale-aware navigation module, update middleware and layout for multi-locale static rendering, and generate all translation files.

Purpose: Enables all subsequent SEO and destination page work to be locale-aware from the start.
Output: Working multi-locale routing, 3 new translation files, navigation module for new components.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-and-internationalization/07-CONTEXT.md
@.planning/phases/07-seo-and-internationalization/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/i18n/routing.ts (CURRENT - to be modified):
```typescript
import { defineRouting } from 'next-intl/routing';
export const routing = defineRouting({
  locales: ['en'],
  defaultLocale: 'en',
});
```

From src/i18n/request.ts (CURRENT - to be modified):
```typescript
// Line 6: locale type cast needs updating from 'en' to union type
if (!locale || !routing.locales.includes(locale as 'en')) {
```

From src/middleware.ts (CURRENT - matcher to be updated):
```typescript
export const config = {
  matcher: ['/', '/(en)/:path*'],
};
```

From src/app/[locale]/layout.tsx (CURRENT - needs setRequestLocale + generateStaticParams):
```typescript
export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
})
```

From messages/en.json: 266 lines, ~200+ translation keys organized by section (nav, theme, landing, browse, dashboard, profile, errors, checkout, auth, delivery).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand i18n routing, request, middleware, and layout for 4 locales</name>
  <files>src/i18n/routing.ts, src/i18n/request.ts, src/i18n/navigation.ts, src/middleware.ts, src/app/[locale]/layout.tsx</files>
  <read_first>
    - src/i18n/routing.ts
    - src/i18n/request.ts
    - src/middleware.ts
    - src/app/[locale]/layout.tsx
    - .planning/phases/07-seo-and-internationalization/07-RESEARCH.md (Pattern 3 and Pattern 4 sections)
  </read_first>
  <action>
1. **src/i18n/routing.ts** -- Change `locales: ['en']` to `locales: ['en', 'pt', 'es', 'fr']`. Keep `defaultLocale: 'en'`. No other changes.

2. **src/i18n/request.ts** -- Update the type cast on line 6 from `locale as 'en'` to `locale as (typeof routing.locales)[number]` so TypeScript accepts the expanded locale union. Everything else stays the same.

3. **src/i18n/navigation.ts** -- Create NEW file:
```typescript
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

4. **src/middleware.ts** -- Change matcher from `['/', '/(en)/:path*']` to `['/', '/(en|pt|es|fr)/:path*']`. No other changes to the middleware function body.

5. **src/app/[locale]/layout.tsx** -- Add two things:
   a. Import `setRequestLocale` from `next-intl/server` and `routing` from `@/i18n/routing`
   b. Add `export function generateStaticParams() { return routing.locales.map((locale) => ({ locale })); }` before the default export
   c. Call `setRequestLocale(locale)` as the first line after `const { locale } = await params;` inside the component body
   d. Keep all existing content (NextIntlClientProvider, AuthProvider, Header, BottomNav, etc.) unchanged
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/routing.ts contains `locales: ['en', 'pt', 'es', 'fr']`
    - src/i18n/routing.ts contains `defaultLocale: 'en'`
    - src/i18n/request.ts contains `(typeof routing.locales)[number]` (not `as 'en'`)
    - src/i18n/navigation.ts contains `createNavigation` and exports `Link, redirect, usePathname, useRouter, getPathname`
    - src/middleware.ts contains `'/(en|pt|es|fr)/:path*'`
    - src/app/[locale]/layout.tsx contains `setRequestLocale(locale)`
    - src/app/[locale]/layout.tsx contains `generateStaticParams`
    - src/app/[locale]/layout.tsx contains `routing.locales.map`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 4 locales routable, navigation module created, layout supports static generation for all locales</done>
</task>

<task type="auto">
  <name>Task 2: Generate PT, ES, FR translation files from en.json</name>
  <files>messages/pt.json, messages/es.json, messages/fr.json</files>
  <read_first>
    - messages/en.json
  </read_first>
  <action>
Create 3 new translation files by translating every key from messages/en.json. Use the exact same JSON structure and key names. Only translate the string values.

**messages/pt.json** -- Portuguese translations. Key examples:
- `nav.home` -> "Inicio"
- `nav.destinations` -> "Destinos"
- `landing.headline` -> "Conecte-se em Qualquer Lugar"
- `landing.cta_primary` -> "Conectar Agora"
- `browse.title` -> "Destinos"
- `browse.searchPlaceholder` -> "Pesquisar destinos..."
- `checkout.title` -> "Pagamento"
- `checkout.pay` -> "Pagar EUR {amount}"
- `auth.login.heading` -> "Entrar"
- `errors.generic` -> "Algo correu mal. Tente novamente ou contacte-nos no WhatsApp."

**messages/es.json** -- Spanish translations. Key examples:
- `nav.home` -> "Inicio"
- `nav.destinations` -> "Destinos"
- `landing.headline` -> "Conectate en Cualquier Lugar"
- `landing.cta_primary` -> "Conectarse"
- `browse.title` -> "Destinos"
- `browse.searchPlaceholder` -> "Buscar destinos..."
- `checkout.title` -> "Pago"
- `checkout.pay` -> "Pagar EUR {amount}"
- `auth.login.heading` -> "Iniciar Sesion"
- `errors.generic` -> "Algo salio mal. Intentalo de nuevo o contactanos por WhatsApp."

**messages/fr.json** -- French translations. Key examples:
- `nav.home` -> "Accueil"
- `nav.destinations` -> "Destinations"
- `landing.headline` -> "Connectez-vous Partout"
- `landing.cta_primary` -> "Se Connecter"
- `browse.title` -> "Destinations"
- `browse.searchPlaceholder` -> "Rechercher des destinations..."
- `checkout.title` -> "Paiement"
- `checkout.pay` -> "Payer EUR {amount}"
- `auth.login.heading` -> "Connexion"
- `errors.generic` -> "Une erreur est survenue. Reessayez ou contactez-nous sur WhatsApp."

IMPORTANT: Preserve all ICU message format placeholders exactly as-is: `{amount}`, `{email}`, `{query}`, `{brand}`, `{model}`, `{seconds}`, `{id}`, `{minutes}`, `{date}`, `{used}`, `{total}`, `{percent}`, `{destination}`, `{orderId}`, `{plan}`, `{method}`, `{code}`, `{discount}`, `{currency}`, `{rate}`, `{iccid}`, `{count}`. Do NOT translate placeholder names.

Each file must have the exact same JSON structure as en.json with all keys present.
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/esim-business && node -e "const en = require('./messages/en.json'); const pt = require('./messages/pt.json'); const es = require('./messages/es.json'); const fr = require('./messages/fr.json'); function keys(o,p=''){return Object.entries(o).flatMap(([k,v])=>typeof v==='object'?keys(v,p+k+'.'):p+k)}; const ek=keys(en); const pk=keys(pt); const sk=keys(es); const fk=keys(fr); const missing=[]; if(pk.length!==ek.length)missing.push('pt:'+pk.length+'/'+ek.length); if(sk.length!==ek.length)missing.push('es:'+sk.length+'/'+ek.length); if(fk.length!==ek.length)missing.push('fr:'+fk.length+'/'+ek.length); if(missing.length)console.log('MISSING:',missing.join(',')); else console.log('ALL_KEYS_MATCH:'+ek.length)"</automated>
  </verify>
  <acceptance_criteria>
    - messages/pt.json exists and is valid JSON
    - messages/es.json exists and is valid JSON
    - messages/fr.json exists and is valid JSON
    - Each file has the exact same number of leaf keys as en.json
    - No file contains English text for nav.home (pt: "Inicio", es: "Inicio", fr: "Accueil")
    - All ICU placeholders preserved (e.g., `{amount}` appears in checkout.pay in all files)
    - Verify script outputs ALL_KEYS_MATCH
  </acceptance_criteria>
  <done>All 3 translation files created with complete key coverage matching en.json structure</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- Visiting `/pt/browse` in dev server does not 404 (middleware matches)
- All 3 translation files parse as valid JSON with matching key counts
- `src/i18n/navigation.ts` exports are importable
</verification>

<success_criteria>
- 4 locales configured in routing (en, pt, es, fr)
- Middleware handles all locale prefixes
- Layout supports static rendering with setRequestLocale
- Navigation module created with locale-aware Link/useRouter/usePathname
- 3 translation files exist with 100% key coverage
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-and-internationalization/07-01-SUMMARY.md`
</output>
