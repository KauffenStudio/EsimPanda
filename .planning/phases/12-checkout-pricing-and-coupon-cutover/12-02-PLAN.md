---
phase: 12-checkout-pricing-and-coupon-cutover
plan: 02
type: execute
wave: 2
depends_on: [12-01]
autonomous: true
requirements: [CHK-06, CHK-08]
files_modified:
  - src/lib/db/destinations.ts
  - src/stores/cart.ts
  - src/stores/quick-checkout.ts
  - src/stores/__tests__/cart.test.ts
  - src/components/checkout/order-summary.tsx
  - src/components/checkout/sticky-order-bar.tsx
  - src/components/checkout/checkout-page.tsx
  - src/components/cart/cart-item.tsx
  - src/components/browse/plan-card.tsx
  - src/hooks/use-plans.ts
  - src/lib/seo/structured-data.ts
  - src/app/[locale]/checkout/page.tsx
  - src/app/[locale]/browse/page.tsx
  - src/components/browse/browse-client.tsx
  - messages/en.json
  - messages/pt.json
  - messages/es.json
  - messages/fr.json
  - messages/ja.json
  - messages/zh.json

must_haves:
  truths:
    - "calculatePrice / checkout/page.tsx resolve the selected plan from Supabase by real plan ID"
    - "Opening checkout with an unknown plan ID redirects to /{locale}/browse?notice=plan-unavailable"
    - "The browse page renders a dismissable notice banner when ?notice=plan-unavailable is present"
    - "A persisted v1.0 cart is silently emptied on first v1.1 load via a version: 2 migrate"
    - "MockPlan is renamed to Plan (the canonical type from db/destinations.ts) across all importers; tsc --noEmit is clean"
  artifacts:
    - path: "src/app/[locale]/checkout/page.tsx"
      provides: "async server component resolving the plan via getPlanById, redirecting unknown IDs with a notice"
      contains: "getPlanById"
    - path: "src/stores/cart.ts"
      provides: "persist config with version: 2 + migrate clearing stale carts"
      contains: "version: 2"
    - path: "src/stores/__tests__/cart.test.ts"
      provides: "unit test for the migrate function"
    - path: "src/lib/db/destinations.ts"
      provides: "canonical Plan interface extended with 3 optional timestamp fields"
      contains: "synced_at"
    - path: "src/components/browse/browse-client.tsx"
      provides: "dismissable plan-unavailable notice banner driven by the notice prop"
  key_links:
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "src/lib/db/destinations.ts (getPlanById)"
      via: "await getPlanById(planId) — null → redirect with notice"
      pattern: "getPlanById"
    - from: "src/app/[locale]/checkout/page.tsx"
      to: "/{locale}/browse?notice=plan-unavailable"
      via: "redirect()"
      pattern: "notice=plan-unavailable"
    - from: "src/app/[locale]/browse/page.tsx"
      to: "src/components/browse/browse-client.tsx"
      via: "notice prop read from searchParams"
      pattern: "searchParams"
    - from: "src/stores/cart.ts (persist migrate)"
      to: "empty cart state"
      via: "version < 2 → clean cart"
      pattern: "version: 2"
---

<objective>
Cut the checkout server component over to live Supabase reads, migrate the persisted Zustand cart, and finish the `MockPlan` → `Plan` type rename.

`checkout/page.tsx` becomes an async server component resolving the plan via `getPlanById`; an unknown plan ID redirects to `/{locale}/browse?notice=plan-unavailable`. The browse page reads that query param and shows a dismissable notice banner (new i18n key across all 6 locales). The cart `persist` config gets `version: 2` + a `migrate` that silently empties any pre-v1.1 cart. `MockPlan` is renamed to the canonical `Plan` type (from `db/destinations.ts`, built in Phase 11) across all 11 importers — the canonical `Plan` is extended with 3 optional timestamp fields so the rename compiles without forking the type.

Purpose: CHK-08 (clean cart on first v1.1 load + the `MockPlan`→`Plan` rename) and CHK-06 (the checkout-page Supabase lookup). Depends on 12-01 (reuses the `getPlanById` cutover pattern and the canonical `Plan` type usage).

Output: async `checkout/page.tsx`, browse notice banner + 6 locale keys, versioned cart migration with a unit test, and a fully propagated type rename gated by `tsc --noEmit`.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-checkout-pricing-and-coupon-cutover/12-CONTEXT.md
@.planning/phases/12-checkout-pricing-and-coupon-cutover/12-RESEARCH.md
@.planning/phases/12-checkout-pricing-and-coupon-cutover/12-VALIDATION.md
@.planning/phases/12-checkout-pricing-and-coupon-cutover/12-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do NOT re-explore the codebase for these. -->

From src/lib/db/destinations.ts (canonical Plan — TO BE EXTENDED in Task 1):
```typescript
export interface Plan {
  id: string;
  destination_id: string;
  wholesale_plan_id: string;
  provider: string;
  name: string;
  data_gb: number;
  duration_days: number;
  wholesale_price_cents: number;
  retail_price_cents: number;
  currency: string;
  is_active: boolean;
  // 3 optional timestamp fields added in Task 1 so MockPlan importers compile:
  // synced_at?: string; created_at?: string; updated_at?: string;
}
export async function getPlanById(planId: string): Promise<Plan | null>;
```

The 11 files importing `MockPlan` (from `grep -rln "MockPlan" src/`):
- src/stores/cart.ts            — `CartItem.plan: MockPlan`
- src/stores/quick-checkout.ts  — `selectedPlan: MockPlan | null` (NO persist — type rename only)
- src/components/checkout/order-summary.tsx
- src/components/checkout/sticky-order-bar.tsx
- src/components/checkout/checkout-page.tsx
- src/components/cart/cart-item.tsx
- src/components/browse/plan-card.tsx
- src/hooks/use-plans.ts
- src/lib/seo/structured-data.ts
- src/lib/mock-data/plans.ts     — DEFINES `MockPlan` (stays — Phase 13 deletes this file; do NOT rename the definition here)
- src/lib/db/destinations.ts     — only mentions MockPlan in a comment

Cart store (src/stores/cart.ts) — persist config currently has NO version key (implicitly version 0):
```typescript
persist(/* store */, {
  name: 'esim-panda-cart',
  partialize: (state) => ({ items: state.items, coupon_code: state.coupon_code, discount_percent: state.discount_percent }),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cart version: 2 migration + cart.test.ts + extend canonical Plan</name>
  <files>src/stores/cart.ts, src/stores/__tests__/cart.test.ts, src/lib/db/destinations.ts</files>
  <read_first>
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-CONTEXT.md (Stale-cart migration; MockPlan → Plan rename — the 3 missing fields)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-RESEARCH.md (§7 cart version: 2 / migrate before-after, §8 the 3 timestamp fields, Pitfall 3, Pitfall 5 quick-checkout does NOT persist)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-VALIDATION.md (Wave 0 — cart.test.ts; Per-Task Verification Map)
    - src/stores/cart.ts (the file being modified — persist config, CartState shape, partialize)
    - src/lib/db/destinations.ts (the canonical Plan interface — extend it here)
    - src/lib/mock-data/plans.ts (the MockPlan interface — to compare its 3 extra fields: synced_at, created_at, updated_at)
    - src/stores/__tests__/ (existing test dir — confirm cart.test.ts does not yet exist)
  </read_first>
  <behavior>
    cart.test.ts (NEW file at src/stores/__tests__/cart.test.ts) — tests the exported `migrate` function directly:
    - migrate(anyPersistedState, 0) returns an empty cart: `{ items: [], coupon_code: null, discount_percent: 0 }`.
    - migrate(anyPersistedState, 1) returns the same empty cart (any version < 2).
    - migrate(someState, 2) returns the passed state unchanged (passthrough for the current version).
  </behavior>
  <action>
    Three edits — write the test FIRST (RED), then the production changes (GREEN).

    1. NEW `src/stores/__tests__/cart.test.ts`: import the `migrate` function from `@/stores/cart`. To make `migrate` unit-addressable, export it from `cart.ts` as a named export (`export function migrateCart(persistedState, version) { ... }`) and reference it inside the `persist` config. Assert the three cases in the `<behavior>` block above. This test fails until the production changes land.

    2. `src/stores/cart.ts` — add `version: 2` + `migrate` to the `persist` options. Per RESEARCH §7:
       ```typescript
       export function migrateCart(persistedState: unknown, version: number) {
         // Any persisted state from version < 2 holds dead v1.0 mock plan IDs.
         // Nothing is safely recoverable → clean empty cart. Silent (no toast).
         if (version < 2) {
           return { items: [], coupon_code: null, discount_percent: 0 };
         }
         return persistedState as CartState;
       }
       ```
       Then in the `persist` config: add `version: 2` and `migrate: migrateCart` alongside the existing `name` and `partialize`. The `migrate` return shape matches `partialize` (`items`, `coupon_code`, `discount_percent`) — Zustand merges it over the initializer so non-persisted fields like `isOpen` are filled from defaults. Confirm the exact empty values against the store's initial state (`coupon_code` initial value may be `null` or `''` — match it).

    3. `src/lib/db/destinations.ts` — extend the canonical `Plan` interface with the 3 timestamp fields that `MockPlan` has and `Plan` lacks, all OPTIONAL (so `getPlanById` rows, which lack them, still satisfy `Plan`, and cart-add code passing a `MockPlan`-shaped object also satisfies it):
       ```typescript
       synced_at?: string;
       created_at?: string;
       updated_at?: string;
       ```
       Compare against `src/lib/mock-data/plans.ts`'s `MockPlan` to confirm these are exactly the 3 fields that differ — if the actual diff is different, add the actual missing fields instead. Do NOT remove any existing `Plan` field.

    Do NOT touch `quick-checkout.ts` here — it does not persist (Pitfall 5); its type rename is Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/cart.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/__tests__/cart.test.ts` exists; `grep -c "migrate" src/stores/__tests__/cart.test.ts` returns ≥ 3
    - `grep -n "version: 2" src/stores/cart.ts` returns 1 hit
    - `grep -n "migrateCart\|export function migrateCart" src/stores/cart.ts` returns ≥ 2 hits (definition + use in persist config)
    - `grep -n "synced_at" src/lib/db/destinations.ts` returns 1 hit (the extended Plan interface)
    - `npx vitest run src/stores/__tests__/cart.test.ts` — all green
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The cart persist config has version: 2 + a migrate that empties pre-v1.1 carts; cart.test.ts covers it; the canonical Plan interface carries the 3 optional timestamp fields.</done>
</task>

<task type="auto">
  <name>Task 2: MockPlan → Plan rename across all importers</name>
  <files>src/stores/cart.ts, src/stores/quick-checkout.ts, src/components/checkout/order-summary.tsx, src/components/checkout/sticky-order-bar.tsx, src/components/checkout/checkout-page.tsx, src/components/cart/cart-item.tsx, src/components/browse/plan-card.tsx, src/hooks/use-plans.ts, src/lib/seo/structured-data.ts</files>
  <read_first>
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-CONTEXT.md (MockPlan → Plan rename — canonical type is db/destinations.ts, do NOT fork)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-RESEARCH.md (§8 rename is search-and-replace; Pitfall 5 quick-checkout type-rename-only)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-VALIDATION.md (tsc --noEmit is the rename gate)
    - src/lib/db/destinations.ts (the canonical Plan — extended in Task 1)
    - src/lib/mock-data/plans.ts (DEFINES MockPlan — its definition STAYS; only importers change)
    - src/stores/cart.ts, src/stores/quick-checkout.ts
    - src/components/checkout/order-summary.tsx, sticky-order-bar.tsx, checkout-page.tsx
    - src/components/cart/cart-item.tsx
    - src/components/browse/plan-card.tsx
    - src/hooks/use-plans.ts
    - src/lib/seo/structured-data.ts
  </read_first>
  <action>
    Rename the `MockPlan` TYPE to `Plan` across every file that IMPORTS it. The canonical `Plan` is the one in `src/lib/db/destinations.ts` (extended in Task 1). Do NOT create a second checkout-specific plan type.

    For each of these 9 importer files — `src/stores/cart.ts`, `src/stores/quick-checkout.ts`, `src/components/checkout/{order-summary,sticky-order-bar,checkout-page}.tsx`, `src/components/cart/cart-item.tsx`, `src/components/browse/plan-card.tsx`, `src/hooks/use-plans.ts`, `src/lib/seo/structured-data.ts`:
    - Change the import from `import type { MockPlan } from '@/lib/mock-data/plans'` to `import type { Plan } from '@/lib/db/destinations'`.
    - Replace every `MockPlan` type reference in that file with `Plan` (`CartItem.plan: Plan`, `selectedPlan: Plan | null`, prop types, function signatures, etc.).

    DO NOT modify `src/lib/mock-data/plans.ts` — it DEFINES `MockPlan` and the pure helpers (`getDiscountPercent` etc.) still use it; that file is deleted in Phase 13, not here. `src/lib/db/destinations.ts` only mentions `MockPlan` in a comment — leave the comment or update it, it is not a type reference.

    `quick-checkout.ts` is type-rename-ONLY — it does not persist (Pitfall 5), no migration.

    If `tsc --noEmit` surfaces a field that the cart/checkout code reads but the canonical `Plan` (post-Task-1 extension) still lacks, extend the canonical `Plan` interface in `db/destinations.ts` with that field as optional — do NOT fork the type. The 3 timestamp fields were added in Task 1; this is a fallback for any field Task 1 missed.

    After the rename: `grep -rln "MockPlan" src/` should return only `src/lib/mock-data/plans.ts` (the definition) and possibly a comment in `src/lib/db/destinations.ts` — no other file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/checkout src/stores</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "MockPlan" src/` returns at most `src/lib/mock-data/plans.ts` and `src/lib/db/destinations.ts` — no other file
    - `grep -rn "import type { MockPlan }" src/` returns 0 hits
    - `grep -n "import type { Plan }" src/stores/cart.ts src/stores/quick-checkout.ts` returns 2 hits
    - `npx tsc --noEmit` clean — zero errors (the CONTEXT-locked rename gate)
    - `npx vitest run src/lib/checkout src/stores` — green
  </acceptance_criteria>
  <done>MockPlan is fully renamed to the canonical Plan across all 9 importers; quick-checkout has the type rename without a migration; tsc --noEmit is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Async checkout/page.tsx + browse notice banner + 6 locale keys</name>
  <files>src/app/[locale]/checkout/page.tsx, src/app/[locale]/browse/page.tsx, src/components/browse/browse-client.tsx, messages/en.json, messages/pt.json, messages/es.json, messages/fr.json, messages/ja.json, messages/zh.json</files>
  <read_first>
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-CONTEXT.md (Invalid plan ID at checkout; Claude's Discretion — i18n key name, banner styling)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-RESEARCH.md (Pattern 4 async RSC redirect, Code Examples checkout/page.tsx + browse notice banner)
    - .planning/phases/12-checkout-pricing-and-coupon-cutover/12-VALIDATION.md (Manual-Only Verifications — checkout redirect, notice banner)
    - src/app/[locale]/checkout/page.tsx (the file being modified — currently mockPlans.find + redirect)
    - src/app/[locale]/browse/page.tsx (the RSC — add searchParams, pass notice down)
    - src/components/browse/browse-client.tsx (the client component — add the dismissable notice banner)
    - src/lib/db/destinations.ts (getPlanById)
    - messages/en.json (locate the namespace the browse/checkout copy lives under — match it for the new key)
    - messages/{pt,es,fr,ja,zh}.json (the other 5 locale files)
  </read_first>
  <action>
    Three coordinated edits — the invalid-plan checkout flow and its browse-page landing.

    1. `src/app/[locale]/checkout/page.tsx` — it is ALREADY an async server component calling `redirect()`. Swap the sync mock lookup for the Supabase read (RESEARCH Pattern 4 — a ~3-line diff):
       - Replace `import { mockPlans } from '@/lib/mock-data/plans'` with `import { getPlanById } from '@/lib/db/destinations'`.
       - Replace `const plan = mockPlans.find((p) => p.id === planId)` with `const plan = await getPlanById(planId)`.
       - When `plan` is null: `redirect(\`/${locale}/browse?notice=plan-unavailable\`)` (was `redirect(\`/${locale}/browse\`)`).
       - The existing `!planId` early-redirect (no plan selected at all) STAYS as-is, with NO notice — that is a different case (no-plan-selected, not plan-unavailable).
       - `getPlanById` returns the canonical `Plan`; `CheckoutPage` now accepts `Plan` (renamed in Task 2) — composes cleanly.

    2. `src/app/[locale]/browse/page.tsx` — add `searchParams` to the page props and read `?notice=`:
       ```typescript
       type Props = {
         params: Promise<{ locale: string }>;
         searchParams: Promise<{ notice?: string }>;
       };
       // inside the component:
       const { notice } = await searchParams;
       ```
       Pass `notice` as a prop to `<BrowseClient>` (the existing client child). Do NOT add `'use client'` to `browse/page.tsx` — it stays an RSC.

    3. `src/components/browse/browse-client.tsx` — add `notice?: string` to `BrowseClientProps`. When `notice === 'plan-unavailable'`, render a small dismissable inline notice banner at the top of the returned JSX (next to / styled like the existing `BrowseErrorBanner` slot — reuse its Phase 11 UI-SPEC tokens for visual consistency). Use a `useState`-backed dismiss (`BrowseClient` already owns client state). The banner copy comes from a new next-intl key.

    4. New i18n key — add ONE new key (e.g. `planUnavailableNotice` under the namespace the browse page already uses; pick a name consistent with the existing keys) to ALL 6 locale files: `messages/{en,pt,es,fr,ja,zh}.json`. English copy along the lines of "That plan is no longer available — here's the current catalog." Translate appropriately for pt/es/fr/ja/zh. Keep the key path identical across all 6 files (a missing key in any locale breaks next-intl).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getPlanById" src/app/[locale]/checkout/page.tsx` returns 1 hit
    - `grep -rn "mockPlans\|MockPlan" "src/app/[locale]/checkout/"` returns 0
    - `grep -n "notice=plan-unavailable" src/app/[locale]/checkout/page.tsx` returns 1 hit
    - `grep -cn "use client" src/app/[locale]/browse/page.tsx` returns 0 (browse page stays an RSC)
    - `grep -ln "planUnavailable" messages/*.json` (or the chosen key name) returns all 6 locale files
    - `grep -n "notice" src/components/browse/browse-client.tsx` returns ≥ 1 hit (the prop + banner)
    - `npx tsc --noEmit` clean
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>checkout/page.tsx resolves plans via Supabase and redirects unknown IDs with a notice; the browse page reads the notice param and BrowseClient renders a dismissable banner; the i18n key exists in all 6 locales.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/checkout src/lib/currency src/stores` — touched-module suites green
- `npm test` — full suite green; total test count ≥ 256 (post-Phase-11 baseline)
- `npx tsc --noEmit` — clean (the CONTEXT-locked `MockPlan`→`Plan` rename gate)
- `npm run build` — succeeds
- `grep -rln "MockPlan" src/` — returns at most `src/lib/mock-data/plans.ts` + `src/lib/db/destinations.ts`
- Manual (record in VERIFICATION.md, not unit-tested): `npm run dev`, open `/en/checkout?plan=00000000-0000-0000-0000-000000000000`, confirm redirect to `/en/browse?notice=plan-unavailable` and a dismissable notice shows; seed a v0 `esim-panda-cart` localStorage payload, reload, confirm the cart is silently empty
</verification>

<success_criteria>
- CHK-06: `checkout/page.tsx` is an async server component resolving the plan via `getPlanById`; an unknown/missing plan ID redirects to `/{locale}/browse?notice=plan-unavailable`
- CHK-08: the cart `persist` config uses `version: 2` + a `migrate` that silently empties any pre-v1.1 cart; the cart `migrate` function has a unit test; `MockPlan` is renamed to the canonical `Plan` across all importers and `tsc --noEmit` is clean
- The browse page renders a dismissable notice banner from `?notice=plan-unavailable`; the i18n key exists in all 6 locale files
- `npm test` green, `npx tsc --noEmit` clean, `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-checkout-pricing-and-coupon-cutover/12-02-SUMMARY.md` using the summary template. Note in it: the exact i18n key name chosen, the final list of files touched by the `MockPlan`→`Plan` rename, and whether any field beyond the 3 timestamp fields had to be added to the canonical `Plan` interface.
</output>
