---
phase: 11-read-layer-module-and-browse-cutover
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/browse/destination-card.tsx
  - src/components/browse/regional-plan-card.tsx
  - src/components/browse/destination-card-skeleton.tsx
  - src/components/browse/browse-error-banner.tsx
  - src/components/browse/browse-client.tsx
  - src/app/[locale]/browse/loading.tsx
  - src/app/[locale]/browse/actions.ts
  - messages/en.json
  - messages/pt.json
  - messages/es.json
  - messages/fr.json
  - messages/ja.json
  - messages/zh.json
  - src/components/browse/__tests__/browse-client.test.tsx
  - src/components/browse/__tests__/destination-card.test.tsx
  - src/components/browse/__tests__/destination-card-skeleton.test.tsx
  - src/components/browse/__tests__/regional-plan-card.test.tsx
autonomous: true
requirements: [CAT-07, UXD-05, UXD-06, UXD-07, CAT-06]
must_haves:
  truths:
    - "A destination card with image_url === null renders a typographic name card (bold name on brand gradient) — no <img>, no flag, no placeholder graphic"
    - "When image_url is present, a motion.img photo blur-cross-fades in over the typographic card; reduced-motion shows the photo instantly"
    - "Catalog fetch shows a shimmer skeleton grid of 12 cards matching real card dimensions via loading.tsx"
    - "On getCatalog error the inline error banner renders above the grid with a Retry button; Retry re-runs getCatalog via a server action"
    - "Search-miss shows plain text + a Clear search button; Clear resets the filter and returns focus to the search input"
  artifacts:
    - path: "src/components/browse/destination-card-skeleton.tsx"
      provides: "Shimmer placeholder card + grid skeleton"
      contains: "animate-[pulse_1.5s_ease-in-out_infinite]"
    - path: "src/components/browse/browse-error-banner.tsx"
      provides: "Inline error banner with role=alert + Retry button"
      contains: "role=\"alert\""
    - path: "src/app/[locale]/browse/loading.tsx"
      provides: "Route-level skeleton during the RSC/ISR-miss window"
    - path: "src/app/[locale]/browse/actions.ts"
      provides: "refetchCatalogAction server action wrapping getCatalog"
      contains: "'use server'"
    - path: "src/components/browse/destination-card.tsx"
      provides: "Typographic fallback card + motion.img photo cross-fade, prop-driven from CatalogDestination"
  key_links:
    - from: "src/components/browse/browse-client.tsx"
      to: "src/app/[locale]/browse/actions.ts"
      via: "Retry button calls refetchCatalogAction()"
      pattern: "refetchCatalogAction"
    - from: "src/components/browse/destination-card.tsx"
      to: "motion.img"
      via: "opacity+blur cross-fade over the typographic gradient card"
      pattern: "motion\\.img"
---

<objective>
Adapt the three browse card components (`destination-card`, `plan-card`, `regional-plan-card`) to the real `CatalogDestination`/`Plan` shape and build the four browse-grid states: the shimmer skeleton, the inline error banner with a working Retry, the search-miss empty state with Clear-search, and the typographic image-fallback card with a `motion.img` photo cross-fade.

Purpose: Completes the visible UX of the live-data cutover — no FOUC, no broken images, no mascot, graceful error recovery — per the APPROVED 11-UI-SPEC.md design contract.

Output: 3 new components (`destination-card-skeleton`, `browse-error-banner`), `loading.tsx`, `actions.ts`, adapted cards, 3 new i18n keys across all 6 locales, 3 new Wave 0 test files + 1 migrated test.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-CONTEXT.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-VALIDATION.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-UI-SPEC.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-01-PLAN.md

<interfaces>
<!-- Consumes types created by 11-01. Import type-only from @/lib/db/destinations. -->
<!-- CatalogDestination = Destination + { startingPriceCents: number; bestDiscountPercent: number }. -->
<!-- Catalog = { destinations: CatalogDestination[]; regionalPlans: CatalogDestination[]; error: boolean }. -->

NEW i18n keys — add to all 6 locale files under the existing `browse` namespace (EN authoritative):
```json
"browse": {
  "error": {
    "retry": "Try loading again",
    "message": "We couldn't load destinations right now. Check your connection and try again."
  },
  "clearSearch": "Clear search",
  "noPlans": "No plans available"
}
```
ALSO reword the existing `browse.noResults` key in all 6 locales:
- from: `No plans for "{query}" yet`  →  to: `No destinations match "{query}"`
</interfaces>

# Card components being adapted + skeleton/token references (read before implementing)
@src/components/browse/destination-card.tsx
@src/components/browse/regional-plan-card.tsx
@src/components/browse/plan-card.tsx
@src/components/checkout/checkout-skeleton.tsx
@src/styles/globals.css
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys + skeleton + loading.tsx + error banner + Retry server action</name>
  <files>messages/en.json, messages/pt.json, messages/es.json, messages/fr.json, messages/ja.json, messages/zh.json, src/components/browse/destination-card-skeleton.tsx, src/app/[locale]/browse/loading.tsx, src/components/browse/browse-error-banner.tsx, src/app/[locale]/browse/actions.ts, src/components/browse/__tests__/destination-card-skeleton.test.tsx</files>
  <read_first>
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-UI-SPEC.md (Component & State Inventory; Copywriting Contract; Color — skeleton bg-gray-200/bg-gray-700; Accessibility — error banner role=alert)
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md (§ Code Examples — skeleton card, browse loading state, inline error banner Approach A + refetchCatalogAction)
    - src/components/checkout/checkout-skeleton.tsx (the exact animate-[pulse_1.5s_ease-in-out_infinite] pattern to reuse)
    - src/styles/globals.css (theme tokens: --color-accent #2979FF, --color-destructive #E53935, --radius-card)
    - messages/en.json (existing browse namespace — keys to add/reword)
    - src/lib/db/destinations.ts (getCatalog signature for the server action)
  </read_first>
  <action>
**i18n keys — all 6 locale files** (`messages/{en,pt,es,fr,ja,zh}.json`), under the existing `browse` namespace:
- Add nested `error: { retry, message }`, plus `clearSearch` and `noPlans` — see the `<interfaces>` block for EN copy. Translate `pt/es/fr/ja/zh` appropriately (keep the apologetic-but-brief tone; no exclamation marks in error copy; no mascot voice).
- Reword existing `browse.noResults` in ALL 6 locales from `No plans for "{query}" yet` to `No destinations match "{query}"` (translate accordingly). Keep the `{query}` interpolation token.
- Do NOT delete `browse.noResultsSuggestion` — UI-SPEC says leave the key, just stop rendering it (deletion is housekeeping, not Phase 11 scope).

**`src/components/browse/destination-card-skeleton.tsx`** — `'use client';`. Export `DestinationCardSkeleton` and `DestinationGridSkeleton`. Skeleton card dimensions MUST match the real `DestinationCard` exactly: `rounded-card` + `aspect-[4/3]` + same border (`border border-border dark:border-border-dark`). Pulse class is the exact string from `checkout-skeleton.tsx`: `animate-[pulse_1.5s_ease-in-out_infinite] bg-gray-200 dark:bg-gray-700`. `DestinationGridSkeleton({ count = 12 })` renders the cards inside the exact country-grid class string `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-5` (UI-SPEC grid contract — load-bearing, no layout shift). Skeleton container gets `aria-hidden="true"` (UI-SPEC accessibility — decorative). Skeleton cards do NOT use the card-entry animation.

**`src/app/[locale]/browse/loading.tsx`** — Next route-level loading UI shown while the async RSC awaits `getCatalog()`. Page container `px-4 pt-6 pb-20 max-w-[1200px] mx-auto`, a `h-10 w-48 mb-6 rounded animate-[pulse_1.5s_ease-in-out_infinite] bg-gray-200 dark:bg-gray-700` title placeholder, then `<DestinationGridSkeleton count={12} />`.

**`src/app/[locale]/browse/actions.ts`** — `'use server';` then `import { getCatalog } from '@/lib/db/destinations';` and `export async function refetchCatalogAction() { return getCatalog(); }`. This is the Retry handler — re-runs the full `getCatalog()` fetch (CONTEXT-locked: NOT `router.refresh()`).

**`src/components/browse/browse-error-banner.tsx`** — `'use client';`. `export function BrowseErrorBanner({ onRetry }: { onRetry: () => void })`. Root `<div role="alert">` (UI-SPEC accessibility) with classes `rounded-card border border-destructive/30 bg-destructive/5 dark:bg-destructive/10 px-4 py-3 flex items-center justify-between gap-3`. A `<p className="text-sm text-destructive dark:text-destructive-dark">` showing `t('browse.error.message')` (use `useTranslations`). A real `<button onClick={onRetry}>` — Retry uses the ACCENT fill, not destructive (UI-SPEC: Retry is a constructive recovery action): `shrink-0 rounded-button bg-accent text-white text-sm font-medium px-4 py-2 min-h-[40px]`, label `t('browse.error.retry')`. Body text 400 weight, sizes 14px (`text-sm`).

**`src/components/browse/__tests__/destination-card-skeleton.test.tsx`** — UXD-05: assert `DestinationGridSkeleton` renders exactly 12 placeholder cards by default (and `count` prop respected); assert the skeleton container carries `aria-hidden`.
  </action>
  <verify>
    <automated>npx vitest run src/components/browse/__tests__/destination-card-skeleton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/browse/__tests__/destination-card-skeleton.test.tsx` passes
    - `node -e "['en','pt','es','fr','ja','zh'].forEach(l=>{const b=require('./messages/'+l+'.json').browse; if(!b.error||!b.error.retry||!b.error.message||!b.clearSearch||!b.noPlans) throw new Error('missing key in '+l)})"` exits 0
    - `node -e "const n=require('./messages/en.json').browse.noResults; if(n.includes('No plans for')) throw new Error('noResults not reworded')"` exits 0
    - `grep -n "animate-\[pulse_1.5s_ease-in-out_infinite\]" src/components/browse/destination-card-skeleton.tsx` returns at least 1 line
    - `grep -n "role=\"alert\"" src/components/browse/browse-error-banner.tsx` returns 1 line
    - `grep -n "'use server'" src/app/[locale]/browse/actions.ts` returns 1 line and `grep -n "getCatalog" src/app/[locale]/browse/actions.ts` returns at least 1 line
  </acceptance_criteria>
  <done>All 6 locales carry the 3 new browse keys and the reworded noResults; the skeleton card/grid matches real card dimensions with the pulse animation; loading.tsx renders the skeleton; the error banner has role=alert with an accent Retry button; refetchCatalogAction server action exists.</done>
</task>

<task type="auto">
  <name>Task 2: Typographic fallback card + photo cross-fade; wire error/search-miss states into BrowseClient</name>
  <files>src/components/browse/destination-card.tsx, src/components/browse/regional-plan-card.tsx, src/components/browse/browse-client.tsx, src/components/browse/__tests__/browse-client.test.tsx, src/components/browse/__tests__/destination-card.test.tsx, src/components/browse/__tests__/regional-plan-card.test.tsx</files>
  <read_first>
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-UI-SPEC.md (Component & State Inventory — DestinationCard image present/null/zero-plans contracts; Photo cross-fade exact spec; Typographic fallback-card gradient; Accessibility — motion.img alt + prefers-reduced-motion)
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md (§ Code Examples — typographic fallback card + cross-fade, search-miss empty state, inline error banner Approach A wiring; New Pitfall — regional card image_url may be missing)
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-01-PLAN.md (CatalogDestination type, BrowseClient shape)
    - src/components/browse/destination-card.tsx (current card — props, next/image usage, handleClick auto-add)
    - src/components/browse/regional-plan-card.tsx (current regional card)
    - src/components/browse/plan-card.tsx (current plan card)
    - src/components/browse/browse-client.tsx (the boundary from 11-01 — has the 11-02 placeholder for error banner)
  </read_first>
  <action>
**Shared typographic fallback primitive.** Build the typographic card ONCE (per UI-SPEC: "build it once; both DestinationCard and RegionalPlanCard consume it"). Either a small local component or a shared JSX block. Spec: `bg-gradient-to-br from-accent to-primary` (`#2979FF` top-left → `#18181B` bottom-right, 135°), absolutely positioned `absolute inset-0 flex items-center justify-center px-3`, the destination name in `text-white text-lg md:text-xl font-bold tracking-tight text-center line-clamp-2`. Same gradient in light and dark (self-contained branded surface — does not invert).

**`destination-card.tsx`** — adapt to receive a `CatalogDestination` (type-only import from `@/lib/db/destinations`). Image area is `aspect-[4/3] relative overflow-hidden rounded-card`:
- The typographic fallback card is ALWAYS rendered as the base layer (so the photo cross-fades in OVER it and there is no flicker if the photo 404s).
- When `image_url` is present, render a `motion.img` (from `motion/react` — NOT `next/image`; `motion` cannot wrap it, CONTEXT-locked) absolutely positioned `absolute inset-0 w-full h-full object-cover` over the typographic card, with `alt={name}`. Cross-fade spec EXACTLY: `initial={{ opacity: 0, filter: 'blur(12px)' }}`, `animate={{ opacity: 1, filter: 'blur(0px)' }}`, `transition={{ duration: 0.4, ease: 'easeOut' }}`. Gate with `useReducedMotion()` — when reduced motion is set, render the img at `opacity: 1` with no blur/transition. Preserve the existing `group-hover:scale-105` photo zoom.
- When `image_url` is null, only the typographic card renders — no `<img>`, no flag, no placeholder graphic, no broken-image icon (CAT-07).
- Keep the existing bottom scrim `bg-gradient-to-t from-black/70 via-black/20 to-transparent` overlay so the name/price chip stays legible over photo OR gradient.
- Zero-plans state: when `startingPriceCents === 0`, the card still renders but the price sub-label shows `t('browse.noPlans')` ("No plans available") instead of `from {price}`.
- handleClick: per RESEARCH Open Question 2, DROP the auto-add-to-cart-on-click — `handleClick` becomes navigation-only (`router.push` to `/esim/[slug]`). Remove the `getPlansForDestination()` call.

**`regional-plan-card.tsx`** — adapt to receive its data as a `CatalogDestination` prop (it no longer calls `useDestinations()`). When `image_url` is present, keep the EXISTING regional-plan-card photo treatment unchanged — no new "Multi-country" frame/badge. When `image_url` is null, render the SAME shared typographic fallback primitive (New Pitfall: a regional card with a missing image must fall back identically to country cards).

**`browse-client.tsx`** — wire the three remaining states (the placeholder left by 11-01):
- Error state: hold catalog in local state — `const [catalog, setCatalog] = useState({ destinations, regionalPlans, error });`. When `catalog.error === true`, render `<BrowseErrorBanner onRetry={handleRetry} />` ABOVE the grid (chrome — `<h1>`, search input, `WelcomeDiscountBanner`, region pills — stays mounted). `handleRetry` is an async function calling the server action: `const fresh = await refetchCatalogAction(); setCatalog(fresh);` (import `refetchCatalogAction` from `@/app/[locale]/browse/actions`).
- Search-miss state: when `searchQuery` is non-empty AND `filtered.length === 0`, render a centered block `flex flex-col items-center justify-center py-16 gap-3` — a `<p className="text-gray-600 dark:text-gray-400 font-semibold text-center">{t('browse.noResults', { query: searchQuery })}</p>` and a `<button>` with `rounded-button bg-accent text-white text-sm font-medium px-4 py-2 min-h-[40px]` labelled `t('browse.clearSearch')`. On click, reset the search filter (`useBrowseStore` setter to `''`) AND return focus to the search input (use a ref to the `DestinationSearch` input, or call `.focus()` on it — UI-SPEC accessibility requirement).
- Remove the rendering of `browse.noResultsSuggestion` (the suggested-destinations row) — it is replaced by the Clear-search button.
- Grid uses the exact UI-SPEC class strings: country grid `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-5`, regional grid `grid grid-cols-1 md:grid-cols-3 gap-4`.

**Tests:**
- `src/components/browse/__tests__/destination-card.test.tsx` (NEW — CAT-07): render `DestinationCard` with a fixture where `image_url` is null → assert the bold destination name renders and NO `<img>` is present; render with `image_url` set → assert an `<img>` (motion.img) with `alt` is present.
- `src/components/browse/__tests__/browse-client.test.tsx` (NEW — replaces/migrates `destination-grid.test.tsx`; CAT-06 + UXD-06): render `<BrowseClient destinations={fixtureDestinations} regionalPlans={fixtureRegionalPlans} error={false} />` using fixtures from `src/lib/__test-fixtures__/catalog.ts`. Assert: fixture destination names render; setting a search query that matches nothing shows "No destinations match" + a Clear-search button, and clicking Clear-search resets the filter; rendering with `error={true}` shows the error banner (`role="alert"`) and the Retry button. Delete the old `destination-grid.test.tsx` (its component no longer exists — 11-01 deleted `destination-grid.tsx`).
- `src/components/browse/__tests__/regional-plan-card.test.tsx` (MIGRATE — Pitfall 6): rewrite to pass a `regionalPlans` fixture prop instead of relying on the deleted mock import / `useDestinations`.
  </action>
  <verify>
    <automated>npx vitest run src/components/browse/__tests__/destination-card.test.tsx src/components/browse/__tests__/browse-client.test.tsx src/components/browse/__tests__/regional-plan-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/browse/__tests__/destination-card.test.tsx src/components/browse/__tests__/browse-client.test.tsx src/components/browse/__tests__/regional-plan-card.test.tsx` all pass
    - `grep -n "motion.img" src/components/browse/destination-card.tsx` returns at least 1 line
    - `grep -n "from-accent to-primary" src/components/browse/destination-card.tsx` returns at least 1 line (typographic gradient)
    - `grep -n "useReducedMotion" src/components/browse/destination-card.tsx` returns at least 1 line
    - `grep -n "refetchCatalogAction" src/components/browse/browse-client.tsx` returns at least 1 line
    - `grep -rn "noResultsSuggestion" src/components/browse/browse-client.tsx` returns 0 (suggestion row removed)
    - `test ! -f src/components/browse/__tests__/destination-grid.test.tsx` (old test removed)
    - `grep -rn "ilike\|textSearch" src/components/browse/` returns 0 (CAT-06 — search stays in-memory)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Destination cards show the typographic fallback when image_url is null and blur-cross-fade the photo in when present; the error banner, search-miss empty state with focus-returning Clear-search, and skeleton states are all wired into BrowseClient; card tests + migrated regional-plan-card test pass.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green; test count does not drop below the pre-Phase-11 baseline (Pitfall 6)
- `npm run build` succeeds
- All 6 locale files carry `browse.error.retry`, `browse.error.message`, `browse.clearSearch`, `browse.noPlans` and the reworded `browse.noResults`
- `grep -rn "ilike\|textSearch" src/components/browse/` returns 0 (CAT-06 search stays client-side)
- `grep -rn "bambu-empty\|bambu-error\|bambu-loading\|BambuVideo" src/components/browse/` returns 0 (no mascot poses)
- Manual (note in VERIFICATION.md): photo blur-cross-fade smoothness (UXD-07), skeleton→grid no-layout-shift (UXD-05), error banner placement above grid (UXD-06), dark-mode gradient card contrast (CAT-07)
</verification>

<success_criteria>
- A destination with null `image_url` shows a typographic name card (bold name on `from-accent to-primary` gradient) — no flag, no placeholder, no broken-image icon (CAT-07)
- A destination with `image_url` set blur-cross-fades a `motion.img` in over the typographic card, reduced-motion safe (UXD-07)
- The catalog fetch shows a 12-card shimmer skeleton grid matching real card dimensions (UXD-05)
- A `getCatalog()` error shows an inline `role="alert"` banner above the grid with a Retry button that re-runs `getCatalog()` via a server action (UXD-06)
- Search-miss shows plain text + a Clear-search button that resets the filter and returns focus to the search input (CAT-06)
</success_criteria>

<output>
After completion, create `.planning/phases/11-read-layer-module-and-browse-cutover/11-02-SUMMARY.md`
</output>
