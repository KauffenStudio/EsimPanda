---
phase: 11-read-layer-module-and-browse-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/destinations.ts
  - src/lib/db/__tests__/destinations.test.ts
  - src/lib/__test-fixtures__/catalog.ts
  - src/app/[locale]/browse/page.tsx
  - src/components/browse/browse-client.tsx
  - src/components/browse/destination-grid.tsx
  - src/hooks/use-destinations.ts
autonomous: true
requirements: [INF-07, INF-08, CAT-05, CAT-06]
must_haves:
  truths:
    - "src/lib/db/destinations.ts exists with import 'server-only' and exports listActiveDestinations, listPlansForDestination, getDestinationBySlug, getPlanById, getCatalog"
    - "getCatalog() returns only curated destinations (popularity_rank < 9999 OR region_bucket IS NOT NULL); uncurated Celitech rows are excluded"
    - "browse/page.tsx is an async RSC with export const revalidate — no 'use client' directive"
    - "BrowseClient receives { destinations, regionalPlans } as props and filters/groups them in-memory with no Supabase call"
  artifacts:
    - path: "src/lib/db/destinations.ts"
      provides: "Typed server-only catalog read module"
      contains: "import 'server-only'"
      exports: ["listActiveDestinations", "listPlansForDestination", "getDestinationBySlug", "getPlanById", "getCatalog", "Destination", "Plan", "CatalogDestination", "Catalog"]
      min_lines: 120
    - path: "src/app/[locale]/browse/page.tsx"
      provides: "Async RSC that fetches the catalog and renders BrowseClient"
      contains: "export const revalidate"
    - path: "src/components/browse/browse-client.tsx"
      provides: "Client boundary owning search filter + region grouping + Framer Motion + comparison"
      contains: "'use client'"
    - path: "src/lib/db/__tests__/destinations.test.ts"
      provides: "Unit tests for getCatalog filtering/partition/enrichment + getDestinationBySlug/getPlanById null-on-empty"
  key_links:
    - from: "src/app/[locale]/browse/page.tsx"
      to: "src/lib/db/destinations.ts"
      via: "await getCatalog() in the async RSC"
      pattern: "getCatalog\\(\\)"
    - from: "src/components/browse/browse-client.tsx"
      to: "props.destinations"
      via: "useMemo in-memory search filter"
      pattern: "useMemo"
---

<objective>
Create the shared, typed, `server-only` catalog read module `src/lib/db/destinations.ts` and convert `app/[locale]/browse/page.tsx` from a `'use client'` page into an async RSC that fetches the catalog server-side via `getCatalog()` and passes `{ destinations, regionalPlans }` as props to a new `<BrowseClient>` client child.

Purpose: This is the foundational read boundary for the entire v1.1 live-data cutover. Phase 12 reuses `getPlanById`; Phase 11-02 and 11-03 depend on the canonical `Destination`/`Plan`/`CatalogDestination` types defined here. Browse stops importing `src/lib/mock-data/`.

Output: `src/lib/db/destinations.ts` (read module + types), `browse/page.tsx` as RSC, `browse-client.tsx` (client boundary), Wave 0 test file + shared fixtures.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-CONTEXT.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-VALIDATION.md
@.planning/phases/11-read-layer-module-and-browse-cutover/11-UI-SPEC.md

<interfaces>
<!-- Canonical types this plan CREATES. 11-02 and 11-03 consume these directly — no codebase exploration needed. -->
<!-- Copy these interfaces verbatim into src/lib/db/destinations.ts. -->

```ts
// src/lib/db/destinations.ts — CREATE
import 'server-only'; // build-time guard: a client component importing this fails the build
import { createClient } from '@/lib/supabase/server';
import { getDiscountPercent } from '@/lib/mock-data/plans'; // pure helper, no I/O — extraction is Phase 13, NOT here

export interface Destination {
  id: string;
  name: string;
  slug: string;
  iso_code: string;
  region: string | null;          // Celitech classifier: 'country' | 'region'
  region_bucket: string | null;   // curation bucket: 'europe' | 'asia' | 'europe-wide' | 'asia-wide' | 'global' | ...
  image_url: string | null;       // NULL for uncurated rows + possible curated gaps → typographic fallback
  popularity_rank: number;        // NOT NULL DEFAULT 9999
  is_active: boolean;
}

export interface Plan {
  id: string;
  destination_id: string;
  wholesale_plan_id: string;
  provider: string;
  name: string;
  data_gb: number;                // Postgres NUMERIC → JS number (eSIM sizes are safe integers)
  duration_days: number;
  wholesale_price_cents: number;
  retail_price_cents: number;
  currency: string;               // assume 'USD'; Phase 12 verifies
  is_active: boolean;
}

// Browse-grid destination enriched with per-destination pricing computed server-side
// so DestinationCard never fetches plans client-side.
export interface CatalogDestination extends Destination {
  startingPriceCents: number;     // min retail_price_cents of its active plans; 0 if none
  bestDiscountPercent: number;    // max getDiscountPercent across its plans; 0 if none
}

export interface Catalog {
  destinations: CatalogDestination[];   // country cards, popularity-sorted
  regionalPlans: CatalogDestination[];  // EU/AS/GL hero cards
  error: boolean;                       // true if the destinations query errored — drives UXD-06 banner in 11-02
}
```
</interfaces>

# Code patterns to mirror (read these before implementing)
@src/lib/db/orders.ts
@src/lib/supabase/server.ts
@src/app/[locale]/esim/[slug]/page.tsx
@src/app/[locale]/browse/page.tsx
@src/components/browse/destination-grid.tsx
@src/hooks/use-destinations.ts
@src/lib/auth/__tests__/order-linking.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create server-only catalog read module + Wave 0 tests</name>
  <files>src/lib/db/destinations.ts, src/lib/db/__tests__/destinations.test.ts, src/lib/__test-fixtures__/catalog.ts</files>
  <read_first>
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-CONTEXT.md
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md (§ Architecture Patterns — Pattern 1, the full getCatalog sketch)
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
    - src/lib/db/orders.ts (the query-module template — mirror its error handling and createClient usage)
    - src/lib/supabase/server.ts (the anon-key createClient factory)
    - src/lib/auth/__tests__/order-linking.test.ts (the vi.mock('@/lib/supabase/server') chain test pattern to copy)
    - src/lib/mock-data/plans.ts (confirm getDiscountPercent is a pure function — it is imported, NOT redefined)
  </read_first>
  <behavior>
    Wave 0 tests in src/lib/db/__tests__/destinations.test.ts (use vi.mock('@/lib/supabase/server') chain from order-linking.test.ts):
    - Test 1 (INF-07): listActiveDestinations calls supabase.from('destinations') with .eq('is_active', true).order('popularity_rank', { ascending: true }); returns data array; returns [] when the query errors.
    - Test 2 (INF-07): getDestinationBySlug returns null (not throw) when maybeSingle yields 0 rows; returns the row when present.
    - Test 3 (INF-07): getPlanById returns null (not throw) when maybeSingle yields 0 rows.
    - Test 4 (CAT-05): getCatalog() EXCLUDES rows where popularity_rank === 9999 AND region_bucket === null; INCLUDES a row with popularity_rank < 9999; INCLUDES a row with region_bucket set even if popularity_rank === 9999.
    - Test 5 (INF-07): getCatalog() partitions region_bucket ∈ {europe-wide, asia-wide, global} into regionalPlans, everything else into destinations.
    - Test 6 (INF-07): getCatalog() enriches each CatalogDestination — startingPriceCents = min retail_price_cents of its active plans (0 if none); bestDiscountPercent = max getDiscountPercent across its plans (0 if none).
    - Test 7 (UXD-06 support): getCatalog() returns error: true when the destinations query errors, error: false on success.
  </behavior>
  <action>
Create `src/lib/db/destinations.ts` mirroring `src/lib/db/orders.ts` style — named async functions, `await createClient()` per call, errors logged via `console.error('<fn> error:', error)`, return `[]`/`null` on error (never throw).

First line MUST be `import 'server-only';` then `import { createClient } from '@/lib/supabase/server';` then `import { getDiscountPercent } from '@/lib/mock-data/plans';`.

Export the interfaces `Destination`, `Plan`, `CatalogDestination`, `Catalog` exactly as given in the `<interfaces>` block above.

Implement the five functions per the RESEARCH §Pattern 1 sketch:
- `listActiveDestinations(): Promise<Destination[]>` — `.from('destinations').select('id, name, slug, iso_code, region, region_bucket, image_url, popularity_rank, is_active').eq('is_active', true).order('popularity_rank', { ascending: true })`. On error: `console.error('listActiveDestinations error:', error); return [];`
- `listPlansForDestination(destinationId: string): Promise<Plan[]>` — `.from('plans').select('*').eq('destination_id', destinationId).eq('is_active', true).order('retail_price_cents', { ascending: true })`. On error return `[]`.
- `getDestinationBySlug(slug: string): Promise<Destination | null>` — same select as listActiveDestinations, `.eq('slug', slug).eq('is_active', true).maybeSingle()`. On error return `null`.
- `getPlanById(planId: string): Promise<Plan | null>` — `.from('plans').select('*').eq('id', planId).maybeSingle()`. On error return `null`.
- `getCatalog(): Promise<Catalog>` — call `listActiveDestinations()`; filter to curated rows with `const curated = all.filter((d) => d.popularity_rank < 9999 || d.region_bucket !== null);`. If `curated.length === 0` return `{ destinations: [], regionalPlans: [], error: false }`. Run ONE batched plans query: `await createClient()` then `.from('plans').select('id, destination_id, data_gb, retail_price_cents, is_active').in('destination_id', ids).eq('is_active', true)`. Index plans by `destination_id` into a `Map`. Enrich each destination: `startingPriceCents = ps.length ? Math.min(...ps.map(p => p.retail_price_cents)) : 0`; `bestDiscountPercent = ps.length ? Math.max(...ps.map(p => getDiscountPercent(p.retail_price_cents, p.data_gb))) : 0`. Partition with `const REGIONAL_BUCKETS = new Set(['europe-wide', 'asia-wide', 'global']);` — `destinations` = `!REGIONAL_BUCKETS.has(d.region_bucket ?? '')`, `regionalPlans` = the inverse. Return `{ destinations, regionalPlans, error }` where `error` is `true` only if the destinations query (inside listActiveDestinations) errored. To surface the destinations-query error: have getCatalog detect it by checking if `listActiveDestinations` returned `[]` is ambiguous — instead, do the destinations query directly inside getCatalog (do not delegate to listActiveDestinations for the error flag), or add an internal helper that returns `{ data, error }`. Recommended: inside getCatalog run the destinations query inline (same select/filter/order as listActiveDestinations) so you can read its `error` and set `Catalog.error` precisely; keep `listActiveDestinations` as a separate public export for callers that don't need the flag.

Add a one-line comment above the `getDiscountPercent` import: `// pure compute helper — no I/O; extraction to src/lib/plans/pricing-display.ts is Phase 13 (INF-11), do not extract here`.

Create `src/lib/__test-fixtures__/catalog.ts` exporting stable fixtures decoupled from `mock-data/`: `export const fixtureDestinations: CatalogDestination[]` (3-5 country destinations e.g. France/Spain/Italy with `region_bucket: 'europe'`, varied `image_url` including one `null`, `popularity_rank` values), `export const fixtureRegionalPlans: CatalogDestination[]` (1-2 with `region_bucket: 'europe-wide'`), and `export const fixturePlans: Plan[]` (2-3 `Plan` objects). Import the `CatalogDestination`/`Plan` types from `@/lib/db/destinations`.

Create `src/lib/db/__tests__/destinations.test.ts` covering Tests 1-7 from `<behavior>`. Use the `vi.mock('@/lib/supabase/server')` chain pattern from `src/lib/auth/__tests__/order-linking.test.ts` — build a mock query builder where `.from().select().eq().order()` etc. resolve to `{ data, error }`. For getCatalog tests, the mock must handle two distinct `.from()` calls (destinations then plans).
  </action>
  <verify>
    <automated>npx vitest run src/lib/db/__tests__/destinations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/db/__tests__/destinations.test.ts` passes (all 7 behavior tests green)
    - `head -1 src/lib/db/destinations.ts` is exactly `import 'server-only';`
    - `grep -n "export async function getCatalog\|export async function listActiveDestinations\|export async function listPlansForDestination\|export async function getDestinationBySlug\|export async function getPlanById" src/lib/db/destinations.ts` returns 5 lines
    - `grep -n "export interface Destination\|export interface Plan\|export interface CatalogDestination\|export interface Catalog" src/lib/db/destinations.ts` returns 4 lines
    - `grep -n "popularity_rank < 9999" src/lib/db/destinations.ts` returns at least 1 line (CAT-05 filter)
  </acceptance_criteria>
  <done>The typed server-only read module exists, exports the 5 functions + 4 interfaces, filters uncurated rows, and all Wave 0 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Convert browse page to async RSC + create BrowseClient boundary</name>
  <files>src/app/[locale]/browse/page.tsx, src/components/browse/browse-client.tsx, src/components/browse/destination-grid.tsx, src/hooks/use-destinations.ts</files>
  <read_first>
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-CONTEXT.md
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-RESEARCH.md (§ Pattern 2 — the BEFORE/AFTER browse RSC sketch; § Pattern 3 — region grouping by region_bucket; Open Question 1 — delete use-destinations.ts)
    - .planning/phases/11-read-layer-module-and-browse-cutover/11-UI-SPEC.md (page container px-4 pt-6 pb-20 max-w-[1200px] mx-auto; grid contract)
    - src/app/[locale]/esim/[slug]/page.tsx (async RSC precedent — setRequestLocale, getTranslations, revalidate)
    - src/app/[locale]/browse/page.tsx (current 'use client' page — the BEFORE state)
    - src/components/browse/destination-grid.tsx (current grid body — groupByRegion, CountryGrid, region pills, useDestinations)
    - src/hooks/use-destinations.ts (sync mock hook being deleted)
    - src/lib/db/destinations.ts (the module created in Task 1 — getCatalog + CatalogDestination type)
  </read_first>
  <action>
Convert `src/app/[locale]/browse/page.tsx` to an async RSC per the RESEARCH §Pattern 2 AFTER sketch:
- Remove the `'use client'` directive.
- Add `export const revalidate = 3600;` (ISR — matches `esim/[slug]/page.tsx`; tunable at your discretion but 3600 is the researched default).
- Signature: `export default async function BrowsePage({ params }: { params: Promise<{ locale: string }> })`.
- Body: `const { locale } = await params; setRequestLocale(locale); const t = await getTranslations();` (import `getTranslations, setRequestLocale` from `next-intl/server` — NOT the `useTranslations` hook).
- `const { destinations, regionalPlans, error } = await getCatalog();` (import `getCatalog` from `@/lib/db/destinations`).
- Keep the page container exactly `<div className="px-4 pt-6 pb-20 max-w-[1200px] mx-auto">`, the `<h1>` with `text-3xl md:text-4xl font-bold tracking-tighter`, and `<WelcomeDiscountBanner showCta={false} />` as-is.
- `<CurrencySwitcher />` is a client component — keep rendering it as a child of the RSC (valid; no change needed).
- Render `<BrowseClient destinations={destinations} regionalPlans={regionalPlans} error={error} />`.

Create `src/components/browse/browse-client.tsx` as the `'use client'` boundary — this is the current `destination-grid.tsx` body made prop-driven:
- `'use client';` directive at the top.
- Props interface: `interface BrowseClientProps { destinations: CatalogDestination[]; regionalPlans: CatalogDestination[]; error: boolean; }` — import `CatalogDestination` from `@/lib/db/destinations` (type-only import; the module's runtime code never loads client-side because only the type is imported).
- Move ALL interactive logic from `destination-grid.tsx` into here: `useBrowseStore` search state, `useState(selectedRegion)`, region pills, `AnimatePresence`/Framer Motion, `useComparisonStore`, the `DestinationSearch`, `RegionalPlanCard`, `CountryGrid`, `ComparisonBar`, `ComparisonSheet` render tree.
- In-memory search filter via `useMemo`: `const filtered = useMemo(() => destinations.filter((d) => d.name.toLowerCase().includes(searchQuery.toLowerCase())), [destinations, searchQuery]);`. NO Supabase call, NO useEffect fetch, NO `useDestinations()` — data arrives entirely as props (Pitfall 4: hydration-safe, props-only on first render).
- Region grouping: move `groupByRegion` in but change the grouping key from `d.region` to `d.region_bucket` (Pitfall §Pattern 3 — post-Phase-10 the live `region` column holds Celitech's `'country'/'region'` classifier; the UI groups by `region_bucket`). Keep the existing `REGION_ORDER`/`regionLabels` maps unchanged (they already use `europe`/`asia`/`north-america` values that match `region_bucket`).
- The `error`, search-miss, and skeleton STATES are wired by plan 11-02 — for now, accept the `error` prop and leave a clearly-marked `{/* 11-02: error banner renders here when error === true */}` placeholder above the grid. Do not break the build.
- Remove the `BambuVideo` import (CONTEXT decision — no Bambu poses on browse).
- `regionalPlans` are passed through to `RegionalPlanCard` rendering (11-02 adapts the card props shape).

For `destination-grid.tsx`: its only consumer is `browse/page.tsx`. Per RESEARCH Open Question Pattern 2, make `browse-client.tsx` the real component and DELETE `src/components/browse/destination-grid.tsx` (move its remaining helpers — `groupByRegion`, `CountryGrid`, `REGION_ORDER`, `regionLabels` — into `browse-client.tsx`). If a test still imports `destination-grid` it is migrated in 11-02 (Wave 0 `browse-client.test.tsx`).

DELETE `src/hooks/use-destinations.ts` — per RESEARCH Open Question 1, its only consumers (`destination-grid.tsx`, `regional-plan-card.tsx`) are rewritten to take props; it has no test. Leave `src/hooks/use-plans.ts` UNTOUCHED (its consumer `esim/[slug]` stays on mock until Phase 12).

NOTE: `regional-plan-card.tsx` currently calls `useDestinations()` — to keep the build green after deleting the hook, in this task pass `regionalPlans` data into `RegionalPlanCard` as a prop (the full prop-shape adaptation is 11-02's job; here just unblock the build by threading the prop). If a clean prop pass is not trivially possible, 11-02 owns it — but the build MUST stay green at the end of this task.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - `grep -cn "use client" "src/app/[locale]/browse/page.tsx"` returns 0
    - `grep -n "export const revalidate" "src/app/[locale]/browse/page.tsx"` returns 1 line
    - `grep -n "getCatalog" "src/app/[locale]/browse/page.tsx"` returns at least 1 line
    - `head -1 src/components/browse/browse-client.tsx` is `'use client';`
    - `grep -rn "BambuVideo\|bambu-video\|bambu-empty\|bambu-error" src/components/browse/browse-client.tsx` returns 0
    - `grep -rn "useDestinations" src/components/browse/ src/app/[locale]/browse/` returns 0
    - `test ! -f src/hooks/use-destinations.ts` (file deleted)
  </acceptance_criteria>
  <done>browse/page.tsx is an async RSC with no 'use client', fetches via getCatalog(), and renders BrowseClient with catalog props; BrowseClient owns search/grouping/comparison in-memory; the Bambu pose import and the use-destinations mock hook are gone; the build is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/db/__tests__/destinations.test.ts` — all 7 Wave 0 tests green
- `npm run build` succeeds (proves the RSC compiles and `import 'server-only'` is not violated by a client import)
- `grep -cn "use client" "src/app/[locale]/browse/page.tsx"` returns 0 (INF-08)
- `grep -rn "useDestinations\|mock-data/destinations" src/components/browse/browse-client.tsx src/app/[locale]/browse/page.tsx` returns 0 (no mock imports in the browse path)
- `npm test` full suite stays green; test count does not drop below the recorded pre-Phase-11 baseline
</verification>

<success_criteria>
- `src/lib/db/destinations.ts` exists, `import 'server-only'` first line, exports the 5 functions + 4 interfaces, `getCatalog()` excludes uncurated rows (CAT-05) and partitions regional vs country (INF-07)
- `browse/page.tsx` is an async RSC with `export const revalidate`, calls `getCatalog()`, renders `<BrowseClient>` (INF-08)
- `<BrowseClient>` filters/groups the catalog in-memory from props with no network round-trip per keystroke (CAT-06)
- `use-destinations.ts` deleted; `BambuVideo` import removed from the browse path
</success_criteria>

<output>
After completion, create `.planning/phases/11-read-layer-module-and-browse-cutover/11-01-SUMMARY.md`
</output>
