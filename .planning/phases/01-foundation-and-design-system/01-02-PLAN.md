---
phase: 01-foundation-and-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/layout/bottom-nav.tsx
  - src/components/layout/header.tsx
  - src/components/layout/page-transition.tsx
  - src/components/layout/theme-toggle.tsx
  - src/components/bambu/bambu-base.tsx
  - src/components/bambu/bambu-loading.tsx
  - src/components/bambu/bambu-success.tsx
  - src/components/bambu/bambu-error.tsx
  - src/components/bambu/bambu-empty.tsx
  - src/stores/theme.ts
  - src/components/ui/__tests__/button.test.tsx
  - src/components/ui/__tests__/card.test.tsx
autonomous: true
requirements:
  - UXD-01

must_haves:
  truths:
    - "Button component renders 4 variants (primary, secondary, ghost, destructive) with correct accent colors"
    - "Card component renders with shadow-card and transitions to shadow-card-hover on hover"
    - "Bambu mascot renders different SVG poses based on pose prop"
    - "BambuLoading shows continuous eating bamboo animation with spring physics"
    - "BottomNav shows 4 tabs with sliding active indicator animated via spring"
    - "PageTransition wraps content with slide+fade animation keyed on pathname"
    - "ThemeToggle toggles dark class on html element and persists to localStorage"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button primitive with variants"
      exports: ["Button"]
    - path: "src/components/bambu/bambu-base.tsx"
      provides: "Base Bambu component with pose system"
      exports: ["Bambu"]
    - path: "src/components/bambu/bambu-loading.tsx"
      provides: "Bambu eating bamboo loading animation"
      exports: ["BambuLoading"]
    - path: "src/components/layout/bottom-nav.tsx"
      provides: "Mobile bottom tab navigation"
      exports: ["BottomNav"]
    - path: "src/components/layout/page-transition.tsx"
      provides: "AnimatePresence page transition wrapper"
      exports: ["PageTransition"]
    - path: "src/stores/theme.ts"
      provides: "Dark mode toggle store"
      exports: ["useThemeStore"]
  key_links:
    - from: "src/components/bambu/bambu-loading.tsx"
      to: "src/components/bambu/bambu-base.tsx"
      via: "imports Bambu base SVG elements"
      pattern: "import.*bambu-base"
    - from: "src/components/layout/theme-toggle.tsx"
      to: "src/stores/theme.ts"
      via: "useThemeStore for toggle state"
      pattern: "useThemeStore"
    - from: "src/components/layout/bottom-nav.tsx"
      to: "motion/react"
      via: "spring-animated active tab indicator"
      pattern: "motion\\.div"
---

<objective>
Build the eSIM Panda design system: UI primitives (Button, Card, Input, Badge), layout components (BottomNav, Header, PageTransition, ThemeToggle), and the Bambu mascot pose system with Motion animations.

Purpose: Establish the premium visual identity with micro-interactions that differentiate eSIM Panda from competitors.
Output: Complete component library ready to compose into pages in later phases.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-and-design-system/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs needed by this plan -->

From src/styles/globals.css (Tailwind v4 theme tokens available as utilities):
- bg-accent (#2979FF), text-accent, bg-surface (#F5F5F5), bg-primary (#000000)
- rounded-card (10px), rounded-button (8px), rounded-input (8px), rounded-badge (9999px)
- shadow-card, shadow-card-hover
- font-sans (Plus Jakarta Sans)
- bg-destructive (#E53935), bg-success (#43A047), bg-warning (#FB8C00)
- bg-accent-hover (#2264D1), bg-accent-pressed (#1B4F9E), bg-accent-soft (#E3F0FF)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI primitives (Button, Card, Input, Badge) and theme store with Motion animations</name>
  <files>
    src/components/ui/button.tsx, src/components/ui/card.tsx,
    src/components/ui/input.tsx, src/components/ui/badge.tsx,
    src/stores/theme.ts, src/components/layout/theme-toggle.tsx,
    src/components/ui/__tests__/button.test.tsx, src/components/ui/__tests__/card.test.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
    src/styles/globals.css
  </read_first>
  <action>
1. Create `src/stores/theme.ts` — Zustand store for dark mode:
   ```typescript
   import { create } from 'zustand';
   import { persist } from 'zustand/middleware';

   interface ThemeState {
     isDark: boolean;
     toggle: () => void;
   }

   export const useThemeStore = create<ThemeState>()(
     persist(
       (set) => ({
         isDark: false,
         toggle: () => set((state) => {
           const newDark = !state.isDark;
           if (typeof document !== 'undefined') {
             document.documentElement.classList.toggle('dark', newDark);
           }
           return { isDark: newDark };
         }),
       }),
       { name: 'esim-panda-theme' }
     )
   );
   ```
   Manual toggle only -- NO system auto-detect per user decision.

2. Create `src/components/ui/button.tsx` — 'use client' component:
   - Props: variant ('primary' | 'secondary' | 'ghost' | 'destructive'), size ('sm' | 'md' | 'lg'), children, className, disabled, onClick, type
   - Wraps a motion.button from 'motion/react'
   - Primary variant: bg-accent text-white hover:bg-accent-hover active:bg-accent-pressed
   - Secondary variant: border border-border bg-transparent text-primary hover:bg-surface
   - Ghost variant: bg-transparent text-primary hover:bg-surface
   - Destructive variant: bg-destructive text-white hover:bg-destructive-hover
   - All variants: rounded-button (8px), font-semibold, min-h-[44px] for mobile touch target
   - Motion: whileTap={{ scale: 0.97 }} with transition {{ duration: 0.15, ease: 'easeInOut' }}
   - Haptic feedback: onClick wrapper that calls navigator.vibrate?.(10) before the actual onClick
   - Disabled state: opacity-50 pointer-events-none

3. Create `src/components/ui/card.tsx` — 'use client' component:
   - Props: variant ('elevated' | 'flat'), children, className, onClick
   - Elevated variant: shadow-card rounded-card bg-white dark:bg-surface-dark
   - Flat variant: rounded-card bg-surface dark:bg-surface-dark (no shadow)
   - Hover: transition-shadow duration-200 hover:shadow-card-hover (elevated only)
   - Uses standard div, not motion.div (shadow transition via CSS is sufficient)

4. Create `src/components/ui/input.tsx` — 'use client' component:
   - Props: label, error (string), disabled, and all standard input HTML attributes
   - Styling: border border-border rounded-input bg-white dark:bg-surface-dark px-3 py-2 text-base
   - Focus: ring-2 ring-accent outline-none (150ms ease-in-out transition)
   - Error state: border-destructive text-destructive, error message below in 14px destructive text
   - Disabled: opacity-50 cursor-not-allowed bg-surface

5. Create `src/components/ui/badge.tsx`:
   - Props: variant ('default' | 'accent' | 'success' | 'warning'), children, className
   - Styling: inline-flex items-center rounded-badge px-2 py-0.5 text-sm (14px)
   - Default: bg-surface text-gray-600
   - Accent: bg-accent-soft text-accent
   - Success: bg-success/10 text-success
   - Warning: bg-warning/10 text-warning

6. Create `src/components/layout/theme-toggle.tsx` — 'use client' component:
   - Imports useThemeStore from '@/stores/theme'
   - Imports Sun, Moon from lucide-react
   - Renders motion.button with rotating icon (rotate 180deg on swap, 300ms ease-in-out)
   - Shows Sun icon when dark, Moon icon when light
   - onClick calls toggle()

7. Create `src/components/ui/__tests__/button.test.tsx`:
   - Test: renders with primary variant class
   - Test: renders with destructive variant class
   - Test: disabled button has opacity-50
   - Test: renders children text

8. Create `src/components/ui/__tests__/card.test.tsx`:
   - Test: elevated variant has shadow-card class
   - Test: flat variant does not have shadow-card class

CRITICAL: All imports from 'motion/react' -- NOT 'framer-motion'.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/button.tsx contains "'use client'" and "motion/react" and "whileTap" and "scale: 0.97" and "navigator.vibrate"
    - src/components/ui/button.tsx contains "bg-accent" and "bg-destructive" and "rounded-button"
    - src/components/ui/card.tsx contains "shadow-card" and "rounded-card"
    - src/components/ui/input.tsx contains "ring-accent" and "border-destructive" and "rounded-input"
    - src/components/ui/badge.tsx contains "rounded-badge" and "bg-accent-soft"
    - src/stores/theme.ts contains "useThemeStore" and "persist" and "esim-panda-theme" and "classList.toggle"
    - src/stores/theme.ts does NOT contain "matchMedia" or "prefers-color-scheme" (no auto-detect)
    - src/components/layout/theme-toggle.tsx contains "useThemeStore" and "Sun" and "Moon" and "rotate"
    - No file in src/ contains "framer-motion" (must use "motion/react")
    - `npx vitest run src/components/ui/__tests__/ --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>4 UI primitives (Button, Card, Input, Badge) with correct brand tokens and Motion animations, theme store with manual-only toggle persisted to localStorage, ThemeToggle component with rotating icon, and passing component tests.</done>
</task>

<task type="auto">
  <name>Task 2: Layout components (BottomNav, Header, PageTransition) and Bambu mascot pose system</name>
  <files>
    src/components/layout/bottom-nav.tsx, src/components/layout/header.tsx,
    src/components/layout/page-transition.tsx,
    src/components/bambu/bambu-base.tsx, src/components/bambu/bambu-loading.tsx,
    src/components/bambu/bambu-success.tsx, src/components/bambu/bambu-error.tsx,
    src/components/bambu/bambu-empty.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
    .planning/phases/01-foundation-and-design-system/01-CONTEXT.md
    src/components/ui/button.tsx
    src/stores/theme.ts
  </read_first>
  <action>
1. Create `src/components/layout/header.tsx` — 'use client' component:
   - Fixed top, h-14 (56px), bg-white dark:bg-background-dark, border-b border-border dark:border-border-dark
   - Left: Panda emoji or small SVG icon + "eSIM Panda" text in font-bold text-primary
   - Right: ThemeToggle component
   - Desktop (md+): Add horizontal nav links center (Home, Destinations, My eSIMs, Profile) using regular Link components
   - z-50 to stay above content

2. Create `src/components/layout/bottom-nav.tsx` — 'use client' component:
   - Fixed bottom, h-16 (64px) + pb-4 (safe area), bg-white dark:bg-background-dark, border-t border-border
   - 4 tabs in a flex row, each taking equal width:
     - Home (Home icon from lucide-react, href "/")
     - Destinations (Globe icon, href "/browse")
     - My eSIMs (Smartphone icon, href "/dashboard")
     - Profile (User icon, href "/profile")
   - Active tab detection: compare current pathname (usePathname from next/navigation)
   - Active tab indicator: motion.div with layoutId="activeTab" underneath the active icon, bg-accent rounded-full h-0.5
   - Spring animation on indicator: transition={{ type: 'spring', stiffness: 400, damping: 25 }}
   - Active icon/label: text-accent; inactive: text-gray-400
   - Tab labels below icons in text-xs (14px label style)
   - Haptic feedback on tap: navigator.vibrate?.(10)
   - Hidden on desktop: md:hidden
   - Min touch target: 48px per tab

3. Create `src/components/layout/page-transition.tsx` — 'use client' component:
   - Imports AnimatePresence, motion from 'motion/react'
   - Imports usePathname from 'next/navigation'
   - Wraps children in AnimatePresence mode="wait"
   - Inner motion.div keyed on pathname:
     - initial={{ opacity: 0, x: 20 }}
     - animate={{ opacity: 1, x: 0 }}
     - exit={{ opacity: 0, x: -20 }}
     - transition={{ duration: 0.3, ease: 'easeInOut' }}

4. Create `src/components/bambu/bambu-base.tsx` — 'use client' component:
   - Type BambuPose = 'idle' | 'loading' | 'success' | 'error' | 'browse' | 'empty'
   - Props: pose (BambuPose), size (number, default 120), className
   - Renders an SVG panda character — a simple flat illustration (Duolingo-style):
     - Black circular ears, white circular face, black eye patches, small dot eyes, oval nose, curved mouth
     - Body is a rounded rectangle, black with white belly
     - Arms are simple black rounded shapes
   - The SVG should be self-contained inline SVG (not an external file)
   - Wraps in motion.div with spring transition: { type: 'spring', stiffness: 300, damping: 15 }
   - Pose variants object:
     - idle: { rotate: 0, scale: 1 }
     - loading: handled by BambuLoading separately
     - success: { scale: [1, 1.2, 1], rotate: [0, -5, 5, 0] }
     - error: { rotate: [-3, 3, -3, 0] } (gentle wobble)
     - browse: { scale: 1, rotate: -5 } (tilted, looking)
     - empty: { y: [0, -5, 0] } (subtle floating)
   - Export both Bambu component and BambuPose type

5. Create `src/components/bambu/bambu-loading.tsx` — 'use client' component:
   - Imports base panda SVG elements from bambu-base or duplicates a simplified version
   - Continuous animation: panda arm moves up/down (eating bamboo motion)
   - Uses motion.div with animate={{ rotate: [0, -10, 0, 10, 0] }} and transition={{ repeat: Infinity, duration: 2, ease: 'easeInOut' }}
   - A small bamboo stick SVG element (green lines) near the panda's mouth area
   - Spring physics: type: 'spring', stiffness: 300, damping: 15
   - This replaces ALL spinners/skeleton screens in the app

6. Create `src/components/bambu/bambu-success.tsx` — 'use client' component:
   - Happy bounce animation: scale [1, 1.3, 0.9, 1.1, 1] with spring stiffness: 400, damping: 10
   - Plays once on mount, higher energy than other poses
   - Can include small sparkle/star SVG elements around the panda

7. Create `src/components/bambu/bambu-error.tsx` — 'use client' component:
   - Apologetic wobble: rotate [-5, 5, -3, 3, 0] with spring stiffness: 200, damping: 20
   - SVG includes a sweat drop element (blue teardrop) on the panda's forehead
   - Gentler, sympathetic animation

8. Create `src/components/bambu/bambu-empty.tsx` — 'use client' component:
   - Curious pose with subtle idle sway: y [0, -8, 0] repeating
   - Spring stiffness: 100, damping: 30 (very subtle, breathing feel)
   - Panda has curious wide eyes expression

CRITICAL: All motion imports from 'motion/react'. Bambu animations use spring physics (playful, bouncy). UI animations use ease-in-out (smooth, elegant). This contrast is intentional per CONTEXT.md.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/bottom-nav.tsx contains "layoutId" and "stiffness: 400" and "damping: 25" and "md:hidden" and "navigator.vibrate"
    - src/components/layout/bottom-nav.tsx contains "Home" and "Globe" and "Smartphone" and "User" (Lucide icons)
    - src/components/layout/header.tsx contains "h-14" or "56px" and "eSIM Panda" and "ThemeToggle"
    - src/components/layout/page-transition.tsx contains "AnimatePresence" and "usePathname" and "opacity: 0, x: 20"
    - src/components/bambu/bambu-base.tsx contains "BambuPose" and "stiffness: 300" and "damping: 15" and "<svg"
    - src/components/bambu/bambu-loading.tsx contains "repeat: Infinity" and "motion" (continuous animation)
    - src/components/bambu/bambu-success.tsx contains "stiffness: 400" and "damping: 10" (higher energy)
    - src/components/bambu/bambu-error.tsx contains "stiffness: 200" and "damping: 20" (gentler)
    - src/components/bambu/bambu-empty.tsx contains "stiffness: 100" and "damping: 30" (very subtle)
    - No file in src/components/ contains "framer-motion"
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Layout components (Header with theme toggle and desktop nav, BottomNav with spring-animated tab indicator and haptic feedback, PageTransition with slide+fade) and Bambu mascot system (base with 6 poses, loading with eating bamboo loop, success with bounce, error with wobble, empty with idle sway) all build without errors.</done>
</task>

</tasks>

<verification>
- `npx next build` exits 0
- `npx vitest run --reporter=verbose` exits 0 (all component tests pass)
- All motion imports use 'motion/react', zero imports from 'framer-motion'
- Bambu components use spring physics (stiffness/damping), UI components use ease-in-out
- BottomNav hidden on desktop (md:hidden), Header shows horizontal nav on desktop
</verification>

<success_criteria>
- 4 UI primitives render with correct brand tokens and animations
- Theme toggle persists to localStorage, toggles dark class, manual-only (no auto-detect)
- BottomNav has 4 tabs with spring-animated active indicator
- PageTransition wraps content with slide+fade keyed on pathname
- Bambu has 5 state components (base, loading, success, error, empty) with distinct spring configs
- All components build in Next.js without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-02-SUMMARY.md`
</output>
