---
phase: 09-pwa-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/components/browse/comparison-bar.tsx
  - src/components/browse/destination-card.tsx
  - src/components/browse/plan-accordion.tsx
  - src/components/browse/regional-plan-card.tsx
  - src/components/checkout/checkout-page.tsx
  - src/components/checkout/confetti-effect.tsx
  - src/components/checkout/coupon-input.tsx
  - src/components/checkout/device-check.tsx
  - src/components/checkout/email-field.tsx
  - src/components/checkout/express-checkout.tsx
  - src/components/checkout/order-summary.tsx
  - src/components/checkout/pay-button.tsx
  - src/components/checkout/payment-divider.tsx
  - src/components/checkout/payment-error.tsx
  - src/components/checkout/payment-processing.tsx
  - src/components/checkout/payment-success.tsx
  - src/components/dashboard/circular-gauge.tsx
  - src/components/dashboard/dashboard-skeleton.tsx
  - src/components/dashboard/dashboard-tabs.tsx
  - src/components/dashboard/esim-card.tsx
  - src/components/dashboard/esim-grid.tsx
  - src/components/dashboard/low-data-banner.tsx
  - src/components/dashboard/purchase-history-list.tsx
  - src/components/dashboard/purchase-history-row.tsx
  - src/components/dashboard/top-up-modal.tsx
  - src/components/dashboard/top-up-plan-card.tsx
  - src/components/dashboard/usage-timestamp.tsx
  - src/components/delivery/copyable-field.tsx
  - src/components/delivery/delivery-page.tsx
  - src/components/delivery/esim-credentials.tsx
  - src/components/delivery/install-button.tsx
  - src/components/delivery/manual-codes.tsx
  - src/components/delivery/provisioning-error.tsx
  - src/components/delivery/provisioning-state.tsx
  - src/components/delivery/qr-code-display.tsx
  - src/components/delivery/setup-guide.tsx
  - src/components/delivery/setup-steps.tsx
  - src/components/bambu/bambu-base.tsx
  - src/components/bambu/bambu-empty.tsx
  - src/components/bambu/bambu-error.tsx
  - src/components/bambu/bambu-loading.tsx
  - src/components/bambu/bambu-preparing.tsx
  - src/components/bambu/bambu-success.tsx
  - src/components/bambu/bambu-welcome.tsx
  - src/components/referral/post-purchase-share-cta.tsx
  - src/components/referral/referral-link-card.tsx
  - src/components/referral/referral-stats.tsx
  - src/components/referral/share-buttons.tsx
  - src/components/seo/breadcrumb.tsx
  - src/components/seo/destination-hero.tsx
  - src/components/seo/faq-section.tsx
  - src/components/seo/json-ld.tsx
  - src/components/admin/coupon-create-form.tsx
  - src/components/admin/coupon-stats-card.tsx
  - src/components/admin/coupon-table.tsx
  - src/components/auth/auth-provider.tsx
  - src/components/layout/page-transition.tsx
  - src/components/layout/whatsapp-button.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
autonomous: true
requirements: [UXD-03]

must_haves:
  truths:
    - "Toggling dark mode via ThemeToggle applies dark styling to ALL components, not just the ones already treated"
    - "No white flash on page load when dark mode is saved in localStorage"
    - "Dark mode uses true dark background (#111111) with appropriate surface (#1A1A1A) and border (#2A2A2A) colors"
    - "Accent blue (#2979FF) remains unchanged in both light and dark modes"
    - "Bambu mascot has a subtle glow effect in dark mode for visibility"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Extended dark mode CSS tokens"
      contains: "--color-accent-soft-dark"
    - path: "src/components/ui/button.tsx"
      provides: "Dark mode button variants"
      contains: "dark:"
    - path: "src/components/dashboard/esim-card.tsx"
      provides: "Dark mode dashboard card"
      contains: "dark:"
  key_links:
    - from: "src/stores/theme.ts"
      to: "document.documentElement"
      via: "classList.toggle('dark')"
      pattern: "classList\\.toggle.*dark"
    - from: "src/styles/globals.css"
      to: "all components"
      via: "@variant dark and dark: prefix"
      pattern: "dark:"
---

<objective>
Complete dark mode across ALL 60 remaining components by adding `dark:` Tailwind variant classes and extending CSS tokens.

Purpose: Fulfills UXD-03 (dark mode support). The toggle and store already exist — this plan completes the visual treatment so every component responds to theme changes.
Output: Every UI component in the app has appropriate dark mode styling
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pwa-and-polish/09-CONTEXT.md
@.planning/phases/09-pwa-and-polish/09-UI-SPEC.md

<interfaces>
<!-- Existing dark mode infrastructure -->
From src/stores/theme.ts:
```typescript
export const useThemeStore = create<ThemeState>()(
  persist(
    (set) => ({
      isDark: false,
      toggle: () => set((state) => {
        const newDark = !state.isDark;
        document.documentElement.classList.toggle('dark', newDark);
        return { isDark: newDark };
      }),
    }),
    { name: 'esim-panda-theme' }
  )
);
```

From src/styles/globals.css:
```css
@variant dark (&:where(.dark, .dark *));
@theme {
  --color-background: #FFFFFF;
  --color-background-dark: #111111;
  --color-surface: #F5F5F5;
  --color-surface-dark: #1A1A1A;
  --color-border: #E5E5E5;
  --color-border-dark: #2A2A2A;
  --color-gray-100: #F5F5F5;
  --color-gray-400: #9E9E9E;
  --color-gray-600: #616161;
  --color-gray-800: #212121;
  --color-accent: #2979FF;
  --color-accent-soft: #E3F0FF;
}
```

<!-- Pattern from already-dark-mode-treated components -->
From src/components/layout/header.tsx (reference pattern):
```
dark:bg-background-dark dark:border-border-dark
```

From src/components/ui/card.tsx (reference pattern):
```
dark:bg-surface-dark dark:border-border-dark
```

From src/components/ui/input.tsx (reference pattern):
```
dark:bg-surface-dark dark:border-border-dark dark:text-gray-100
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dark mode CSS tokens in globals.css</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current tokens — will extend the @theme block)
    - .planning/phases/09-pwa-and-polish/09-UI-SPEC.md (dark mode color specs)
  </read_first>
  <action>
Add the following new tokens to the existing `@theme` block in `src/styles/globals.css`:

```css
--color-accent-soft-dark: #1A2744;
--color-text-primary: #212121;
--color-text-primary-dark: #F5F5F5;
--color-text-secondary: #616161;
--color-text-secondary-dark: #9E9E9E;
--color-text-muted: #9E9E9E;
--color-text-muted-dark: #616161;
--color-overlay: rgba(0, 0, 0, 0.5);
--color-overlay-dark: rgba(0, 0, 0, 0.7);
--color-offline-banner: #FFF3E0;
--color-offline-banner-dark: #2A1F00;
--color-offline-text: #E65100;
--color-offline-text-dark: #FFB74D;
--color-destructive-dark: #EF5350;
--color-success-dark: #66BB6A;
--color-warning-dark: #FFA726;
--shadow-card-dark: 0 1px 3px rgba(0, 0, 0, 0.3), 0 1px 2px rgba(0, 0, 0, 0.2);
--shadow-card-hover-dark: 0 4px 12px rgba(0, 0, 0, 0.4);
```

Keep ALL existing tokens unchanged. Only ADD new ones.
  </action>
  <verify>
    <automated>grep -c "accent-soft-dark" src/styles/globals.css && grep -c "text-primary-dark" src/styles/globals.css && grep -c "overlay-dark" src/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/globals.css contains `--color-accent-soft-dark: #1A2744`
    - src/styles/globals.css contains `--color-text-primary-dark: #F5F5F5`
    - src/styles/globals.css contains `--color-text-secondary-dark: #9E9E9E`
    - src/styles/globals.css contains `--color-overlay-dark: rgba(0, 0, 0, 0.7)`
    - src/styles/globals.css contains `--color-destructive-dark: #EF5350`
    - src/styles/globals.css contains `--shadow-card-dark:`
    - src/styles/globals.css still contains `@variant dark (&:where(.dark, .dark *))` (unchanged)
    - src/styles/globals.css still contains `--color-background: #FFFFFF` (existing tokens unchanged)
  </acceptance_criteria>
  <done>globals.css has all dark mode semantic tokens needed by components</done>
</task>

<task type="auto">
  <name>Task 2a: Apply dark: classes to UI, layout, bambu, and browse components (20 files)</name>
  <files>
    src/components/ui/badge.tsx,
    src/components/ui/button.tsx,
    src/components/layout/page-transition.tsx,
    src/components/layout/whatsapp-button.tsx,
    src/components/auth/auth-provider.tsx,
    src/components/bambu/bambu-base.tsx,
    src/components/bambu/bambu-empty.tsx,
    src/components/bambu/bambu-error.tsx,
    src/components/bambu/bambu-loading.tsx,
    src/components/bambu/bambu-preparing.tsx,
    src/components/bambu/bambu-success.tsx,
    src/components/bambu/bambu-welcome.tsx,
    src/components/browse/comparison-bar.tsx,
    src/components/browse/destination-card.tsx,
    src/components/browse/plan-accordion.tsx,
    src/components/browse/regional-plan-card.tsx,
    src/components/seo/breadcrumb.tsx,
    src/components/seo/destination-hero.tsx,
    src/components/seo/faq-section.tsx,
    src/components/seo/json-ld.tsx
  </files>
  <read_first>
    - src/styles/globals.css (the tokens just added in Task 1)
    - src/components/ui/card.tsx (reference: already has dark: pattern — follow this exact approach)
    - src/components/layout/header.tsx (reference: already has dark: pattern for layout elements)
    - src/components/browse/plan-card.tsx (reference: already has dark: pattern for cards)
    - .planning/phases/09-pwa-and-polish/09-UI-SPEC.md (dark treatment table)
  </read_first>
  <action>
Apply `dark:` variant classes to the 20 components listed in files. Read each file, identify its className strings, and add the appropriate dark variant. Follow these rules:

**MAPPING RULES (apply consistently):**

| Light class | Add dark variant |
|-------------|-----------------|
| `bg-white` or `bg-background` | `dark:bg-background-dark` |
| `bg-surface` or `bg-gray-100` | `dark:bg-surface-dark` |
| `bg-accent-soft` | `dark:bg-accent-soft-dark` |
| `border-border` or `border-gray-200` | `dark:border-border-dark` |
| `text-primary` or `text-gray-800` | `dark:text-gray-100` |
| `text-gray-600` | `dark:text-gray-400` |
| `text-gray-400` | `dark:text-gray-600` |
| `shadow-card` | `dark:shadow-card-dark` |
| `shadow-card-hover` | `dark:shadow-card-hover-dark` |
| `divide-gray-200` or `divide-border` | `dark:divide-border-dark` |
| Any inline `style={{ color: '#616161' }}` | Replace with className `text-gray-600 dark:text-gray-400` and remove inline style |
| Any inline `style={{ color: '#212121' }}` | Replace with className `text-gray-800 dark:text-gray-100` and remove inline style |

**SPECIAL CASES for this batch:**

- **Bambu components** (7 files): Add `dark:drop-shadow-[0_0_8px_rgba(255,255,255,0.15)]` to the outermost SVG/img wrapper for subtle glow in dark mode
- **WhatsApp button**: Green stays unchanged, no dark override needed
- **json-ld.tsx**: No visual element, skip dark mode (just a script tag)
- **auth-provider.tsx**: No visual element, skip dark mode (just a context provider)
- **page-transition.tsx**: No background color, skip dark mode (just AnimatePresence wrapper)

**PROCESS per file:**
1. Read the file
2. Find all className strings
3. For each bg/text/border/shadow class, add the corresponding dark: variant from the mapping table
4. For inline styles with hardcoded colors, replace with Tailwind classes
5. Do NOT change any logic, structure, imports, or behavior — ONLY add dark: classes
  </action>
  <verify>
    <automated>echo "Batch 2a check:"; for f in src/components/ui/button.tsx src/components/ui/badge.tsx src/components/bambu/bambu-base.tsx src/components/browse/destination-card.tsx src/components/seo/faq-section.tsx; do grep -q "dark:" "$f" && echo "$f: OK" || echo "$f: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/button.tsx contains `dark:`
    - src/components/ui/badge.tsx contains `dark:`
    - src/components/browse/destination-card.tsx contains `dark:`
    - src/components/browse/comparison-bar.tsx contains `dark:`
    - src/components/bambu/bambu-base.tsx contains `dark:drop-shadow`
    - src/components/bambu/bambu-success.tsx contains `dark:drop-shadow`
    - src/components/seo/faq-section.tsx contains `dark:`
    - src/components/seo/breadcrumb.tsx contains `dark:`
  </acceptance_criteria>
  <done>All 20 UI, layout, bambu, browse, and SEO components have dark: classes applied. Bambu mascots have dark mode glow. Skip files (json-ld, auth-provider, page-transition) confirmed as no-ops.</done>
</task>

<task type="auto">
  <name>Task 2b: Apply dark: classes to checkout and delivery components (21 files)</name>
  <files>
    src/components/checkout/checkout-page.tsx,
    src/components/checkout/confetti-effect.tsx,
    src/components/checkout/coupon-input.tsx,
    src/components/checkout/device-check.tsx,
    src/components/checkout/email-field.tsx,
    src/components/checkout/express-checkout.tsx,
    src/components/checkout/order-summary.tsx,
    src/components/checkout/pay-button.tsx,
    src/components/checkout/payment-divider.tsx,
    src/components/checkout/payment-error.tsx,
    src/components/checkout/payment-processing.tsx,
    src/components/checkout/payment-success.tsx,
    src/components/delivery/copyable-field.tsx,
    src/components/delivery/delivery-page.tsx,
    src/components/delivery/esim-credentials.tsx,
    src/components/delivery/install-button.tsx,
    src/components/delivery/manual-codes.tsx,
    src/components/delivery/provisioning-error.tsx,
    src/components/delivery/provisioning-state.tsx,
    src/components/delivery/qr-code-display.tsx,
    src/components/delivery/setup-guide.tsx
  </files>
  <read_first>
    - src/styles/globals.css (tokens from Task 1)
    - src/components/ui/input.tsx (reference: already has dark: pattern for form elements)
    - src/components/ui/card.tsx (reference: already has dark: pattern)
    - .planning/phases/09-pwa-and-polish/09-UI-SPEC.md (dark treatment table)
  </read_first>
  <action>
Apply `dark:` variant classes to the 21 checkout and delivery components listed in files. Same mapping rules as Task 2a.

**SPECIAL CASES for this batch:**

- **confetti-effect.tsx**: No visual dark mode change needed (particles are colorful on transparent) — skip
- **Checkout forms** (coupon-input, email-field, device-check): Follow the existing input.tsx dark pattern: `dark:bg-surface-dark dark:border-border-dark dark:text-gray-100`
- **Payment states** (payment-processing, payment-error, payment-success): Background cards get `dark:bg-surface-dark`, text follows mapping rules
- **Delivery components** (copyable-field, esim-credentials, manual-codes, etc.): Card-style containers get `dark:bg-surface-dark dark:border-border-dark`
- **QR code display**: QR code itself stays white (for scannability), but surrounding card gets dark treatment

**PROCESS per file:** Same as Task 2a — read, find classNames, add dark: variants, replace inline color styles.
  </action>
  <verify>
    <automated>echo "Batch 2b check:"; for f in src/components/checkout/order-summary.tsx src/components/checkout/checkout-page.tsx src/components/delivery/delivery-page.tsx src/components/delivery/qr-code-display.tsx src/components/checkout/coupon-input.tsx; do grep -q "dark:" "$f" && echo "$f: OK" || echo "$f: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - src/components/checkout/order-summary.tsx contains `dark:bg-surface-dark`
    - src/components/checkout/checkout-page.tsx contains `dark:`
    - src/components/checkout/coupon-input.tsx contains `dark:`
    - src/components/checkout/pay-button.tsx contains `dark:`
    - src/components/delivery/delivery-page.tsx contains `dark:`
    - src/components/delivery/qr-code-display.tsx contains `dark:`
    - src/components/delivery/copyable-field.tsx contains `dark:`
    - src/components/delivery/setup-guide.tsx contains `dark:`
  </acceptance_criteria>
  <done>All 21 checkout and delivery components have dark: classes applied. QR code container has dark background while QR itself stays white for scannability. Confetti effect confirmed as no-op.</done>
</task>

<task type="auto">
  <name>Task 2c: Apply dark: classes to dashboard, referral, admin components and final verification (21 files)</name>
  <files>
    src/components/dashboard/circular-gauge.tsx,
    src/components/dashboard/dashboard-skeleton.tsx,
    src/components/dashboard/dashboard-tabs.tsx,
    src/components/dashboard/esim-card.tsx,
    src/components/dashboard/esim-grid.tsx,
    src/components/dashboard/low-data-banner.tsx,
    src/components/dashboard/purchase-history-list.tsx,
    src/components/dashboard/purchase-history-row.tsx,
    src/components/dashboard/top-up-modal.tsx,
    src/components/dashboard/top-up-plan-card.tsx,
    src/components/dashboard/usage-timestamp.tsx,
    src/components/delivery/setup-steps.tsx,
    src/components/referral/post-purchase-share-cta.tsx,
    src/components/referral/referral-link-card.tsx,
    src/components/referral/referral-stats.tsx,
    src/components/referral/share-buttons.tsx,
    src/components/admin/coupon-create-form.tsx,
    src/components/admin/coupon-stats-card.tsx,
    src/components/admin/coupon-table.tsx,
    src/components/layout/whatsapp-button.tsx
  </files>
  <read_first>
    - src/styles/globals.css (tokens from Task 1)
    - src/components/ui/card.tsx (reference dark: pattern)
    - src/components/auth/login-form.tsx (reference: already has dark: pattern for auth forms)
    - .planning/phases/09-pwa-and-polish/09-UI-SPEC.md (dark treatment table)
  </read_first>
  <action>
Apply `dark:` variant classes to the 20 dashboard, referral, admin, and remaining components listed in files. Same mapping rules as Task 2a.

**SPECIAL CASES for this batch:**

- **Modals** (top-up-modal.tsx): Overlay gets `dark:bg-overlay-dark`, modal card gets `dark:bg-surface-dark`
- **Skeleton loaders** (dashboard-skeleton.tsx): Change shimmer from light gray to `dark:bg-surface-dark` with `dark:via-border-dark` for the animation
- **Circular gauge** (circular-gauge.tsx): Track color should use `dark:stroke-border-dark` (#2A2A2A), accent fill stays the same
- **Admin components** (coupon-create-form, coupon-stats-card, coupon-table): Card containers get `dark:bg-surface-dark`, table rows get `dark:border-border-dark`
- **WhatsApp button**: Green stays unchanged, no dark override needed — only apply dark: to non-green elements if any

**PROCESS per file:** Same as Task 2a — read, find classNames, add dark: variants, replace inline color styles.

**FINAL VERIFICATION after all files processed:**
Run `grep -rL "dark:" src/components/ --include="*.tsx" | grep -v __tests__ | grep -v json-ld | grep -v auth-provider | grep -v page-transition | grep -v confetti` and confirm it returns 0 files. If any files are missing dark: classes, add them.
  </action>
  <verify>
    <automated>echo "Batch 2c check:"; for f in src/components/dashboard/esim-card.tsx src/components/dashboard/top-up-modal.tsx src/components/dashboard/circular-gauge.tsx src/components/referral/referral-link-card.tsx src/components/admin/coupon-table.tsx; do grep -q "dark:" "$f" && echo "$f: OK" || echo "$f: MISSING"; done; echo "---"; echo "Files still missing dark: (excluding skip files):"; grep -rL "dark:" src/components/ --include="*.tsx" | grep -v __tests__ | grep -v json-ld | grep -v auth-provider | grep -v page-transition | grep -v confetti | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/esim-card.tsx contains `dark:bg-surface-dark`
    - src/components/dashboard/circular-gauge.tsx contains `dark:`
    - src/components/dashboard/top-up-modal.tsx contains `dark:bg-surface-dark`
    - src/components/dashboard/dashboard-skeleton.tsx contains `dark:`
    - src/components/referral/referral-link-card.tsx contains `dark:`
    - src/components/referral/share-buttons.tsx contains `dark:`
    - src/components/admin/coupon-table.tsx contains `dark:`
    - src/components/admin/coupon-create-form.tsx contains `dark:`
    - grep -rL "dark:" src/components/ --include="*.tsx" | grep -v __tests__ | grep -v json-ld | grep -v auth-provider | grep -v page-transition | grep -v confetti returns 0 files
    - No file has `style={{ color: '#616161' }}` or `style={{ color: '#212121' }}` remaining (replaced with Tailwind classes)
    - npm run build completes without errors
  </acceptance_criteria>
  <done>All 60 components have dark: variant classes applied. Toggling dark mode via the existing ThemeToggle produces a fully dark-themed UI across browse, checkout, delivery, dashboard, referral, SEO, admin, and bambu components. No inline color styles remain. Final grep confirms zero untreated files.</done>
</task>

</tasks>

<verification>
- Toggle dark mode in browser — every page (browse, checkout, delivery, dashboard, referral) renders with dark backgrounds
- No component renders with white/light background when dark mode is active
- `grep -rL "dark:" src/components/ --include="*.tsx" | grep -v __tests__ | grep -v json-ld | grep -v auth-provider | grep -v page-transition | grep -v confetti` returns 0 files
- `npm run build` succeeds
</verification>

<success_criteria>
- globals.css has all new dark mode tokens (accent-soft-dark, text-primary-dark, overlay-dark, etc.)
- All 60 components have dark: Tailwind classes applied per the mapping rules
- Bambu mascot components have dark mode glow effect
- No hardcoded inline color styles remain (replaced with Tailwind utilities)
- Build passes with all dark mode classes
</success_criteria>

<output>
After completion, create `.planning/phases/09-pwa-and-polish/09-02-SUMMARY.md`
</output>
